# xv6 Kernel-10: Context Switching

| Field | Value |
| --- | --- |
| Video ID | `1sSanF_y8FY` |
| URL | <https://www.youtube.com/watch?v=1sSanF_y8FY> |
| Source | `docs/reference/scripts/original/10-xv6-kernel-10-context-switching-1sSanF_y8FY.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의 중 하나입니다. 이 비디오에서는 컨텍스트 스위칭(context switching)에 대해 이야기하겠습니다. 트랩(trap)과
시스템 리턴(system return) 명령어가 시간 할당(time slice)을 구현하는 데 어떻게 사용되는지, 그리고 시스템이 한 스레드에서 다른 스레드로 어떻게 전환하는지
설명하겠습니다. 여기 있는 그림부터 시작해 봅시다. 시간은 페이지 아래 방향으로 흐르고 있고, 여기 사용자 스레드가 있습니다.

이 부분은 명령어들이 실행되고 있음을 나타내는데, 어느 시점에서 트랩이 발생하고, 그러면 우리가 커널 안으로 들어가서 커널 모드에서 명령어들을 실행하게 됩니다. 사용자 스레드의
명령어들은 사용자 모드에서 실행되고, 커널 모드에서 실행되는 것은 정의상 모두 커널의 일부입니다. 사용자 모드에서 커널 모드로의 전환은 트랩의 결과로 발생합니다.

이 트랩은 장치가 주의를 요구하면서 일으키는 인터럽트일 수도 있고, 시스템 콜 형태로 사용자 스레드 스스로가 요청하는 것일 수도 있고, 또는 사용자 스레드가 프로그램 예외, 즉
어떤 종류의 에러를 일으켜서 그 결과로 트랩이 발생할 수도 있습니다. 커널이 실행을 끝내고 사용자 스레드로 돌아갈 준비가 되면 s-ret, 즉 시스템 리턴 명령어가 실행되고,

그 명령어가 우리를 다시 사용자 모드로 데려갑니다. 이것은 장치가 주의를 요구하는 상황일 수 있습니다. 만약 이것이 인터럽트라면, 사용자 스레드는 이 트랩이 발생했다는 사실을 전혀
인식하지 못할 것이고, 그냥 계속 명령어를 실행하면서 트랩과 리턴이 그 시점에 일어났다는 것 자체를 전혀 눈치 채지 못합니다. 트랩이 발생하는 시점에, 사용자 스레드의 모든
레지스터는

저장되어야 하며 실제로 저장됩니다. 그리고 시스템 리턴 시점에 그 레지스터들은 다시 복원되거나, 말하자면 다시 로드됩니다. 그래서 사용자 스레드는 정확히 중단되었던 지점부터 다시
실행을 이어 갑니다. 이렇게 해서 사용자 스레드는 시간에 따라 일련의 시간 할당들을 경험하게 됩니다. 여기 하나, 여기 또 하나, 또 다른 하나가 있습니다. 이 트랩은 타이머
인터럽트의 결과일 수도 있는데, 그 경우에는

커널이 이 구간에서 다른 어떤 스레드들과 관련된 다른 일을 하러 갔다가, 결국에는 다시 사용자 스레드 x로 돌아오게 됩니다. 만약 트랩이 타이머 인터럽트라면, 이 시점에서 시간
할당이 끝나는 것이고, 어느 시점에 커널이 사용자 스레드에게 또 다른 시간 할당을 주기로 결정하면, 그때 다시 사용자 스레드로 리턴하게 됩니다. 이제 다른 관점에서 한 번
보겠습니다.

여기 왼쪽에 빨간색으로 스케줄러 스레드를 표시했고, 여러 개의 스레드가 있습니다. 아래쪽에 x가 실행 중이고, 여기에는 스레드 y가 실행되고 있습니다. 스레드 x의 관점에서 보면,
쭉 실행하다가 트랩이 발생하고, 어느 시점에 리턴이 일어나서 다시 실행을 이어 가다가, 또 트랩이 발생하고, 또 어느 시점에 리턴이 일어나서 다시 실행을 재개합니다.

커널이 활성화되어 있는 동안, 커널은 스레드 y에게 시간 할당을 실행하기로 선택할 수 있습니다. 그러면 시스템 리턴은 스레드 y로 돌아가고, y는 실행을 계속하다가 트랩이 발생할
때까지 실행합니다. 이 그림에서 흥미로운 점 중 하나는, 트랩 뒤에 리턴이 따라온다는 점인데, 이는 마치 함수 호출 뒤에 리턴이 따라오는 것과 비슷합니다. 특히 시스템 콜의 경우,
사용자 스레드는 자신이 커널을 호출하고 있고,

커널이 결국 자신에게 리턴해 온다고 상상할 수 있습니다. 그런데 여기에서는 상황이 조금 다른데, 리턴과 트랩의 관계가 약간 뒤바뀌어 있습니다. 이 트랩은 즉시 리턴으로 이어지지
않고, 그 사이에 다른 것들이 일어납니다. 스케줄러의 관점에서 보면, 우리는 시스템 리턴 명령어로 스레드 x 안으로 들어가 그 스레드의 시간 할당을 시작하고, 시간 할당은 트랩

명령어와 함께 끝납니다. 이제 트랩이 발생했을 때 무슨 일이 일어나는지 조금 살펴보겠습니다. 이 그림으로 돌아가 봅시다. 트랩이 발생하면 커널이 실행되고, 그 다음 시스템 리턴이
있습니다. 이 그림은 제가 ‘로드맵(road map)’이라고 부르는 것인데, 꽤 복잡해 보입니다. 하지만 기본적으로는, 사용자 코드가 실행되다가 트랩이 발생하고, 그리고 아래쪽
어딘가에서

리턴이 일어나고, 그러면 사용자 코드가 다시 이어서 실행된다는 내용입니다. 이 사이에 많은 일들이 일어나고 있고, 이 부분은 나중에 자세히 다시 다루겠습니다. 하지만 그 중 일부를
보면, 트랩이 발생하는데 그것은 타이머 인터럽트나 장치 인터럽트의 결과일 수 있습니다. 이 둘은 모두 비동기적입니다. 즉, 사용자 코드 바깥에서 일어나며, 사용자 코드는 그것들이
발생했다는 사실을 인식하지 못합니다.

또는 시스템 콜일 수도 있습니다. 사용자 코드가 커널에 무언가를 요청했을 수도 있습니다. 혹은 사용자 코드에 어떤 에러 조건이 발생했을 수도 있습니다. 여기서 보면, 레지스터와
프로그램 카운터를 저장하는 작업이 아주 초기에 일어나는 것을 볼 수 있고, 그리고 맨 아래쪽, 시스템 리턴을 하기 직전에 사용자 레지스터를 복원하는 작업을 하고, 그다음

sret 명령어를 실행합니다. 그 사이에는 무슨 일이 벌어질까요? 이 안에는 큰 if 문이 있다고 볼 수 있고, 특정 장치가 주의를 요구할 수도 있습니다. 이런 경우 해당 장치에
대한 핸들러를 실행해야 합니다. 또는 사용자 코드가 시스템 콜을 요청했을 수도 있고, 그러면 그걸 처리해야 합니다. 혹은 이것이 타이머 인터럽트였을 수도 있고, 프로그램 예외였을
수도 있습니다. 하지만 최소한 이 세 가지 경우에는 모두

여기 아래쪽으로 내려와서, 결국 사용자 코드로 돌아가게 됩니다. 다음으로 이야기하고 싶은 것은, 멀티코어 시스템에서 여러 일이 동시에 벌어질 수 있다는 점입니다. 방금 전에 보여준
그림에서는 스케줄러가 스레드 x를 실행했다가, 그다음 스레드 y, 다시 스레드 x를 실행하는 모습을 보여줬는데, 이는 단일 코어 시스템의 경우입니다.

지금 이 그림에서는 거의 비슷한 일을, 두 개의 코어에 대해 보여주고 있습니다. 여기 파란색으로 한 코어의 스케줄러가 보이고, 저쪽 빨간색으로 또 다른 코어가 보입니다. 그리고
여기 프로세스 x, y, z, w가 있습니다. 보시면, 스케줄러가 이 시점에 프로세스 z에게 시간 할당을 주기로 결정하고,

시스템 리턴을 통해 프로세스 z 안으로 들어갑니다. 그리고 프로세스 z에서 트랩이 발생할 때까지 계속 실행하다가, 트랩이 발생하면 이 코어의 스케줄러로 되돌아갑니다. 그다음
스케줄러가 다른 프로세스를 선택해 다른 일을 할 수도 있습니다. 그동안 저쪽 코어는 계속 실행을 하고 있고, 아마 이 시점에서 프로세스 z에게 시간 할당을 주기로 결정할 수도
있습니다. 그러면 그 시점에 시스템 리턴을 통해 다시 프로세스 z 안으로 들어가고, 프로세스 z는 또 하나의 시간 할당을 받게 됩니다. 프로세스 z의 관점에서 보면,

이 코어에서 하나의 시간 할당을 받았고, 한동안 기다렸다가,

그 다음 다른 코어에서 또 하나의 시간 할당을 받은 셈입니다. 여기에 추가적인 코어들도 더 있을 수 있습니다. 따라서 어떤 프로세스에 대한 시간 할당은 어느 코어에서든 일어날 수
있습니다. xv6의 경우에는, 이게 거의 우연에 가깝게 결정됩니다. 그렇다면 이 트랩에서는 무슨 일이 일어날까요? 이 트랩은 우리가 다시 커널 안으로 들어가는 시점입니다. 이때 이
컨텍스트 스위치에서 프로세스 z의 레지스터들이 저장됩니다.

즉, 코어 상에서의 z의 모든 상태가 저장됩니다. 코어에서의 상태란, 일반 목적 레지스터들과 프로그램 카운터를 말합니다. 그리고 나중에 어느 시점에서, 이 빨간 코어가 z에게 시간
할당을 주기로 결정합니다. 그래서 또 다른 컨텍스트 스위치를 통해 프로세스 z 안으로 들어가고, 그 시점에 프로세스 z가 실행하는 데 필요한 레지스터들을 코어 0 안으로 로드하게
됩니다.

이 시점에서 프로세스 Z의 상태가 저장되면, 그 상태는 메모리에 저장됩니다. 이 메모리는 모든 코어가 함께 사용하는 공유 메모리이고, 컨텍스트 스위치 때 그 공유 메모리에
접근해서, 거기에 저장되어 있던 프로세스 C의 상태를 다시 어떤 코어의 레지스터로 로드하게 됩니다. 프로세스 C의 상태를 저장해 두는 이 공유 메모리는 당연히 매우 중요하고,
반드시 락으로 보호되어야 합니다.

그리고 실제로 락으로 보호됩니다. 그래서 이 컨텍스트 스위치에서 상태를 저장하는 동안에는 락을 잡고 있게 되고, 그 작업이 모두 끝난 다음에야 그 락을 풀어서 다른 데서 쓸 수
있게 됩니다. 이렇게 해야 이 빨간 코어가 프로세스 C의 타임 슬라이스를 너무 일찍 시작하려고 시도하는 일을 막을 수 있습니다. 빨간 코어는 이 락을 획득한 다음에야 비로소

프로세스 C의 레지스터들을 로드하고, 프로세스 Z로의 컨텍스트 스위치를 시작할 수 있습니다. 지금까지는 시간 흐름을 페이지 아래쪽으로 흘러가는 식으로 그렸습니다. 트랩이 커널로
들어가고, 그 다음에 다시 리턴하는 그림입니다. 사용자 스레드의 관점에서 보면, 위쪽 이 다른 그림에서는 약간 다르게 그리기도 합니다. 여기서는 시간 흐름이 오른쪽으로 가는 것으로
되어 있습니다. 그래서 사용자 모드에서 커널 모드로 트랩이 발생하는 것을 볼 수 있고,

그 다음 시스템 리턴에서 커널 모드에서 사용자 모드로의 컨텍스트 스위치가 일어나는 걸 볼 수 있습니다. 만약 작업이 단순한 경우에는 대략 이런 그림이 됩니다. 예를 들어 장치가
서비스를 요청하는 경우, 트랩은 인터럽트이고, 우리는 장치를 처리하고, 그 장치를 다루는 일을 추가적인 컨텍스트 스위치 없이 처리할 수 있어서, 바로

인터럽트되었던 사용자 모드로 곧장 돌아갈 수 있습니다. 일부 시스템 콜의 경우에도, 추가적인 컨텍스트 스위치 없이 시스템 콜을 처리하고, 곧장 사용자 모드의 코드로 되돌아갈 수
있습니다. 이런 경우는 꽤 효율적입니다. 하지만 상황에 따라 그렇게 할 수 없는 경우들도 있습니다. 그래서 여기에는 약간 다른 상황을 그려두었습니다. 여기서는 우리가 사용자
모드에서 계속 실행하고 있다가,

트랩을 수행해서 이제 커널 안에서 실행하게 됩니다. 하지만 어떤 의미에서는 여전히 같은 프로세스입니다. 아직 스케줄러 스레드 안으로 들어온 건 아닙니다. 스케줄러 스레드는 여기
아래쪽에 갈색으로 표시된 별도의 스레드입니다. 여기에서 우리는 이 사용자 프로세스를 위한 커널 스레드 안에 있습니다. 만약 다른 스레드를 스케줄하고 싶다고 결정하면,

두 번째 컨텍스트 스위치를 하게 됩니다. 즉 스케줄러 스레드로 들어가서, 다른 프로세스를 하나 선택하고, 그 프로세스의 커널 스레드로 들어가게 됩니다. 각 프로세스는 사용자 부분과
커널 부분을 갖습니다. 여기 빨간색으로 한 개의 스레드를 보여주고 있습니다. 이것이 사용자 모드에서 실행되는 사용자 부분이고, 이것이 커널 모드에서 실행되는 커널 부분입니다.
하지만 둘 다 합쳐서

하나의 스레드라고 볼 수 있습니다. 그리고 파란색으로 두 번째 스레드도 하나 보여주고 있습니다. 스케줄러 스레드는 다른 스레드이고, 프로세스에서 스케줄러 스레드로의 컨텍스트
스위치가 있고, 다시 되돌아오는 또 다른 컨텍스트 스위치가 있습니다. 이것은 여기 위쪽에서 사용자 모드에서 커널 모드로 넘어갈 때 일어나는 것과는 약간 다릅니다. 사용자 모드에서
커널 모드로 전환할 때는,

일반 목적 레지스터와 프로그램 카운터를 모두 저장해야 합니다. 모든 것을 저장해야 합니다. 하지만 여기 아래쪽에서 스케줄러 스레드로 전환할 때는, 우리는 이미 커널 안에 있고, 몇
가지 가정을 할 수 있습니다. 모든 레지스터를 전부 저장할 필요는 없고, 이 컨텍스트 스위치는 조금 다르게 동작합니다. 이 컨텍스트 스위치와 이 컨텍스트 스위치는 둘 다

swtch 혹은 switch라고 부를 수 있는 어셈블리 언어 함수에서 처리됩니다. 이건 어셈블리 코드이고, 이 두 함수에서 호출됩니다. 스케줄러가 어느 프로세스로 갈지 선택할 때,
그 작업은 scheduler라는 함수 안에서 이루어지고, 이 함수는 switch를 호출해서 그 컨텍스트 스위치를 수행합니다. 또 하나의 함수가 있는데, 이름을 sched 라고
합시다, s-c-h-e-d이고,

이 함수는 어떤 프로세스 스레드의 커널 부분에서 스케줄러로 들어가기 위해 호출됩니다. 이 함수 역시 이 switch 함수를 호출합니다. 그래서 switch 함수는 기본적으로 한
스레드의 레지스터들을 저장하고, 다음 스레드의 레지스터들을 로드합니다. 그리고 말했듯이 어셈블리 코드로 작성되어 있습니다. 컨텍스트 스위칭과 트랩, 시스템 리턴에 대한 내용은
여기까지입니다.

나중에 이후 비디오들에서 이 거대한 로드맵으로 다시 돌아오겠습니다.
