# xv6 Kernel-14: Trap Handling

| Field | Value |
| --- | --- |
| Video ID | `k4f2vHCV5iQ` |
| URL | <https://www.youtube.com/watch?v=k4f2vHCV5iQ> |
| Source | `docs/reference/scripts/original/14-xv6-kernel-14-trap-handling-k4f2vHCV5iQ.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의의 일부입니다. 이 그림에서는 사용자 코드에서 실행을 하다가 트랩이 발생하고, 커널에서 어떤 코드를 실행한 다음, 시스템
반환(system return) 명령을 실행해서 다시 사용자 모드로 돌아가는 과정을 보여줍니다. 이번 비디오에서는 우리가 커널 모드에 있을 때 무슨 일이 일어나는지에 대해
이야기하겠습니다. 특히 데이터 구조에 대해 이야기하고,

proc 파일의 코드를 같이 살펴보겠습니다. 그럼 제가 로드맵이라고 부르는 이 그림부터 시작해 보죠. 이 그림은 트랩이 발생한 시점부터 SR 명령까지 사이에 일어나는 모든 일을
보여줍니다. 이제 이것을 좀 더 자세히 살펴보겠습니다. 트랩은 비동기 인터럽트의 결과로 발생할 수 있습니다. 예를 들어 타이머가 처리를 요구한다든가, 어떤 IO 장치가 처리를
요구한다든가 할 수 있습니다. 또는 사용자 모드 코드가 시스템 콜을 해서 생기는 동기 예외일 수도 있고, 어떤 종류의 프로그램 오류 때문일 수도 있습니다.

하드웨어는 몇 가지 일을 합니다. 인터럽트를 비활성화하고, supervisor 모드로 전환하고, 프로그램 카운터를 scpc 레지스터에 저장하고, 정확히 무엇 때문에 트랩이
발생했는지에 관한 정보를 sCAUSE 레지스터에 저장하고, PC를 uservec 함수의 주소로 적재합니다. stvec이라는 제어/상태 레지스터가 하나 있는데, 거기에 모든 종류의
트랩을 처리하는 코드의 주소가 들어 있습니다. 그래서 그 레지스터에 의해 트램펄린(trampoline) 페이지에 위치한 이 어셈블리 코드로 분기하게 됩니다. 이 트램펄린 페이지는
기억하듯이 모든 주소 공간, 즉 사용자 주소 공간과

커널 주소 공간 모두에 매핑되어 있고, 사용자 상태를 처음으로 저장하는 일을 합니다. 구체적으로 무엇을 하냐 하면, 모든 범용 레지스터와 프로그램 카운터를 trapframe 안에
저장합니다. 각 프로세스는 자기만의 고유한 trapframe을 가지고 있는데, 이것은 사용자 주소 공간의 두 번째로 높은 페이지에 매핑되어 있습니다. 이 코드는 사용자 주소
공간에서 실행됩니다. 그런 다음

커널 코드를 실행할 준비를 합니다. 모든 스레드는 자신만의 스택이 필요하므로, 스택 포인터 레지스터를 초기화해야 합니다. 그리고 코어 번호를 담고 있는 TP 레지스터도
초기화합니다. 이 두 레지스터는 사용자 코드에서는 다른 값들을 가지고 있을 것이고, 그 값들을 신뢰할 수 없기 때문에 새로 적재해야 합니다. 하지만 이 두 개 이외의 것들은 굳이
적재할 필요가 없습니다. 그 다음에는

satp 제어/상태 레지스터에 커널의 페이지 테이블 포인터를 적재합니다. 이렇게 하면 효과적으로 커널의 가상 주소 공간으로 전환됩니다. 이 시점에서 usertrap이라는 함수로
점프하는데, 이 함수는 C 코드입니다. 이 함수는 if 문 등을 가지고 현재 무슨 일이 일어나고 있는지 판단합니다. 하지만 먼저 또 다른 트랩 벡터의 포인터를 stvec에
저장합니다. 이전에 stvec에는 여기 있는 이 함수의 포인터가 들어 있었지만, 만약 커널 모드에서 실행 중일 때 트랩이 발생하면 다른 방식으로 처리하길 원합니다. 그래서
stvec을 여기 있는 것으로 갱신합니다. 그런 다음 sCAUSE 레지스터를 보고 프로그램 예외인지 여부를 판단합니다. 만약 오류라면 메시지를 출력하고

exit 함수를 호출해서 이 프로세스를 아예 종료합니다. 만약 장치가 처리가 필요하다면, 여기 있는 deviceintr 함수를 호출해서 그 처리를 담당하게 합니다. 각 프로세스에는
killed라는 불리언 값이 하나 연결되어 있고, 이것이 설정되면 그 프로세스를 종료해야 합니다. 만약 어떤 이유로 이 프로세스가 죽어야 한다면, 다른 누군가가 이 불리언을
true로 설정했을 것이고, 이 시점에서 그것을 검사합니다. 그리고 만약 이 프로세스가 죽어야 하는 상황이라면, 장치 인터럽트를 처리한 뒤 exit를 호출합니다. 하지만 아마
대부분의 경우에는 그냥 아래로 계속 진행할 것입니다. 만약 인터럽트가 타이머에 의한 것이었다면, 이 타임 슬라이스의 끝이므로 다른 프로세스들에게 실행 기회를 주어야 합니다.

따라서 killed 플래그를 검사해서, 만약 그렇다면 그냥 exit를 호출합니다. 그렇지 않다면 yield를 호출합니다. yield는 결국에는 반환되지만, 그 사이에 다른
프로세스들이 실행할 수 있도록 만들어 줍니다. yield는 스케줄러를 호출하고, 여기 표시한 다른 프로세스들이 실행되도록 합니다. 그러다가 결국 다시 우리가 시간 조각을 할당받아서
yield로 되돌아오고, 그 다음 yield가 반환되고,

계속해서 진행하여 다시 사용자 모드의 프로세스로 돌아갑니다. 만약 인터럽트, 혹은 트랩의 원인이 시스템 콜이었다면, 인터럽트를 활성화합니다. 즉, 이 스레드 자신도 인터럽트될 수
있도록 합니다. 예를 들어 시스템 콜을 처리하는 동안 어떤 장치가 주의를 요구할 수 있고, 인터럽트가 활성화되어 있으므로 실제로 그런 일이 일어날 수 있습니다. 우리가 잠시 다른
일을 하러 갔다 올 수도 있습니다. 하지만 어쨌든 시스템 콜을 처리하고 돌아옵니다. 그 시점에 누군가가 이 프로세스를 종료해 달라고 요청했다면 exit를 호출합니다. 그렇지 않다면
계속 진행합니다. 그리고 마지막으로, 어떤 경우이든 C 함수인 usertrapret 또는 usertrapreturn을 호출하는데, 이것이 사용자 모드 코드로 돌아가는 과정을
시작합니다. 이 함수는 여기서 인터럽트가 활성화되었을 수도 있으니 인터럽트를 비활성화하고,

stvec을 uservec을 가리키도록 설정합니다. 이는 다음 트랩을 준비하기 위한 것입니다. 다음 트랩이 발생했을 때 uservec을 가리키도록 stvec을 설정해야 합니다.
그리고 trapframe 안에 스택 포인터 값과 우리가 실행 중인 코어 번호를 저장해야 합니다. 기억하겠지만, 여기에서 이 값들을 레지스터에 적재했었는데, 여기서는 그것들을 다시
설정해서 다음 트랩이 발생했을 때 사용할 준비를 하는 것입니다. SR 명령은 scpc 레지스터에 의존하므로, 저장해 두었던 프로그램 카운터를 꺼내와서 scpc 레지스터에 넣습니다.
그런 다음 어셈블리 코드로 점프하는데, 이 어셈블리 코드는 트램펄린 페이지에 위치해 있고 이름은 userret입니다. 이 코드는 과정을 마무리하고 SR 명령을 실행합니다. 이제
우리가

트램펄린 페이지에서 실행하고 있으니, 이는 모든 사용자 주소 공간의 맨 위 페이지입니다. 그래서 커널 주소 공간에서 사용자 주소 공간으로 전환할 수 있습니다. 이를 위해 satp
레지스터에 사용자 페이지 테이블의 포인터를 적재합니다. 마지막으로 사용자 레지스터들을 복원하고, 상태 레지스터에 몇 비트를 설정합니다. SR 명령은 상태 레지스터를 사용하므로,
거기에 비트를 설정해서 SR 명령이 사용자 모드로 가길 원한다는 것을 알려 줍니다. 즉, 이전 특권 레벨(previous privilege level)이 사용자 모드가 되도록
설정합니다. 그러면 사용자 모드로 돌아가게 됩니다. 또 이전 인터럽트 활성 플래그를 enabled로 설정합니다. 이 값은 현재 인터럽트 활성 플래그로 복사될 것입니다. 따라서
SRET을 실행하면 인터럽트가

활성화되고, 우리는 사용자 모드에 있게 되며, 사용자 레지스터와 사용자 페이지 테이블을 사용해서 실행하게 됩니다. 자, 이제 여기에 관련된 데이터 구조들을 살펴보겠습니다. 먼저
trapframe을 보겠습니다. 우리가 사용자 모드에서 실행하고 있을 때, scratch라는 제어/상태 레지스터가 있는데, 이것이 이 trapframe을 가리키고 있습니다.

trapframe이 있고, 이게 유용한데, 여기에는 우리가 유저 모드 레지스터를 저장할 위치가 들어 있습니다. 그래서 여기 위에서, 레지스터와 PC를 trapframe에 저장할 때
그 일이 일어나고, 그 정보가 여기 저장됩니다. 31개의 범용 레지스터가 여기에 저장되는데, 32번째 레지스터, 즉 레지스터 0은 항상 0으로

하드와이어되어 있으니까 31개만 저장하면 됩니다. 그리고 프로그램 카운터, 이전 프로그램 카운터가 여기에 저장됩니다. 그래서 이게 유저 모드 스레드의 상태입니다. 그런 다음 커널
스레드의 상태를 로드합니다. 그래서 커널의 페이지 테이블을 가리키는 포인터가 있고, 이건 아주 편리합니다. 그게 바로 여기에 있습니다. 그리고 이 특정 프로세스가

사용하게 될 스택을 가리키는 포인터가 있고, 그다음으로 usertrap의 주소가 있습니다. 이건 사실 감독자 모드 스레드의 상태라기보다는, trapframe 안에 같이 넣어둔
값입니다. 마지막으로 hart ID, 즉 코어 번호가 있는데, 트랩이 발생했을 때 TP 레지스터에 로드해야 하므로 필요합니다. 이게 trapframe에서 벌어지는 일이고, 이
밖에도 몇 가지

매우 중요한 자료구조가 더 있습니다. CPU라는 자료구조가 있고, CPUs라는 배열이 있으며, 코어마다 하나씩 총 8개가 있습니다. 이 배열의 각 원소는 구조체이고, 그 구조체는
이런 필드들을 포함합니다. 어떤 유저 모드 프로세스가 그 CPU에서 실행 중이라면, 그 프로세스를 설명하는 proc 구조체를 가리키는 포인터가 여기에 들어 있습니다. 만약 아무것도
실행

중이 아니거나, 좀 더 정확히 말하면 스케줄러 스레드가 실행 중이라면, 이 proc 필드는 0 또는 null입니다. 그리고 앞에서 인터럽트에 대해 이야기하면서, 인터럽트를 어떻게
중첩해서 막을 수 있는지 설명했는데, 처음에 인터럽트가 활성화되어 있었는지를 기억하고, 우리가 인터럽트를 비활성화하고 싶으면 그 초기 값을 기억해 두고 카운터를 증가시킵니다. 그런
다음

되돌릴 때 카운터를 감소시키고, 최종적으로 인터럽트 활성화 비트의 원래 상태로 돌아갈 수 있습니다. 마지막으로 context라고 부르는 또 다른 레지스터 저장 공간이 있습니다.
제가 이전에 충분히 명확히 말하지 않았던 부분이 하나 있는데, 우리가 어떤 프로세스를 실행할 때, 유저 모드 또는 커널 모드에서 실행하고 있지만, 이 사이를 전환할 수 있습니다.
여기 그림에는

나와 있지 않지만, 우리는 감독자 모드 스레드로 전환할 수 있습니다. 그러면 그 프로세스를 뒤에 두고 떠나는 셈입니다. 그래서 지금까지 여기서 이야기한 모든 것은 어떤 프로세스의
일부분으로서 실행하고 있는 상황입니다. 하지만 여기, 아마도 여기나 yield 안에서, 다른 프로세스의 타임 슬라이스로 넘어갈 수 있습니다. 그래서, 여기서 일어나는 컨텍스트

스위치는 여기서 일어나는 컨텍스트 스위치와는 다른 종류의 컨텍스트 스위치입니다. 만약 이 프로세스를 떠나서 스케줄러 스레드로 들어가면 레지스터를 저장해야 하는데, 이 레지스터 저장
공간이 바로 그 용도입니다. 그리고 여기서, 모든 레지스터를 저장할 필요는 없다는 점을 짚고 넘어가겠습니다. 일부 레지스터만 저장하면 되고,

어떤 레지스터들만 저장하면 되는지는 나중에 이야기하겠습니다. 만약 이 코어가 어떤 프로세스의 스레드를 실행 중이라면, 그 프로세스를 설명하는 구조체를 가리키는 포인터가 있고, 그
구조체에는 여러 필드가 있습니다. 이걸 빠르게 훑어보겠습니다. lock 필드가 있고, 프로세스의 현재 상태가 있습니다. unused일 수도 있고, used일 수도 있고,
sleeping일 수도 있고, runnable일 수도 있는데, runnable은 실행 준비가 되어 있다는 의미이고,

스케줄링되었지만 타임 슬라이스를 기다리고 있을 수도 있고, 실제로 실행 중일 수도 있습니다. 또는 프로세스가 종료된 뒤 exit를 호출한 후에는, 이 구조체가 재활용되어
unused 상태가 되기 전까지 잠시 zombie 상태에 머무릅니다. 만약 어떤 락을 기다리는 중이라면, 즉 이 스레드가 sleeping 상태라면, 이 channel 필드가 정확히
무엇을

기다리고 있는지를 설명합니다. 여기 앞에서 언급했던 killed 불리언이 있고, 프로세스가 종료된 후에는 exit 상태가 있는데, 그게 이 xstatus 필드입니다. 그리고 모든
프로세스에는 ID 번호가 있고, 순차적으로 ID가 부여됩니다. 이 lock은 이러한 필드들을 보호합니다. 좀 더 확대해서 보면, 이 lock, 이 스핀락이 이 필드들을 보호합니다.
다시 말해, 이 필드들 중 어느 하나를

수정하거나 접근하려면, 스핀락을 먼저 잡아야 합니다. 또한 부모를 가리키는 포인터가 있습니다. 이건 다른 proc 구조체 하나를 가리키는 포인터입니다. 최대 64개의 프로세스가
있을 수 있습니다. 정확히는 64개의 프로세스 구조체가 있고, 그 중 일부는 unused일 수 있지만, 동시에 실행 중이거나 runnable한 프로세스가 최대 64개까지 있을 수
있습니다. 각 프로세스마다 구조체가 하나씩 있고, 이것이

그 구조체입니다. 여기서는 한 프로세스에 대한 구조체만 보여 주고 있고, parent는 이 프로세스의 부모 프로세스를 나타내는 구조체를 가리킵니다. 이 프로세스의 부모를 나타내는
구조체를 가리키는 것이죠. 이 프로세스의 커널 측면을 위한 스택이 있습니다. 그러니까 우리가 여기서 실행할 때, 즉 여기서 실행할 때 커널이 사용할 스택이 필요합니다. 64개

프로세스 각각은 분리된 스택이 필요합니다. 그 스택이 여기 이 필드로 주어지고, 이 포인터가 그 스택을 가리킵니다. 이건 가상 주소 공간의 크기이고, 여기에는 사용자 주소 공간용
페이지 테이블을 가리키는 포인터가 있습니다. 그리고 trapframe 페이지를 가리키는 포인터가 있습니다. 각 프로세스는 물리 메모리 상의 고유한 페이지를 하나씩 가지고 있고, 이
포인터가 그 페이지를 가리킵니다. 이 페이지들은

모두 주소 공간의 위에서 두 번째 페이지에 매핑되지만, 이 포인터는 물리 페이지를 가리킵니다. 그리고 여기에는 또 하나의 context가 있습니다. 이건 또 다른 저장 영역입니다.
스케줄러 스레드와 프로세스 간에 왔다 갔다 할 때, 이 두 개의 저장 영역을 사용할 수 있습니다. 그리고 여기에 열린 파일들을 위한 배열과 현재 작업 디렉터리가 있습니다.

심지어 이 프로세스의 이름을 위한 공간도 있어서, 프로세스에 이름을 붙일 수 있습니다. 이제 이 그림들을 봤으니, 이 파일 proc 안의 정의들을 살펴보겠습니다. 이게
context이고, 여기와 여기에서 모두 사용된다고 했던 그 것입니다. ra, 즉 return address 레지스터가 들어 있고, 이건 다시 돌아갈 PC, 즉 어디로

되돌아갈지를 나타냅니다. 그리고 스택 포인터가 있고, 그다음 우리가 신경 써야 할 건 S 레지스터뿐입니다. S 레지스터는 12개가 있고, 이 레지스터들만 저장하면 됩니다. 그래서
이들을 저장하기 위한 영역만 있으면 됩니다. 다음으로 CPU 구조체, 바로 이 CPU 구조체를 보겠습니다. 여기 보면, 프로세스가 실행 중일 때는 그 프로세스를 가리키는 포인터가
들어 있고, 그렇지 않을 때는 null이 들어 있습니다. 그리고 여기에는

제가 앞에서 보여 준 저장 영역인 context 필드가 있습니다. 그리고 push_off의 깊이를 나타내는 두 필드가 있습니다. push_off를 호출할 때마다 그 카운터가
증가합니다. 그리고 이 필드는 최초의 push_off 이전에 인터럽트가 활성화되어 있었는지를 기억합니다. 그리고 여기에는 CPU 배열이 있습니다. CPU가 8개 있으므로, 이
구조체 원소 8개로 이루어진 배열입니다.

다음으로 살펴보고 싶은 구조체는 trapframe입니다. 여기 나와 있는 정보들을 하나하나 읽지는 않겠지만, trapframe 안에서 무슨 일이 일어나는지 설명하겠습니다.
trapframe은 우리가 어셈블리 코드에서 접근할 것이기 때문에, 각 필드의 오프셋을 실제로 신경 써야 합니다. trapframe은 한 페이지(page)이고 페이지 경계에
정렬되어 있습니다. 그리고 여기 주석에 각 필드에 대한 오프셋이 나와 있습니다.

각 필드를 보면, 전부 64비트 숫자라는 것을 알 수 있습니다. 그리고 제가 앞에서 언급했던 필드들이 여기 나와 있습니다. 아까 보여 드렸던 trapframe 그림을 다시 가져와
보면, 그걸 보면서 같이 따라갈 수 있습니다. satp, 스택 포인터, 그 다음에 trap(트랩) 관련 필드들, 제가 그림에서는 약간 다른 순서로 정리했지만, 저장된 PC, 그리고
코어 번호가 있고, 그 다음에

세어 보면 레지스터가 31개 전부 다 여기 들어 있습니다. 그래서 레지스터 31개가 전부 저장됩니다. 혹시 궁금하다면, 레지스터들이 조금 이상한 순서로 나열되어 있다는 걸 볼 수
있습니다. 예를 들어 위쪽에 T 레지스터 몇 개, 그 옆에 s 레지스터들, 또 그 옆에 a 레지스터들, 다시 s 레지스터들, 그리고 아래쪽에 T 레지스터들이 더 있습니다. 이게
RISC-V 프로세서 안에서 실제로 조직되어 있는 방식입니다. 하지만

우리 입장에서는 그냥 이름으로 접근할 뿐이므로, 실제로 이것이 X1, X2, X3 레지스터인지 하는 구체적인 번호는 크게 신경 쓰지 않습니다.

이제 다음으로 proc 구조체를 살펴보겠습니다. 이 구조체가 바로 여기 있는 구조체입니다. 이 구조체를 볼 건데, 화면 아래쪽에 state가 보입니다. 이 state의 타입은
procstate이고, 여기에 가능한 상태들이 나와 있습니다.

unused, used, sleeping, runnable, running, zombie입니다. 어떤 프로세스가 실제로 코어에서 실행 중일 때 그 프로세스의 상태(또는
state)는 running입니다. 실행할 준비는 끝났지만, 불행히도 지금 당장 사용할 수 있는 코어가 없어서 시간 할당(Time slice)을 기다리며 그냥 대기하고 있는 상태일
때는 runnable입니다.

어떤 것을 기다리면서 블록된 상태라면 sleeping 상태로 들어갑니다. 그리고 종료되었지만 아직 정리되지 않은 상태에서는 zombie가 됩니다. 이 구조체들은 재사용됩니다.
xv6에는 이런 구조체가 64개뿐이기 때문에, 프로세스가 죽은 뒤에는 이 구조체를 다시 써야 합니다. 프로세스가 죽은 뒤에도 잠시 동안은 zombie 상태로 남아 있는데, 그
이유는 부모 프로세스나 그 프로세스를 wait 하는 쪽에서 이 exit status, 즉 종료 상태를 이 exitstatus 필드에서 가져갈 수 있어야 하기 때문입니다.

그 상태가 부모(또는 기다리는 쪽)에 의해 회수된 다음에야, 이 구조체는 재사용될 수 있고 unused 상태가 됩니다. 그리고 unused에서 다른 상태로 넘어가기 전에 중간에
used라는 중간 상태를 거칩니다.

여기에는 lock이 있습니다. 이 lock은 매우 중요합니다. 프로세스를 다룰 때는 lock을 잡아야 하고, 특히 이 아래에 있는 필드들을 사용할 때는 반드시 이 lock을 잡아야
합니다. 여기 위에서 보았던 state 필드가 있고, 그 다음에 channel 필드가 있습니다.

channel은 0일 수도 있지만, sleeping 상태인 경우에만 의미가 있습니다. 이 필드는 우리가 무엇을 기다리면서 잠들어 있는지 나타냅니다. 어떻게 동작하는지는 나중에
자세히 다루겠지만, 기본적으로는 우리가 기다리고 있는 대상을 식별하는 식별자입니다.

그리고 여기에는 이 프로세스를 종료하라고 누군가가 요청했는지를 나타내는 Boolean 값이 있습니다. 그 다음 정수 하나는 프로세스가 스스로의 종료 상태(exit status)를,
자신을 wait하는 주체에게 전달할 때 사용됩니다. 또 이 정수 하나는 단순히 프로세스 ID 번호입니다.

여기에는 또 다른 proc 구조체를 가리키는 포인터가 있고, 이것은 부모 프로세스를 가리킵니다. 이 필드를 다룰 때는 wait lock을 잡고 있어야 합니다.

그 아래에는 이른바 private 필드들이 있습니다. 이 필드들을 다룰 때는 lock을 잡을 필요가 없습니다. 이 필드는 커널 스택의 주소입니다. 그 다음 필드는 프로세스의 가상
주소 공간 크기입니다. 그리고 이 필드는 그 가상 주소 공간을 설명하는 페이지 테이블을 가리키는 포인터입니다.

여기는 trampoline이 사용할 U 데이터 페이지를 가리키는 포인터입니다. 이 페이지는 두 번째로 높은 가상 페이지에 매핑될 것이고, 여기서 그 페이지를 가리키는 포인터를
보관합니다.

여기는 context 영역입니다. 컨텍스트 스위치를 할 때 이 프로세스는 이 context를 사용하게 됩니다. 마지막으로, 열린 파일들을 위해 사용되는 배열이 있습니다.
NOFILE은 동시에 열 수 있는 파일의 최대 개수입니다. 그리고 현재 작업 디렉터리를 가리키는 필드가 있습니다. 마지막으로, 프로세스 이름을 저장하는 16바이트짜리 필드가
있어서, 여기에 프로세스 이름을 넣어 둘 수 있습니다.

이 정도면 전체적인 로드맵 설명은 다 한 것 같습니다. 그리고 이 그림이 다음 비디오에서 진행되는 내용을 이해하는 데 도움이 되기를 바랍니다. 다음 비디오에서는 usertrap과
usertrapret 함수, 그리고 uservec과 userret에 대해 설명할 예정입니다. 다음 비디오에서 봅시다.
