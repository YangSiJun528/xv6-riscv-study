# xv6 Kernel-19: VM Functions: Overview

| Field | Value |
| --- | --- |
| Video ID | `rhq08Lr-Yo4` |
| URL | <https://www.youtube.com/watch?v=rhq08Lr-Yo4> |
| Source | `docs/reference/scripts/original/19-xv6-kernel-19-vm-functions-overview-rhq08Lr-Yo4.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의의 일부입니다. 이 비디오에서는 가상 메모리 관리용 여러 보조 함수들을 살펴보겠습니다. 이 함수들은 vm.c 파일에 들어 있고,
페이지 테이블 구조를 조작하는 데 사용됩니다. 함수 자체는 꽤 단순하고, 다른 것과 분리해서 이해할 수 있습니다. 예를 들어 이 함수들은 어떤 락을 잡거나 sleep 하지
않습니다.

다만 내용이 꽤 많기 때문에 두 개의 비디오로 나누겠습니다. 이 비디오에서는 함수들에 대한 전반적인 개요를 설명하고, 다음 비디오에서는 실제 코드를 따라가면서 살펴보겠습니다. 우선
페이지 테이블이 어떻게 생겼는지부터 기억해 봅시다. 페이지 테이블은 트리 형태로 표현되며, xv6의 경우 3단계 트리입니다. 우리는 루트 노드를

루트 페이지라고 부를 수 있고, 페이지 테이블을 구성하는 나머지 모든 페이지들을 인덱스 페이지라고 부를 수 있습니다. 가장 아래 단계에서 인덱스 페이지들은 데이터 페이지들을
가리킵니다. 이제 이것들이 구체적으로 어떻게 생겼는지 좀 더 자세히 보겠습니다. 여기 그림에는 페이지 테이블의 트리에서 한 갈래(branch)가 나와 있습니다. 여기가 루트
노드이고, 여러 개의 페이지 테이블 엔트리가 있어서 각각이 다음 단계

를 가리키고 있습니다. 그리고 가장 아래 단계에는 데이터 페이지들이 있습니다. 가상 주소는 이렇게 생겼다는 것을 기억하세요. 주소의 윗부분 필드를 사용해서 첫 번째, 두 번째, 세
번째 페이지들을 인덱싱할 수 있고, 오프셋은 데이터 페이지 내부를 인덱싱하는 데 사용됩니다. 페이지 테이블 엔트리는 이런 형식을 가집니다. 즉, 페이지
정렬된(page-aligned) 포인터를 하나 가지고 있고, 이것이 트리에서

한 단계 아래로 내려가는 데 사용됩니다. 또 여러 개의 비트들을 가지고 있는데, 읽기·쓰기·실행 비트, 그리고 유효(valid) 비트, 그리고 이 페이지가 사용자 모드에서 접근
가능한지 나타내는 비트가 있습니다. 이 비트들은 맨 마지막, 가장 아래 단계의 페이지 테이블 엔트리에서 의미가 있습니다. 위쪽의 인덱스 페이지, 즉 이 두 단계에서는 유효 비트만
중요하고 다른 비트들은 0입니다. 자, 이제 여러

함수들을 살펴보겠습니다. 먼저 walk 함수부터 보죠. walk 함수에는 페이지 테이블, 좀 더 정확히 말하면 페이지 테이블의 루트 노드를 가리키는 포인터와 가상 주소가
전달됩니다. 그러면 이 함수는 트리를 따라 내려가서 데이터 페이지를 가리키는 페이지 테이블 엔트리의 주소를 반환합니다. 다시 말해, 루트에 대한 포인터를 인자로 받아서 트리를 따라
내려간 뒤,

데이터 페이지를 가리키는 페이지 테이블 엔트리에 대한 포인터를 반환합니다. 만약 페이지 테이블에 필요한 인덱스 페이지가 빠져 있으면, 그것들을 새로 만들지 여부를 선택할 수
있습니다. 세 번째 매개변수인 alloc은 필요한 경우 인덱스 페이지를 생성해야 하는지를 나타냅니다. 만약 페이지를 할당할 수 없다든지 하는 문제가 생기면, 이 함수는 null
포인터를 반환합니다. 다음으로 볼 함수는

mappages이고, 이는 페이지 테이블에 페이지 테이블 엔트리를 추가하는 데 사용됩니다. 이 함수는 페이지 테이블과 가상 주소를 인자로 받는데, 이 가상 주소는 가상 주소
공간에서 페이지들이 어디에 추가될지를 알려 줍니다. 또한 물리 주소도 인자로 받는데, 이 물리 주소는 여러 개의 페이지를 가리킵니다. size 인자는 가리키는 페이지의 개수를
나타내고, 이 함수는 하나 또는 여러 개의 매핑을

추가합니다. 여기서는 세 개의 매핑을 페이지 테이블에 추가해서, 이 가상 페이지들이 이 물리 페이지들에 매핑되도록 합니다. 매핑에는 마지막 인자로 주어진 권한이 설정됩니다. 읽기,
쓰기, 실행 가능 여부, 사용자 모드에서 접근 가능 여부 같은 것들입니다. 문제가 생기면 이 함수는 -1을 반환하고, 그렇지 않으면 0을 반환합니다. xv6 커널의 함수들은,
문제가 있을 때

-1을 반환하고 모든 게 정상일 때 0을 반환하는 경우가 자주 있습니다.

커널의 경우 페이지 테이블은 정확히 하나만 있고, 모든 코어가 그 페이지 테이블을 공유합니다. 이 페이지 테이블을 만들 때 사용하는 함수가 kvmmap, kernel virtual
memory map입니다. 이는 기본적으로 mappages와 거의 같지만, 커널 페이지 테이블을 만들 때는 오류가 발생하지 않을 것이라고 가정한다는 점이 다릅니다. 그래서 이
함수는 mappages가

에러 코드 -1을 반환했을 경우 panic을 호출하도록 되어 있습니다. 앞서 말했듯이, 이 함수는 커널의 페이지 테이블을 구축하는 데 사용됩니다. kvmmake 함수가 바로
kvmmake를 호출해서 커널의 페이지 테이블을 생성하고 초기화하는 함수입니다. 이 함수는 모든 물리 메모리를 페이지 테이블에 직접 매핑(direct mapping)으로 추가하고,
모든 memory-mapped IO 장치도 역시 직접 매핑으로 페이지 테이블에 추가합니다. 그리고

trampoline 페이지에 대한 매핑도 생성합니다. trampoline 페이지는 물리 메모리 어딘가에 존재하지만, 그와 더불어 가상 주소 공간의 가장 높은 페이지에서 그 물리
페이지로 가는 두 번째 매핑도 존재합니다. 마지막으로 이 함수는 proc_mapstacks를 호출합니다. 여기 커널의 가상 주소 공간 그림이 있습니다. 맨 위에는
trampoline 페이지가 보이고, 그 아래로

각 프로세스마다 한 페이지씩 있는 것을 볼 수 있습니다. 이 페이지들은 kalloc에서 얻어온 어떤 물리 페이지들에 매핑되어 있습니다. 각 프로세스는, 그 프로세스가 커널 모드에서
실행될 때 사용할 스택을 위해 한 페이지가 필요합니다. 그래서 64개의 페이지를 할당하고, 그 매핑들을 커널의 페이지 테이블에 추가합니다. 우리가 kstack이라는 전처리기 매크로
함수를 가지고 있다는 것을 기억하세요.

이 매크로는 0에서 63 사이의 숫자를 인자로 받아서, 해당 커널 스택 페이지의 주소를 반환합니다. kvminit 함수는 kvmmake를 호출해서 커널의 페이지 테이블을 생성하고,
그 결과를 전역 변수 kernel_pagetable에 저장합니다. 이 함수는 초기화 과정에서 코어 0에 의해 호출되어 커널의 페이지 테이블을 만들고, 그 다음에

각 코어마다 init_hart를 호출하는데, 이 함수는 그 페이지 테이블을 사용해서 페이지 테이블 주소를 satp 레지스터에 기록합니다. satp는 제어 및 상태 레지스터이고,
satp 레지스터를 어떤 페이지 테이블을 가리키도록 설정하면 해당 코어에서 페이징이 켜진 효과가 납니다. 다음으로 walkaddr 함수를 보겠습니다. 이 함수는 가상 주소를 받아서

물리 주소로 변환하는 데 사용됩니다. 이 함수는 페이지 테이블을 사용해 트리를 따라 내려가서, 해당 가상 주소가 어떤 페이지에 매핑되어 있는지를 찾고, 그 가상 주소를 물리 주소로
변환합니다. 이 가상 주소가 속한 페이지는 유효(valid)해야 하고, user 권한 비트가 설정되어 있어야 합니다. 만약 어떤 문제가 있으면, 이 함수는 null 또는 0을
반환합니다. 왜냐하면

어느 가상 주소 공간에서도 물리 메모리의 맨 첫 페이지로 매핑되는 가상 페이지는 없기 때문에, 어떤 가상 주소도 물리 주소 0을 가질 수 없습니다. 따라서 이 호출의 결과로 0을
사용하는 것이 안전합니다. 다음으로 uvmcreate 함수를 봅시다. 이 함수는 빈 가상 주소 공간을 만드는 함수입니다. 사실상 하는 일은

물리 페이지 하나를 할당하고 그 내용을 0으로 지운 다음, 그것을 페이지 테이블의 루트 인덱스 페이지로 사용해 반환하는 것뿐입니다. uvm은 user virtual memory를
뜻하고, user virtual memory init도 있습니다. 이것은 첫 번째 사용자 가상 주소 공간을 만드는 데 사용되며, 약간 특별한 함수입니다. 이 함수는 페이지 하나를
할당해서 가상 주소 0에 매핑하고, 그 페이지를 읽기·쓰기 가능하도록 표시합니다.

user 모드에서 실행 가능하고 접근 가능하도록 표시한 다음, 여기에 어떤 코드를 채워 넣습니다. 이 함수의 init 코드, 혹은 더 정확히 말하면 이 프로세스의 init 코드를
채우게 됩니다. init 코드는 커널 안에서 52바이트짜리 바이트 배열로 표현되어 있고, 16진수로 한 바이트씩 써 넣어져 있습니다. 이 함수가 하는 일은, source가 가리키는
그 바이트 시퀀스를 길이 size만큼 첫 페이지로 그대로 복사하는 것뿐입니다.

이 바이트들은 무엇일까요? 이건 명령어들이고, 원래는 어디서 왔을까요? initcode.s라는 파일에서 왔습니다. 이 파일은 최초로 실행되는 사용자 모드 프로세스이고, 어셈블리
언어로 작성되어 있습니다. 기본적으로 이 코드가 하는 일은 exec 시스템 콜을 호출하면서 파일 이름을 인자로 넘기는 것입니다.

그 파일 이름은 init 프로세스의 이름이고, 0개의 인자를 넘깁니다. 이 코드는 절대 리턴하면 안 됩니다. 이 init 파일은 존재해야 하고, 그래서 문제가 없어야 합니다.
그런데 만약 어떤 이유로든 리턴한다면, 이 코드는 exit 시스템 콜을 호출하는데, 이것 역시 리턴해서는 안 됩니다. 그 뒤에는 이 loop 레이블로 가는 goto가 이어집니다.
이것 전체가 어셈블된 뒤, 그 바이트들이 실행 파일에서 추출되어 이 배열 안에 들어가고, 그 배열이 단일 페이지짜리 가상 주소 공간으로 복사됩니다.

다음으로 볼 함수는 uvmalloc입니다. 이 함수는 기존 가상 주소 공간에 페이지를 추가합니다. 인자로는 기존 주소 공간을 가리키는 포인터, 예전 가상 주소 공간의 크기, 그리고
새로 늘리고 싶은 크기가 들어옵니다.

이 함수는 kalloc을 호출해 물리 페이지를 할당하고, mappages를 호출해 페이지 테이블에 페이지 테이블 엔트리를 추가합니다. 새 페이지들은 읽기 가능, 쓰기 가능, 실행
가능, 그리고 user 모드에서 접근 가능하게 표시됩니다. 그림으로 표현해 보면 이렇습니다. 여기 이전 가상 주소 공간이 있고, 이를 new 크기까지 늘리기 위해 필요한 만큼
페이지를 추가하고 싶습니다.

old와 new 둘 다, 그리고 옛 크기와 새 크기 모두 페이지 경계에 맞춰져 있을 필요는 없습니다. 우리는 필요한 만큼 페이지를 더해서, 가상 주소 공간이 최소한 new
크기만큼은 되도록 합니다. 그리고 성공하면 new 크기를 리턴합니다. 만약 문제가 생기면, 이 함수는 kfree와 uvmdealloc(잠시 뒤에 설명하겠습니다)을 호출해서 모든
페이지를 free pool로 되돌려 보내고, 페이지 테이블도 원래 상태로 복구한 다음, 실패를 나타내기 위해 0을 리턴합니다.

사용 예를 하나 보겠습니다. 여기 한 사용자 가상 주소 공간이 있고, 가장 낮은 주소에 코드와 데이터가 있습니다. 그 위에 스택 페이지와, 스택 오버플로를 잡아내기 위한 가드
페이지가 있고, 그 뒤에 0개 이상의 힙 페이지가 있습니다. 이 지점을 흔히 브레이크 포인트라고 부르는데, 이것이 사용자 입장에서의 주소 공간 크기입니다. 하지만 실제로는 높은
메모리 쪽에 trampoline 페이지와 trapframe 페이지도 있습니다. 사용자 프로그램이 힙을 늘리고 싶어 하면, 궁극적으로는 uvmalloc을 호출하게 되고, 이 포인트를
old로 삼아, 사용자가 필요로 하는 크기까지 힙을 확장하게 됩니다.

또 uvmunmap이라는 함수도 있습니다. 이 함수는 각 페이지를 순회하면서 해당 페이지 테이블 엔트리를 0으로 설정합니다. 여기에는 valid 비트도 포함되므로, 정의에 따라 이
페이지 테이블 엔트리를 invalid 엔트리로 바꾸게 됩니다. 인자로는 수정하려는 페이지 테이블을 가리키는 포인터, 가상 주소, 그리고 가상 주소 공간에서 제거하고 싶은 페이지
수를 받습니다. 네 번째 인자로 dofree라는 불리언이 있는데, 데이터 페이지를 free할지 말지를 알려 줍니다.

만약 이 값이 true이면, 이 함수는 kfree도 호출해서 unmap되고 있는 데이터 페이지들을 free pool로 되돌려 보냅니다. 다음으로 uvmdealloc 함수를
보겠습니다. 이 함수는 방금 설명한 함수와 매우 비슷합니다. 방금 이야기한 uvmunmap은 페이지 수 기준으로 동작하는 반면, 이 함수는 옛 크기와 새 크기 기준으로 동작합니다.

이 함수는 주소 공간을 줄이는 데 사용됩니다. 실제로 하는 일은 uvmunmap을 호출해서 페이지를 제거하고, 데이터 페이지들을 free하는 것입니다. 즉 uvmunmap을 호출할
때 dofree 인자를 true로 넘깁니다. 옛 크기와 새 크기는 페이지 경계에 맞춰져 있을 필요가 없고, 예제가 그걸 보여 줍니다. 또 새 크기가 옛 크기보다 클 수도 있는데,
그런 경우에는 아무 것도 하지 않아도 괜찮습니다.

다음으로 freewalk에 대해 이야기하겠습니다. 이 함수는 페이지 테이블을 제거할 때 사용되며, 페이지 테이블에 연관된 모든 인덱스 페이지를 free하는 데 사용됩니다. 인자로는
페이지 테이블 트리의 루트를 가리키는 포인터를 받고, 트리 전체를 순회하면서 인덱스 페이지들을 free합니다. 데이터 페이지는 free하지 않습니다. 이 함수는 실제로 재귀
함수이고, 임의의 인덱스 노드를 가리키는 포인터를 인자로 받습니다.

이 함수가 하는 일은, 그 노드의 자식들에 대해 자기 자신을 호출해서 서브트리를 삭제하고, 마지막에 루트 페이지, 즉 가장 위의 페이지를 free하는 것입니다. 커널 코드를 작성할
때는 재귀 함수에 주의해야 합니다. 커널 스택은 크기가 크지 않고, 보통 고정 크기에다가 꽤 작습니다. 따라서 이 함수가 너무 깊이 재귀 호출되지 않도록 조심해야 합니다.

하지만 페이지 테이블 트리는 깊이가 세 레벨뿐이기 때문에, 재귀 깊이가 그리 깊어지지 않는다는 걸 확신할 수 있습니다. 따라서 스택 사용량이 한정되어 있고, 그로 인해 문제가
생기지 않습니다. 다음으로 uvmfree 함수를 보겠습니다. 이 함수는 가상 주소 공간을 free하는 함수입니다. 인자로는 페이지 테이블을 가리키는 포인터와, 현재 가상 주소
공간의 크기를 받습니다.

여기 사용자 가상 주소 공간을 다시 그려 보았습니다. 코드와 데이터가 있고, 스택 페이지와, 오버플로를 잡아내기 위한 가드 페이지가 있습니다. 그리고 마지막으로 0개 이상의 힙
페이지가 있습니다. 이 함수는 size 아래에 있는 모든 페이지를 free하고, 페이지 테이블을 순회하면서 모든 인덱스 페이지를 free합니다. 사용자 주소 공간에는
trampoline 페이지와 trap 페이지도 있습니다.

trampoline 페이지는 모든 가상 주소 공간이 공유하는 페이지이므로 free하면 안 됩니다. trap 페이지 역시 미리 할당되어 있고, 프로세스마다 하나씩 있으므로 이것도
free하지 않습니다. 다음 함수는 uvmcopy입니다. fork 시스템 콜을 수행할 때, fork를 호출한 프로세스의 가상 주소 공간 전체를 복사해야 한다는 점을 기억하세요.

우리는 자식 프로세스를 위해 두 번째 주소 공간을 만들어야 하고, uvmcopy가 이 일을 처리해 줍니다. 이 함수는 옛 주소 공간에서 size만큼의 모든 페이지를 복사해서 새
주소 공간에 넣습니다. 인자로는 옛 페이지 테이블을 가리키는 포인터, 새 페이지 테이블을 가리키는 포인터, 그리고 크기(size)를 받습니다. 여기 옛 주소 공간 그림이 있습니다.

코드, 데이터, 가드 페이지, 스택, 힙 페이지들, 그리고 trampoline과 trapframe이 보입니다. 그리고 이 함수 호출 전에 새 주소 공간은 이렇게 생겼다고 합시다.
이미 맨 위 페이지는 trampoline 페이지를 가리키도록 설정되어 있고, 자기 자신의 trapframe 페이지를 하나 가지고 있습니다. 이 함수가 하는 일은 모든 데이터
페이지를 복사하는 것입니다. 여기 제가…

데이터 페이지가 복사되고 있다는 것이 표시되면, 여기 있는 각각에 대해 매핑을 생성하고, 여기서 유효했던 권한들을 새 페이지 테이블에 그대로 적용합니다. 그래서 이 새 페이지들도
완전히 동일한 권한을 갖게 됩니다. 제가 말하고 싶었던 또 다른 점은, 가드 페이지도 실제로 메모리의 물리 페이지에 매핑된다는 것입니다. 이 페이지는 사용자 모드에서는 접근 불가로
표시될 것입니다. 그래서 여기서도 역시

사용자 모드에서는 접근 불가로 표시됩니다. 다만 단순하게 처리하기 위해, 이 데이터 페이지는 실제로는 결코 접근되지 않더라도 페이지를 그냥 복사합니다. 이 일을 하기 위해 함수는
walk를 호출해서 페이지 테이블을 따라가고, kalloc을 호출해 새로운 물리 메모리 페이지를 얻고, memmove로 데이터를 복사하고, mappages로 새 페이지 테이블에
매핑을 추가합니다. 만약 무언가 잘못되면, 예를 들어

메모리가 부족해지면, 기본적으로 지금까지 한 모든 일을 되돌립니다. kfree와 uvmunmap을 호출해서 페이지 테이블의 모든 엔트리를 제거하고, 모든 페이지를 반환합니다.
다음으로 uvmclear를 보겠습니다. 이 함수는 사용자 모드에서 가드 페이지를 접근 불가로 표시하는 데에만 사용됩니다. 그게 이 함수가 쓰이는 유일한 곳입니다. 그래서 이
함수에는 페이지 테이블에 대한 포인터와

가드 페이지의 가상 주소가 넘어오고, 페이지 테이블 엔트리에서 u 비트를 클리어함으로써 그 페이지를 표시합니다. 말씀드렸듯이, 이는 사용자 주소 공간에서 스택의 스택 가드 페이지를
표시하는 데 사용됩니다. 때때로 커널은 사용자 프로세스의 가상 주소 공간 안으로 바이트를 집어넣거나 그 밖으로 바이트를 꺼낼 필요가 있습니다. 예를 들어 open 시스템 콜이
일어날 때, 열고자 하는 경로나 파일 이름을 가리키는 문자열의 포인터가

인자로 넘어오는데, 그 문자열은 사용자 가상 주소 공간 안에 위치해 있습니다. 커널은 그 문자열을 가져와야 합니다. 문제는 그 문자열이 여러 페이지에 걸쳐 있을 수 있다는
점입니다. 하나의 페이지가 아니라 둘 이상의 페이지 위에 걸쳐 있을 수 있습니다. 이 예에서 제가 접근하고자 하는 바이트 블록을 빨간색으로 보여주는데, 이 바이트 블록이 네 개의
페이지에 걸쳐 있다는 것을 볼 수 있습니다.

가상 주소 공간 상에서는 이런 식으로 보이고, 적어도 사용자 입장에서는 전부 연속적인 것처럼 보입니다. 하지만 실제 메모리의 물리 페이지들은 여기저기 흩어져 있을 수 있고, 따라서
이 데이터는 물리 메모리에서는 연속적이지 않습니다. 그래서 이제 살펴볼 copyin과 copyout 함수들은, 데이터를 커널 안의 버퍼로 복사하거나, 혹은 그 버퍼에서 다시 되돌려
보낼 때, 가상 주소 공간 안에서는 연속적인 형태가 되도록 복사하는 데 사용됩니다.

먼저 copyin부터 보겠습니다. 이 함수에는 페이지 테이블과, 바이트들을 가져올 가상 주소, 그리고 가져오고자 하는 바이트 수가 전달됩니다. 그리고 이런 버퍼의 주소 같은,
바이트를 저장하고자 하는 위치도 함께 넘어옵니다. 이 함수가 하는 일은

사용자의 가상 주소 공간에서, destination이 가리키는 이 커널 영역으로 바이트를 복사하는 것입니다. 그러니까 개별 조각들을 복사해 나갑니다. 예를 들어 메모리가 이
방향으로 증가한다고 하면, 먼저 이 조각을 복사하고, 그 다음 이 조각, 또 그 다음 이 조각, 그리고 마지막 조각을 복사하는 식입니다. copyout은 그 반대입니다. 이
버퍼에서 다시

가상 주소 공간으로 데이터를 되돌려 보내는 데 사용됩니다. 이 함수에는 페이지 테이블과, 데이터가 놓여야 할 가상 주소, 그리고 소스(즉 버퍼)의 포인터와 바이트 수가 넘어옵니다.
그리고 커널 영역에서 사용자 가상 주소 공간으로 바이트를 복사합니다. 무언가 잘못되면, 예를 들어 그 가상 주소가 유효하지 않으면, 이 함수는

에러일 때는 -1을, 모든 것이 괜찮으면 0을 반환합니다. open 시스템 콜의 경우, 널 종료 문자열(null-terminated string)을 찾고 있으므로, 널 바이트까지의
바이트만 복사하고 싶습니다. 그래서 copyin의 변형인 copyinstr가 있고, 비슷하게 작동합니다. 이 함수에는 소스 가상 주소와, 문자열이 저장될 버퍼의 주소인 목적지,
그리고

복사할 최대 바이트 수가 넘어옵니다. 보통 이 값은 버퍼의 길이가 될 것입니다. 그러면 이 함수는 복사를 시작해서 최대 길이까지 진행하지만, 문자열 끝의 널 종료 바이트를 복사하고
나면 멈춥니다. 만약 종료 널 바이트를 만나지 못한 채 최대 길이까지 가 버리면,

복사를 중단하고, 어떤 종류의 오류가 있었다는 것을 나타내기 위해 -1을 반환합니다. 말씀드렸듯이, 이것은 이 함수들의 개요 설명이고, 다음 비디오에서는 코드를 실제로 살펴볼
것입니다. 만약 여러분이 이 함수들을 충분히 잘 이해했다고 느낀다면, 그 코드를 따라가는 다음 비디오를 건너뛰어도 괜찮습니다.

다음 비디오는 코드를 자세히 살펴보는 내용이고, 이 함수들이 무엇을 하는지에 대한 이런 개요만으로도 괜찮겠다고 생각되면, 그냥 이 정도에서 진행하셔도 됩니다. 자, 여기까지입니다.
감사합니다. 다음 비디오, 혹은 그 다음 비디오에서 뵙겠습니다.
