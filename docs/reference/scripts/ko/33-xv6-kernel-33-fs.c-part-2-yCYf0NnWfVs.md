# xv6 Kernel-33: fs.c Part-2

| Field | Value |
| --- | --- |
| Video ID | `yCYf0NnWfVs` |
| URL | <https://www.youtube.com/watch?v=yCYf0NnWfVs> |
| Source | `docs/reference/scripts/original/33-xv6-kernel-33-fs.c-part-2-yCYf0NnWfVs.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의의 일부입니다. 앞선 비디오에서 저는 inode 시스템을 설명했고, 파일 fs.c의 전체적인 구성에 대해 이야기했습니다. 데이터
구조들을 다뤘고, 그 파일 안에 있는 모든 함수들에 대한 개요를 제공했습니다. 바로 이전 비디오에서는 fs.c에 들어 있는 함수들의 코드를 실제로 따라가 보기 시작했습니다.

이번 비디오에서는 그 나머지 함수들을 끝까지 따라가 보겠습니다. 우선 시각적인 참고를 위해, 이전 비디오에서 어떤 함수들을 다뤘는지 화면에 잠깐 보여 드리겠습니다. 이 모든
함수들을 다뤘고, 여기 보이는 이런 함수들도 이미 설명했습니다. 이번 비디오는 bmap 함수부터 시작하겠습니다만, 이전 비디오에서는 itrunc와 stati를 다뤘습니다.
bmap에 대해 이야기한 뒤에,

나머지 함수들, readi부터 시작해서 계속 설명할 겁니다. 그리고 말했듯이, 최종적으로 namei 함수까지 가는 것이 목표입니다. 마지막에는 namei 함수에 대해 이야기하면서
끝낼 텐데, namei는 경로가 들어 있는 문자열에 대한 포인터를 인자로 받아서, 디스크 상에서 그 파일을 찾아서 inode를 메모리 캐시에 읽어 들이고, 그에 대한 포인터를
반환하는 함수입니다. 그럼 시작해 봅시다.

수시로 우리는 파일에서 데이터를 읽거나 파일에 데이터를 써야 하고, 그 데이터가 디스크의 어디에 저장되어 있는지를 알아야 합니다. 즉, 그 데이터를 읽거나 쓰기 위해 디스크의 어느
블록 번호를 사용해야 하는지를 알아야 합니다. 이 일을 하는 함수가 바로 bmap입니다. 이 함수는 파일, 더 정확히는 어떤 파일을 설명하는 inode에 대한 포인터와, 파일의
시작으로부터 상대적인 블록 번호를 인자로 받습니다.

예를 들어 파일의 일곱 번째 블록에 몇 바이트를 쓰고 싶다고 해 보죠. 그러면 그 7이라는 블록 번호를 실제 디스크 상의 블록 번호로 변환해야 합니다. 이 함수는 “파일 안의
n번째 블록이 디스크의 어느 블록인지” 그 디스크 블록 주소를 반환합니다. 그리고 그 블록이 아직 이 파일에 할당되어 있지 않다면, 새 블록을 할당해서 0으로 채우는 일도 이
함수가 합니다. 여기 그림을 보면서 설명해 보겠습니다.

예를 들어 일곱 번째 블록을 가져오고 싶다고 하면, 캐시에 있는 inode로 가야 합니다. 음, 간접 블록이 그려져 있는 이 그림을 사용하겠습니다. 만약 찾는 블록이 처음 12개
블록 중 하나라면, 그냥 inode에 있는 배열에서 해당 블록 번호를 읽어서, 그 블록이 디스크의 어디에 저장되어 있는지 알면 됩니다. 만약 여기에서 12보다 크다면, 즉 12
이상이면,

간접 블록을 접근해서 그 안의 배열에서 블록 번호를 가져와야 합니다. 코드가 하는 일이 바로 이것입니다. 먼저 우리가 찾고 있는 블록 번호가 12보다 작은지, 즉 0에서 11
사이인지 묻습니다. 그런 경우에는 직접 포인터(direct pointer)를 사용할 수 있습니다. inode 안의 배열을 보고, 그 배열에서 해당 원소를 꺼내어 저장한 뒤 그 값을
반환합니다.

그런데 만약 그 배열 원소가 0 포인터(0 값)이면, 그 블록은 아직 할당되지 않은 상태입니다. 그러면 balloc을 호출해야 합니다. free 블록을 하나 찾아서 할당한 뒤, 그
블록 전체를 0으로 채우고, 그 블록의 디스크 블록 번호를 반환합니다. 그 번호를 배열에 저장해 두면, 다음에 다시 사용할 수 있습니다. 그리고 어느 경우든 최종적으로는 블록
번호를 반환합니다.

반대로, 우리가 찾는 블록 번호가 12 이상이면, 이 포인터를 따라가서 간접 블록을 들여다봐야 합니다. 예를 들어 14번 블록을 찾는다고 해 보겠습니다. 여기가 12, 13,
14이니 이 원소를 써야 합니다. 14라는 블록 번호에서 처음 12개를 빼서 이 배열의 인덱스를 얻어야 합니다. 즉 인덱스 2가 필요합니다. 코드에서 이 줄이 하는 일이 바로
그것입니다.

블록 번호에서 12를 빼고 나면, 인덱스는 0에서 255 사이의 값이 됩니다. 적어도 우리는 그렇게 되기를 바랍니다. 첫 번째로 하는 일은, 이 인덱스가 255보다 크지 않은지
확인하는 것입니다. 만약 그렇다면, 최대 파일 크기를 넘어서는 블록에 접근하려고 한 것이고, 그런 오류는 이 함수를 호출하는 쪽에서 미리 검사했어야 합니다. 그렇지 않았다면
panic을 발생시킵니다.

그 다음 해야 할 일은 간접 블록을 메모리로 읽어 오는 것입니다. 배열의 마지막 원소를 조사해서 그 값이 0인지 아닌지 확인합니다. 0이면, 간접 포인터들이 들어갈 블록이 아직
할당되지 않은 상태입니다. 여기서 그 간접 블록을 할당하고, 전체를 0으로 채웁니다. 그리고 간접 블록의 블록 번호를 inode의 배열에 저장합니다. 즉 여기 이 블록을 새로
할당하고, 이 원소에 그 포인터(블록 번호)를 저장하는 것이고, 그 작업이 여기에 해당합니다.

어느 쪽이든 우리는 간접 블록의 주소를 얻게 됩니다. 여기에서 그 간접 블록을 어떤 버퍼로 읽어 오고, a가 그 버퍼 안의 데이터가 있는 위치, 즉 이 배열의 시작을 가리키도록
설정합니다. 그리고 우리가 계산한 인덱스를 이용해 그 배열을 인덱싱하고, 그 원소 값을 저장합니다. 이것이 우리가 관심 있는 블록 번호입니다.

만약 그 값이 0이라면, 해당 데이터 블록이 아직 할당되지 않은 상태이므로 다시 balloc을 호출해서 데이터 블록을 하나 할당하고 0으로 채워야 합니다. 그리고 새로 할당된
블록의 주소(블록 번호)를 배열에 저장합니다. 배열을 수정했으면, 간접 블록의 내용이 바뀐 것이므로 이를 디스크에 반영할 필요가 있고, 그때 로그를 위해 여기 있는 log 관련
코드를 실행합니다. 반대로 간접 블록을 수정하지 않았다면, 그냥 배열에서 읽어 온 주소를 사용해서 그 값을 반환하면 됩니다. 마지막으로 간접 블록, 더 정확히는 간접 포인터 블록을
담고 있는 버퍼를 해제(release)합니다.

여기에서 우리가 inode를 수정하고 있다는 점을 볼 수 있습니다. 즉, 이 배열을 직접 수정할 수도 있습니다. 여기서도 그렇고, 또 여기에서도 그렇습니다. inode를
수정했다면, 디스크 상의 inode도 갱신해야 합니다. 그 책임은 bmap을 호출한 쪽에 있습니다. 이것을 기억하고 넘어가겠습니다.

이제 언젠가는 파일에서 메모리로 바이트를 옮기고 싶을 때가 오고, 그 일을 하는 함수가 readi입니다. 이 함수는 IP가 가리키는 inode로 설명되는 파일에서 데이터를
읽습니다. 파일 안에서 어느 위치에서 읽을지는 이 offset 인자가 정합니다. 그리고 몇 바이트를 옮길지 in이라는 인자가 정하고,

그 바이트들을 메모리로 옮깁니다. 옮길 메모리 주소는 dest 인자로 주어집니다. 이 dest는 가상 주소일 수도 있고 물리 주소일 수도 있는데, 그 구분은 이 인자가 결정합니다.
이 인자가 1이면 dest는 현재 실행 중인 프로세스의 가상 주소 공간 안의 주소입니다. 이 인자가 0이면 dest는 커널 주소 공간 안의 물리 주소입니다.

우리는 먼저 몇 가지 검사를 합니다. 첫 번째로, offset이 파일 끝을 넘어가 있으면 0을 반환합니다. 참고로 이 함수는 실제로 전송에 성공한 바이트 수를 반환합니다. 여기
있는 이 테스트는 약간 어색하게 쓰여 있습니다만, 양변에서 offset을 빼 보면,

실제로는 “우리가 전송하려는 바이트 수가 0보다 작은지”를 검사하는 것과 같습니다. 만약 그렇다면 마찬가지로 0을 반환합니다.

offset은 파일 안에 있을 수 있지만, 우리가 전송하려는 데이터의 양이 파일의 끝을 넘어갈 수도 있습니다. 그런 경우에는, 파일이 실제로 가지고 있는 바이트 수만큼만 전송하도록
n을 줄여야 합니다. 그래서 여기서는 offset에서 파일 끝까지의 바이트 수를 계산하고, 그 값을 새로운 n으로 사용합니다. 즉, 우리가 전송하려는 실제 바이트 수입니다.

전송하려는 바이트들은 파일 안에서 여러 블록에 걸쳐 흩어져 있을 수 있고, 우리는 각 블록을 개별적으로 읽어서 그 블록에서 원하는 바이트들을 꺼내야 합니다. 그래서 BREAD를
여러 번 호출하고, 각 블록에 대해 이 either_copyout 함수를 사용해서 바이트를 전송합니다. 이 루프는 매번 일정량의 바이트를 전송하고, 마지막에는 종료하면서 전체
전송된 바이트 수를 반환합니다.

total을 0으로 시작하고, 총 n 바이트를 전송하면 끝나며, 그동안 전송한 바이트 수를 반환합니다. 가장 먼저 해야 할 일은 “다음 청크가 파일 안에서 어디에 있고 디스크에서는
어디에 있는지”를 알아내는 것입니다. 지금 데이터를 가져오고 있는 현재 offset은 offset 변수로 주어지고, 루프를 돌면서 offset을 증가시키고, 각 청크마다 목적지
포인터도 함께 증가시킵니다. 여기에서 이번에 얼마를 전송할지, 즉 M을 계산하고, 파일 안에서의 소스 위치(offset), 목적지 포인터, 그리고 전송된 바이트 수를 모두
증가시킵니다. 현재 offset을 가지고 그것이 어느 블록에 속하는지 알아내기 위해,

offset을 포함하고 있는 블록 번호를 여기서 계산하고 bmap을 호출합니다. bmap은 파일의 시작을 기준으로 한 이 블록 번호를 디스크 상의 블록 번호로 매핑합니다. 그런
다음 BREAD를 호출해서 그 블록을 버퍼에 읽어오고, 이 버퍼 포인터가 그 버퍼를 가리킵니다. 이제 다음 단계에서 우리가 얼마나 많은 바이트를 전송할지 계산합니다. 이 블록의
끝을 넘어가면 안 되기 때문에,

이 블록의 끝을 지나지 않도록 여기서 처리합니다. offset을 이용해서, 이 블록 안에 아직 얼마나 남아 있는지 이 식으로 계산합니다. 그리고 우리가 전송해야 할 전체 바이트
수를 넘겨서도 안 됩니다. 지금까지 전송한 바이트 수를 보고, 그것을 M에서 빼서 아직 전송해야 할 바이트 수를 구합니다.

이 두 값의 최소값을 취해서, 이번에 실제로 전송할 바이트 수를 정합니다. 그런 다음 either_copyout을 호출합니다. 이 함수는 앞에서 다뤘지만, 요지는 소스 주소,
복사할 바이트 수, 목적지 주소, 그리고 그 바이트들을 사용자 가상 주소 공간으로 복사할지 아니면 커널 공간의 물리 메모리로 복사할지를 알려주는 플래그를 받는다는 것입니다.

위에서 봤던 그 인자가 바로 이 플래그입니다. 우리는 M 바이트를 복사하고 있습니다. 그러면 어디에서 복사하느냐 하면, 데이터가 들어 있는 버퍼에서 복사합니다. offset을 보고
버퍼 안에서의 위치를 계산해서, 버퍼의 data 배열 안을 offset만큼 건너뛰어 그 지점에서부터 M 바이트를 복사합니다. destination은 그 바이트들을 어디로 복사할지를
나타냅니다.

무언가 잘못되면 이 함수는 -1을 반환하고, 우리는 즉시 중단합니다. 예를 들면, 목적지 주소가 실제로 사용자 가상 주소 공간 안에 없는 경우, 즉 할당되지 않은 페이지라든가 하면
문제가 생길 수 있습니다. 그런 경우에는 즉시 버퍼를 해제하고 -1을 반환하며 여기에서 빠져나옵니다.

그래서 total을 -1로 설정하고 반환하게 됩니다. 하지만 복사가 정상적으로 끝났다면 그냥 계속 진행합니다. 여기서 할당했던 버퍼를 해제하고, 다음 청크를 처리하기 위해 반복을
진행합니다. 방금 M 바이트를 복사했으므로, 지금까지 복사한 전체 바이트 수 total에 M을 더하고, 파일 안의 소스 위치(offset)와 목적지 주소도 M만큼 증가시킵니다.

여기서 한 가지 짚고 넘어가고 싶은 점은 우리가 bmap을 호출하고 있다는 것입니다. 앞에서 말했듯이, bmap은 메모리 안의 inode를 수정할 수 있습니다. 파일 안에 데이터가
아직 할당되어 있지 않다면, bmap이 새 블록을 할당해서 파일에 추가할 수 있고, 그러면 inode가 수정됩니다. 그리고 수정된 inode를 디스크에 다시 기록하는 책임은
bmap을 호출한 쪽에 있습니다. 그런데

여기서는 그렇게 하지 않고 있고, 저는 이것이 이 코드의 진짜 버그라고 생각했습니다. 하지만 어쩌면 제가 못 본 게 있을 수도 있습니다. 실제로는, 제가 처음에 보지 못했던 것이
분명히 있었습니다. 이 코드를 자세히 본 후에는, 이 코드가 지금 상태로도 괜찮다고 믿게 되었습니다. Unix에는 fseek 시스템 호출이 있고, 파일이 이렇게 있다고 하면,
fseek을 써서 현재

파일 끝을 넘는 어떤 지점으로 이동한 다음 write를 할 수 있습니다. POSIX 명세에 따르면, 여기 사이에 실제로는 한 번도 쓰여지지 않은 바이트들은 0으로 초기화된 것으로
간주됩니다. 그래서 나중에 이 구간을 read 시스템 호출로 읽으면, 그 읽기 호출은 여기에 있어야 할 0 바이트들을 반환해야 합니다. 저는 xv6도 같은 식으로 동작하고,

이렇게 중간에 구멍(gap)이 있을 수 있다고 가정했습니다. 하지만 사실 xv6에서는 그렇지 않습니다. xv6에는 fseek 시스템 호출이 없습니다. 그래서 이런 일을 하는 것이
불가능합니다. 즉, 파일 끝을 넘는 위치에서부터 쓰기를 시작할 수 없습니다. 실제로 xv6에서는 항상 위치 0에서부터 쓰기가 시작됩니다. 만약 0이 아닌 다른 위치에서 쓰기를
시작하고 싶다면,

그 위치에 도달할 때까지 여러 번 read를 해서 건너뛰는 수밖에 없을 것입니다. 그래서 xv6에서는 파일 안의 블록들이 항상 연속적으로 할당돼 있습니다. 파일이 이렇게 어떤
지점부터 어떤 크기까지 있다고 가정해 봅시다. 파란색 부분이 파일이라고 하면, 각 사각형 하나가 블록이고, 이 모든 블록은 할당되어 있습니다. 마지막 블록은 완전히 사용되지 않을
수도 있지만,

이 영역 안의 모든 블록은 할당되어 있고, 이 지점 이후의 블록들은 전혀 할당되지 않습니다. 따라서 readi 시스템 호출이 bmap을 호출할 때는, 파일에 이미 존재하는 블록들만
읽게 됩니다. 그러면 inode가 갱신되는 일은 결코 없고, 따라서 write에서처럼 iupdate 함수를 호출해야 할 필요도 전혀 없습니다. 그건 우리가 곧 보게 될 write
쪽과는 다릅니다.

이 이야기는, 우리가 읽거나 작성하고 있는 코드에 대해 자주 여러 가지 가정을 하고 있다는 점을 보여줍니다. 이런 가정들은 종종 의식 수준 아래에 있고, 머릿속 어딘가에 깔려
있지만 명시적으로 표현되지는 않습니다. 만약 어떤 코드를 수학적으로 엄밀한 방식으로 “정확하다”고 증명하려고 한다면,

이런 모든 가정들을 명시적으로 꺼내서 적어야 합니다. 프로그램을 증명하고, 모든 가정을 명시적으로 만드는 일은 버그를 찾는 아주 좋은 방법입니다. 하지만 xv6 운영체제 커널과
관련해서는, 우리는 그런 수준의 분석까지는 들어가지 않을 것입니다. 자, 이제 writei 함수로 넘어가서 파일에 데이터를 쓰는 부분을 보겠습니다.

right eye 함수와 마찬가지로, 이 함수는 이 inode로 기술되는 파일에 데이터를 쓸 것입니다. 그리고 호출자는 반드시 그 inode에 대한 락을 잡고 있어야 합니다.
데이터는 이 인자로 주어진 source 위치의 메모리에서 오고, 이 위치는 현재 실행 중인 프로세스의 주소 공간 안의 가상 주소일 수도 있고, 여기 있는 이 인자로 지정된 것처럼
커널 공간 안의 물리 주소일 수도 있습니다.

우리는 이 offset 인자로 주어진 위치에 파일 안에 데이터를 배치할 것이고, n 바이트를 복사할 것입니다. 이 함수는 성공적으로 기록한 바이트 수를 리턴하며, 어떤 종류의
오류가 있으면 요청된 n보다 작을 수 있습니다. 코드 구조는 readi 함수와 매우 비슷합니다. 먼저 offset이 파일의 끝을 넘어섰는지 확인하고, 그렇다면 이 경우 -1을
리턴합니다. 또한 전송할 바이트 수가 0보다 작은지도 확인하는데, 이것도 오류로 간주하여 -1을 리턴합니다. 그리고 최대 파일 크기를 넘어가는지 여부도 검사합니다. maxfile은
파일 안의 최대 블록 수에 블록 크기를 곱한 값이고, offset + n이 그것보다 크면 오류를 리턴해야 합니다.

여기 있는 루프는 readi에서 보았던 것과 대략 같습니다. 전송을 크기 m인 여러 조각으로 나눌 것입니다. 먼저 지금까지 전송한 총 바이트 수를 나타내는 변수를 0으로 시작하고,
n 바이트를 모두 전송할 때까지 반복한 뒤, 마지막에 이 total 값을 리턴합니다. 그리고 이번에 전송할 바이트 수를 M으로 계산합니다. 루프의 각 반복에서, 우리가 쓰고 있는
파일의 offset에 m을 더하고, 바이트를 가져올 소스 주소에도 m을 더하며, 지금까지 쓴 총 바이트 수도 m만큼 증가시킵니다. 이 부분도 readi와 정확히 같습니다. 현재
파일 offset을 가지고 그게 어떤 블록에 속하는지 계산하고, 그 블록 번호를 알아내기 위해 bmap을 호출하고, 그다음 Bread를 호출해서 그 블록을 버퍼로 읽어옵니다. 그
버퍼를 가리키는 포인터가 bp입니다.

그 다음 M을 계산하는데, 여기서는 우리가 써야 할 전체 바이트 수를 넘어가게 된다면 거기서 멈추도록 제한합니다. 즉, n - total이 아직 써야 할 남은 바이트 수이므로,
이보다 더 많이 쓰고 싶지는 않습니다. 또 이 블록의 끝을 넘어가고 싶지도 않으므로, 이 블록 안에서 현재 offset으로부터 블록 끝까지 남은 바이트 수를 계산합니다. 그리고
여기서는 either_copyin 함수를 사용하는데, 이 함수는 이 인자에서 주어진 것처럼, 가상 주소 공간에 있든 커널 주소 공간에 있든 상관없이 source가 가리키는 메모리
위치에서 M 바이트를 복사합니다. 그럼 이 데이터는 어디로 복사될까요? 우리가 지금 쓰려는 바이트 범위를 포함하는 버퍼를 이미 읽어왔기 때문에, 여기에서 그 위치를 계산합니다.
버퍼 안의 데이터 시작 주소가 있고, 그 안에서의 offset이 있으며, 바로 그 위치로 데이터를 옮기는 것입니다. 메모리에서 버퍼로 데이터를 복사한 다음에는, 이 버퍼를 다시
메모리, 아니, 파일로 되돌려 써야 합니다. 그래서 여기서 log_write 함수를 호출해서 그렇게 하고, 이후 버퍼를 해제합니다.

either_copyin 함수에 문제가 생기면 -1을 리턴하고, 그러면 우리는 버퍼를 해제한 후 루프를 빠져나옵니다. 이제 size 필드를 갱신해야 합니다. 여기서는 이 파일의
size 속성을 갱신합니다. 루프를 끝낸 뒤의 offset이 현재 파일 크기를 초과하면, 파일을 키워야 하거나, 적어도 파일이 이미 커졌다는 사실을 기록해야 합니다. 실제로 블록을
파일에 추가하는 일은 bmap 함수가 할 것입니다. 그리고 size가 바뀌지 않았더라도 inode를 디스크에 다시 기록합니다. 왜냐하면 위 루프에서 bmap을 호출했을 수도 있고,
실제로 호출했고, 그 bmap이 파일에 새 블록을 추가하면서 블록 포인터 배열을 수정했을 수 있기 때문입니다. 그래서 iupdate를 여기서 호출하고, 마지막으로 전송한 총 바이트
수를 리턴합니다.

다음으로 디렉터리 lookup 함수(dirlookup)를 살펴봅시다. 이 함수는 디렉터리, 더 정확히 말하면 그 디렉터리 파일에 대한 inode의 캐시된 사본을 가리키는 포인터와,
한 개의 문자열에 대한 포인터를 인자로 받습니다. 그리고 이 이름을 디렉터리에서 찾습니다. 만약 찾으면, 그 파일에 대한 inode의 캐시된 버전을 할당하고 그 포인터를
리턴합니다. 또한 디렉터리 안에서 이 이름을 발견한 offset을 함께 리턴할 수도 있는데, 예를 들어 디렉터리에서 항목을 삭제하고 싶을 때 유용할 수 있습니다.

디렉터리가 어떤 모습인지 다시 상기해 봅시다. 길이가 16바이트인 directory entry 구조체가 있고, 필드는 두 개입니다. inum 필드는 2바이트뿐이고, 파일의
inode 번호를 담습니다. 그리고 파일 이름을 위한 14바이트가 있습니다. 이름이 14바이트보다 짧으면 그 뒤에 널 바이트가 따라옵니다. 디렉터리는 이런 구조체들의 배열을 담고
있는 하나의 파일일 뿐입니다. 예를 들어 이런 디렉터리가 있다고 합시다. 항목들 중 일부는 inode 번호가 0인데, 이런 것들은 사실상 사용되지 않는 디렉터리 엔트리입니다. 이
디렉터리는 세 개의 파일을 가지고 있는 것처럼 보입니다. 여기 파일 이름이 있고, 이름이 14글자보다 짧으면 널 바이트가 뒤따릅니다. 14글자면 널 바이트는 없습니다. 어쨌든,
이름마다 연관된 inode 번호가 있습니다. inode 번호가 0인 항목들에 대해서는 이름 필드에 어떤 문자가 들어 있는지는 신경 쓰지 않습니다.

이제 이 함수에서 무엇을 하는지 보겠습니다. 먼저, 인자로 전달된 파일(디렉터리)이 실제로 타입이 디렉터리인지 확인합니다. 그렇지 않다면 심각한 문제가 있는 것입니다. 그런 다음
파일을 쭉 훑어볼 건데, offset을 0에서 시작해서 디렉터리 엔트리의 크기만큼씩 증가시킵니다. dе라는 지역 변수가 있는데, directory entry이고 16바이트
크기입니다. 그래서 offset을 16바이트 단위로 증가시키며, 여기, 여기, 여기 이런 식으로 진행하고, 디렉터리 파일 자신의 크기, 즉 파일의 끝에 도달하면 반복을 멈춥니다.

루프의 각 반복에서 할 일은 readi 함수를 호출하는 것입니다. 이 디렉터리 파일에서 읽을 것이고, 어디에서부터 읽느냐 하면, 현재 offset에서부터 읽습니다. 그리고 얼마나
읽느냐 하면, directory entry가 16바이트이므로 16바이트를 읽습니다. 읽은 16바이트를 이 dе 변수에 저장합니다. 우리가 실제로 16바이트를 읽었는지 확인하고,
그렇지 않다면 정말 큰 문제가 있는 것이므로 panic을 호출합니다. 만약 이 directory entry의 inode 번호가 0이면, 이 엔트리는 건너뛰고 continue로 다음
엔트리로 넘어갑니다. 그렇지 않다면, 우리에게 전달된 이름, 즉 이 문자열과 directory entry 안의 이름을 비교해서 같은지 확인합니다.

이 시점에서 namecmp를 살펴보겠습니다. 여기 있는 작은 함수인데, 두 개의 문자열을 인자로 받고, 최대 14글자까지 비교하기 위해 strncmp를 사용합니다. 즉, 첫 번째
널 바이트까지 비교합니다.

어쨌든 이름은 최대 14바이트이기 때문에, 만약 이름이 같다면 우리가 찾던 항목을 발견한 것이다. 두 이름이 같다면 우리가 찾고 있던 디렉터리 엔트리를 찾은 것이고, 그에 연관된
I-number를 가져온다. 또한 이 Poff 인자가 널이 아니라면, 이 디렉터리 엔트리의 현재 오프셋을 여기서 가리키는 변수에 저장한다. 그리고 이 엔트리에 대한
I-number를 얻었으면,

그다음에 iget을 호출한다. iget은 inode 캐시 안에 inode를 하나 할당하고, 거기에 적절한 디바이스와 I-number로 초기화한다는 것을 기억하라. 그 이후에는
필요하다면 그 inode를 잠글 수 있다. 어쨌든 메모리에 캐시된 inode를 가리키는 포인터를 하나 얻게 되는 것이다. 이 루프가 끝까지 돌아서 결국 디렉터리 파일의 크기에
도달했는데도 여전히 일치하는 항목을 찾지 못하면, 아무것도 찾지 못했다는 뜻으로 0을 반환한다.

다음으로 디렉터리 링크 함수가 있는데, 이것은 디렉터리에 엔트리를 하나 추가할 때 사용된다. 인자로는 디렉터리의 inode를 가리키는 포인터인 DP, 그리고 디렉터리에 추가할
이름과 I-number가 넘어온다. 모든 것이 정상적으로 끝나면 0을 반환하고, 문제가 있으면 -1을 반환한다.

가장 먼저 하는 일은 directory lookup을 호출해서 이 이름이 이미 그 디렉터리에 존재하는지를 확인하는 것이다. 만약 뭔가 찾으면, IP를 그 파일의 inode를
가리키도록 설정한다. directory lookup 안에서 iget을 호출해서 그렇게 한다. 그런 경우라면, 이미 존재하는 파일이므로 문제가 되고, -1을 반환해야 한다. 하지만
이미 찾은 그 파일은 우리가 관심 있는 대상이 아니므로, 말하자면 iget을 되돌리기 위해 Iput을 호출한다.

그다음에는 빈 디렉터리 엔트리를 찾는다. 디렉터리 파일을 오프셋 0부터 시작해서 디렉터리 엔트리 크기만큼씩 증가시키면서 훑어본다. 그리고 모든 엔트리를 다 살펴본 뒤, 파일의
크기에 도달하면 멈춘다. 각 오프셋마다, redui를 호출해서

디렉터리 파일로부터 16바이트를 가져온 뒤, 파일 내의 현재 오프셋에서 이 de 변수, 즉 여기의 디렉터리 엔트리 변수로 옮긴다. 이 디렉터리 엔트리 변수는 16바이트짜리 로컬
변수이고, 이는 가상 주소 공간이 아니라 물리 메모리에 있다. 그래서 여기 플래그가 0인 것이다. 그리고 어떤 이유에서든 데이터를 옮기지 못하거나, 16바이트 전체를 다 옮기지
못하면

심각한 문제가 있는 것이므로 panic을 일으킨다. 하지만 그렇지 않다면, 디렉터리 엔트리의 I-number를 확인해서 비어 있는지 검사한다. 만약 0이라면, 디렉터리 안에서
우리가 사용할 수 있는 자리를 찾은 것이므로 그 오프셋에서 루프를 빠져나온다. 그 외의 경우에는 파일 끝까지 쭉 가게 되고, offset은 파일의 크기와 같아질 것이다. 디렉터리의
크기는 16의 배수이기 때문이다.

어느 쪽이든 이제 디렉터리 엔트리를 새로 만들고 파일에 쓸 준비가 된 것이다. 여기서는 이름을 디렉터리 엔트리 변수로 복사하고, 최대 14글자까지 복사한다. 그리고
I-number를 디렉터리 엔트리에 복사한다. 그다음 그 디렉터리 엔트리를 쓴다. 여기에서 우리가 쓰고 있는 주소가 바로 커널 공간의 주소이기 때문에 플래그는 0이다. 그리고
offset 위치에 쓴다. 이 offset은 사용되지 않은 엔트리의 오프셋일 수도 있고, 파일 끝을 넘어선 오프셋일 수도 있는데, 둘 중 어느 쪽이든 상관없다. 필요하다면 이
작업이 파일을 늘릴 것이다. 그리고 디렉터리 엔트리의 크기인 16바이트를 쓴다. 뭔가 잘못되면 panic을 일으키고, 그렇지 않으면 작업이 끝난 것이다.

그리고 성공을 나타내기 위해 0을 반환한다. 파일을 열기 위해서는 경로 이름을 처리할 수 있어야 한다. 다음으로 살펴볼 함수는 skip element라고 불리는 함수이다. 이것은
완전히 문자열 처리 함수로, 경로 이름을 파싱하는 데 도움을 준다. inode는 전혀 관여하지 않는다. 이 함수는 이런 식의 경로 이름이 들어 있는 문자열에 대한 포인터를 인자로
받고, 그 경로에서

첫 번째 파일 이름을 찾아내고, 포인터를 두 번째 파일 이름으로 진행시킨다. 그리고 두 번째 파일 이름에 대한 포인터를 반환한다. 또한 첫 번째 파일 이름을 저장한다. 두 번째
인자는 14바이트 영역에 대한 포인터인데, 파일 이름은 최대 14글자까지일 수 있다는 것을 기억하라. 이 영역에 첫 번째 구성 요소를 저장한다. 경로 안에 파일 이름이 없다면

널을 반환한다. 이제 코드를 살펴보자. 한 가지 언급하고 싶은 점은, 이 함수는 앞쪽에 오는 슬래시는 무시한다는 것이다. 그리고 파일 이름들을 구분하는 슬래시가 여러 개 연속으로
있어도, 그것들을 무시하고 하나의 슬래시와 같은 것으로 취급한다.

자, 여기서 우리는 path에 대한 포인터와 첫 번째 파일 이름을 저장할 14바이트 영역에 대한 포인터를 넘겨받는다. 함수는 먼저 처음 나오는 슬래시들을 건너뛰면서 무시한다.
그리고 만약 아무것도 찾지 못했다면, 즉 문자열 끝의 널 바이트 위치에 도달했다면, 경로 안에 파일 이름이 없는 것이므로 그냥 0을 반환한다.

이제 우리는 첫 번째 파일 이름의 시작 위치에 서 있다. 그래서 여기에 그 위치에 대한 포인터를 저장한다. 그리고 이 while 루프에서, 포인터를 그 파일 이름 다음에 오는
무언가의 위치까지 진전시킨다. 다음 슬래시나 다음 널 문자까지를 찾으면서 진행하고, 둘 중 하나를 만나면 멈춘다. 그 시점에는 첫 번째 파일 이름이 어디서 시작해서 어디서
끝나는지를 알게 되므로, 그 길이를 계산할 수 있다. 이제 그 이름을 이 name 영역으로 옮길 것이다. 이것이 두 번째 인자이다. 그리고 길이가 14보다 큰지를 검사해야 한다.

만약 길이가 14 이상이면, 널 문자를 신경 쓸 필요가 없다. 그냥 name 필드로 그대로 옮기면 된다. 디렉터리 이름 크기는 이 14 상수이다. 만약 13자 이하라면, 끝에 널
문자를 하나 추가해 주어야 한다. 여기에서 그렇게 처리한다. 그리고 마지막으로, 슬래시들을 지나서 다음 위치로 포인터를 옮긴다. 즉, 우리가 슬래시를 가리키고 있는 상태로
끝났다면, 포인터를 두 번째 파일 이름이나 문자열 끝으로 옮기고 그 포인터를 결과로 반환한다.

다음으로 살펴보고 싶은 함수는 namei 함수이다. 지금까지 본 함수들의 정리, 결론 같은 역할을 한다. namei는 path에 대한 포인터를 인자로 받아서, 그 파일을 디스크
상에서 찾아서 그 파일에 대한 inode 포인터를 반환한다.

만약 파일을 찾을 수 없다든지, 경로 이름이 잘못되었다든지 하는 문제가 있으면 널을 반환한다. 실제 작업은 namex라는 함수가 수행한다. nameiparent라는 다른 함수도
있는데, 이것은 namei와 매우 비슷하지만 한 단계 앞에서 멈춘다. 그리고 namei와 nameiparent 모두 실제 일을 하기 위해 namex를 호출한다. 그럼 namei와
nameiparent를 살펴보자.

보는 것처럼, namei는 namex를 호출하고, nameiparent도 namex를 호출한다. 차이는 어떤 플래그를 넘겨서 어떤 동작을 원하는지를 구분한다는 점뿐이다.
namex는 경로의 각 파일 이름을 저장하기 위한 14바이트 메모리 영역이 필요하다. 그래서 nameiparent의 경우에는 그 14바이트 영역을 밖에서 전달받아, 그곳에 결과를
빼내려는 것이고, namei의 경우에는 로컬 변수를 사용해서 그 영역을 갖는다.

14바이트 필드이고, 우리는 그 변수의 주소를 넘기고 있습니다. 자, 그러면 namex를 한 번 보죠. 이 함수는 경로에 대한 포인터와, 이 플래그인 nameiparent를 인자로
받습니다. 이 플래그가 0이면, 부모의 inode를 리턴하고, 마지막 파일 이름을 name 영역에 복사합니다. 0이 아니면 그 파일 자체의 inode를 직접 열어서 돌려줍니다.
namex는 오직 namei와 nameiparent에서만 호출됩니다.

그래서 이 코드를 이해하는 한 가지 방법은 먼저 namei에서 호출된다고 상상해 보는 것입니다. 두 번째 경우는 조금 이따가 보겠습니다. 플래그가 0이라고 가정하고, namex
함수 안에서 사용할 수 있는 14바이트짜리 영역이 있다고 해 봅시다. 여기서 먼저 하는 일은 경로(path)가 슬래시(/)로 시작하는지 확인하는 것입니다. 만약 그렇다면 루트
디렉터리에서부터 시작해야 합니다.

여기서는 파일 시스템이 올라가 있는 디바이스와, 루트 디렉터리의 inode 번호(1번)를 가지고, 그에 해당하는 inode의 포인터를 얻습니다. 그리고 예를 들어 경로가 그냥
슬래시 하나뿐이고 그 뒤에 아무것도 없다고 해 봅시다. 그러면 이 while, 즉 경로 이름의 각 요소를 처리하는 while 루프가 있는데, 요소가 아무것도 없으니까 곧바로 루프를
빠져나옵니다.

그리고 지금 플래그가 0이라고 가정했으므로 이 부분을 건너뛰고, 루트의 inode 포인터를 그대로 반환합니다. 이 함수는 inode에 락을 걸지는 않지만, 캐시에는 추가합니다. 이
플래그 이름이 다소 헷갈리게도 nameiparent인데, 플래그 이름이 nameiparent이고, 우리가 지금 이 코드를 볼 때는 그 값이 false(0)라고 가정하고 있습니다.

이제 경로가 슬래시로 시작하지 않는다면, 현재 작업 디렉터리에서부터 경로 처리를 시작해야 합니다. 지금 실행 중인 이 프로세스 구조체에는 현재 작업 디렉터리(current
working directory)라는 필드가 있고, 이것은 어떤 inode를 가리키는 포인터입니다. 이게 현재 작업 디렉터리이고, 우리는 이것을 사용해야 합니다. 아직 idup
함수는 본 적이 없는데, 이 함수가 하는 일은 기본적으로 이 inode의 참조 카운트를 증가시키는 것입니다.

잠깐 멈추고 그 코드를 보여 주죠. 여기 idup 함수가 있고, inode 포인터를 인자로 받습니다. 캐시에 대한 스핀락을 잡고, 참조 카운트를 증가시키고, 그리고 락을
해제합니다. 이게 idup이 하는 일입니다. 그리고 iget은 루트 디렉터리의 참조 카운트를 증가시킬 것입니다.

그래서 이쪽을 실행하든 저쪽을 실행하든, 캐시에 추가된 inode가 하나 있고, 그 inode의 참조 카운트는 증가해 있는 상태가 됩니다. 이게, 아래로 내려와서 리턴할 때 항상
참인 상황입니다. 즉, 슬래시로 시작하든 안 하든, 우리는 이제 경로를 살펴볼 준비가 된 것입니다.

지금 막 이야기한 skipelem 함수를 호출해서, 경로에서 첫 번째 파일 이름을 찾아내고, path 포인터를 그 다음 요소로 진행시키게 됩니다. 그게 여기에서 벌어지는 일입니다.
만약 다음 요소가 더 이상 없다면, 우리는 끝난 것이고 루프에서 빠져나옵니다. 하지만 그렇지 않으면, 지금 우리가 가리키고 있는 이 디렉터리에서, 방금 얻은 이름과 일치하는 파일을
찾아야 합니다.

여기서 이름을 찾아서 name 변수에 저장합니다. 그리고 나서 우리가 다음으로 할 일은 directory lookup을 호출하는 것입니다. 그 전에 그 inode에 락을 걸어야
합니다. 여기에서 락을 걸고, 그 다음에 directory lookup을 호출합니다. 그 전에, 우리가 디렉터리라고 생각하고 있는 이 파일이 실제로 디렉터리인지 확인해야 합니다.
타입을 검사해서 디렉터리가 아니면, 무언가 잘못된 것이므로 중단하고 0을 리턴해서 문제가 있었다는 것을 나타냅니다.

하지만 기억해야 할 것은, 지금 이 디렉터리의 참조 카운트를 증가시켰고, 락까지 잡은 상태라는 점입니다. 그러니 이 두 가지를 되돌려야 합니다. 락을 해제하고, 참조 카운트를
감소시키고, 그 다음에 리턴합니다.

아까는 이 플래그, nameiparent 플래그가 false인 경우만 보겠다고 했으니, 여기 코드는 일단 건너뛰겠습니다. 이제 directory lookup을 할 준비가 됐습니다.
경로 안에서 첫 번째, 또는 더 정확히 말하면 “다음” 파일 이름이 이 문자열에 있고, 이쪽은 그 파일을 찾아야 할 디렉터리의 inode입니다. 이제 directory
lookup을 호출합니다.

여기 이 인자는 디렉터리 안에서의 오프셋이고, 이쪽은 그 오프셋을 저장할 수 있는 위치에 대한 포인터입니다. 우리는 디렉터리 안에서 이 이름이 어느 위치에 있는지는 신경 쓰지 않기
때문에, null을 넘깁니다. 즉, 그 정보를 저장할 변수의 주소 대신 null을 넘기는 것입니다.

이 함수는 이 파일에 대한 inode 포인터를 리턴합니다. 그게 next inode이고, 우리는 곧 그쪽으로 진행할 것입니다. 모든 일이 잘 되면 inode 포인터를 리턴하고,
문제가 있으면 null을 리턴합니다. 이 경우에도, 우리가 들여다보고 있던 디렉터리에 락을 걸어두었고, 참조 카운트도 증가시켜 둔 상태이므로, 이 두 가지를 되돌린 뒤에 null을
리턴해야 합니다. 즉, 파일을 찾지 못했음을 나타냅니다.

경로 이름에 문제가 있어서, 경로 중 하나의 파일을 찾지 못하면, 우리는 그걸 에러로 간주하고 null을 리턴합니다. 하지만 모든 것이 괜찮다고 가정해 봅시다. 그러면 이제 이
디렉터리와의 작업은 끝났습니다. 경로 이름에서 다음에 나오는 것은, 우리가 찾고 있는 최종 파일일 수도 있고, 또 다른 디렉터리일 수도 있습니다.

다시 말해, 우리가 /bin 디렉터리를 보고 있을 수도 있고, 혹은 이 아래쪽에서 실제로 얻으려는 파일을 보고 있을 수도 있습니다. 어쨌든 우리는 그 다음으로 진행합니다. 그래서
이 디렉터리의 락을 해제하고, 경로에서 다음 파일(다음 요소)로 이동합니다. 그리고 다시 위로 올라가 루프를 반복합니다.

만약 방금 처리한 것이 경로의 마지막 요소였다면, skipelem이 0을 리턴하고, 우리는 루프를 빠져나옵니다. 그런 다음 찾은 파일의 inode 포인터를 리턴합니다. 그렇지
않으면 while 루프가 계속 돌아가며, 경로 이름의 다음 요소를 처리합니다.

이제 nameiparent 함수를 보겠습니다. 여기서는 플래그로 1을 넘깁니다. 이번 경우에는 중간에 멈추고 싶습니다. 다시 시작해 봅시다. 예를 들어 루트 디렉터리에서 시작해서,
ip를 다음 디렉터리, 즉 경로의 다음 요소로 설정한다고 합시다. 그리고 그 다음에는 더 이상 아무것도 없다고 해 봅시다. 그러면 skipelem이 0을 리턴합니다. 즉, 두 번째
요소가 없다는 뜻이고, 이것은 문제입니다.

만약 이 조건이 참이면, 즉 nameiparent 상황에서는 이 조건이 참이 될 텐데, 우리는 문제가 있다고 보고 0을 리턴해야 합니다. 경로에 최소한 하나의 파일 이름조차 없다는
뜻입니다. 그래서 ip를… 아, 아직 ip에 락을 걸지는 않았습니다. 락은 루프 안에서 걸리기 때문에, 이 시점에서는 ip에 락이 잡혀 있지 않습니다. 그러니 락을 풀 필요는
없고, 단지 참조 카운트만 줄인 다음 리턴하면 됩니다.

이제 루프 안에서 무슨 일이 벌어지는지를 보면, 기본적으로 같은 일을 합니다. 경로를 따라가며 다음 요소를 얻고…

path를 따라서, 우리가 IP가 가리키는 디렉토리를 들여다보고 다음 파일을 찾아냅니다. 그리고 만약 우리가 중간에 멈추려고 하고, path의 끝까지 온 상태라면, 그러니까
path 포인터를 name 뒤를 가리키는 위치까지 진행시켰는데 name 뒤에 더 이상 아무 것도 없다면, 거기서 멈출 수 있습니다. 이제 끝입니다. 파일 경로상의 마지막 항목을
알아냈고, 이 시점에서 IP는 그 디렉토리를 가리키고 있습니다.

그래서 그 디렉토리의 잠금을 풀고(unlock) 그것을 반환합니다.

fs.c 파일 안에 있는 함수들은 여기까지입니다. 다음 비디오에서 다시 보겠습니다.
