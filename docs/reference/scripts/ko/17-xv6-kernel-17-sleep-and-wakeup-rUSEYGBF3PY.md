# xv6 Kernel-17: sleep and wakeup

| Field | Value |
| --- | --- |
| Video ID | `rUSEYGBF3PY` |
| URL | <https://www.youtube.com/watch?v=rUSEYGBF3PY> |
| Source | `docs/reference/scripts/original/17-xv6-kernel-17-sleep-and-wakeup-rUSEYGBF3PY.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의 중 하나입니다. 이번 비디오에서는 sleep과 wakeup 함수에 대해 이야기하겠습니다. 예제로는 여기 보이는 이 함수에서
다루는 sleep라는 시스템 호출을 사용할 것입니다. 어떤 프로세스든지 sleep을 호출해서 자기 자신을 잠들게 할 수 있고, 그러면 현재 타임 슬라이스를 끝내고 상태를 실행 중
또는 실행 가능에서 sleeping 상태로 바꾸게 됩니다. 그리고

그 프로세스는 다른 어떤 프로세스가 wakeup을 호출할 때까지 sleeping 상태로 머무르게 됩니다. 그런데 여기서 문제가 되는 것은, 어떤 프로세스가 wakeup을 호출했을
때 정확히 어느 프로세스들을 깨워야 하는가 하는 점입니다. 따라서 우리가 sleep으로 갈 때, 언제 깨워지기를 원하는지에 대한 어떤 정보를 제공해야 하고, wakeup은 모든
sleeping 프로세스를 다 깨우는 것이 아니라

그중 일부만을 깨워야 합니다. 그러면 어떤 프로세스들을 깨울지 어떻게 식별할 수 있을까요? xv6에서는 채널(channel)이라는 것을 사용합니다. 채널은 그냥 단순한 숫자일
뿐입니다. 사실 뭐든 될 수 있고, sleep과 wakeup 함수 입장에서는 그 숫자의 의미를 해석하지 않습니다. 예를 들어 그 숫자가 37일 수도 있습니다. 한 프로세스가 숫자
37에서 sleep할 수 있고,

그 다음에 wakeup이 호출될 때 어떤 숫자 채널이 인자로 넘어옵니다. 만약 37에 대해 wakeup을 한다면, 그 프로세스를 깨울 것입니다. 하지만 wakeup이 54 같은
다른 숫자와 함께 호출된다면, 54에서 sleeping 중인 프로세스들만 깨우게 됩니다.

이것이 채널이 동작하는 방식입니다. 다른 시스템들에서는 다른 접근 방식을 사용합니다. 어떤 곳에서는 여기에서 스레드들의 리스트를 제공합니다. 특정한 집합 또는 스레드 리스트에서
sleep하고, 그 다음 wakeup은 특정한 sleeping 스레드 리스트에 적용되어 그 리스트에 있는 모든 것을 깨웁니다. 어떤 시스템들은 조건 변수(condition
variable)도 갖고 있는데, 조건 변수는 근본적으로

sleeping 스레드들의 리스트에 불과합니다. 하지만 xv6에서는 이렇게 동작합니다. 채널은 필드이며, proc 구조체 안에 그냥 보관됩니다. 모든 프로세스는, 상태가
sleeping일 때 자신이 어떤 숫자에서 sleep 중인지 저장하는 필드를 하나 갖고 있습니다. 그리고 wakeup이 동작하는 방식은, 모든 프로세스를 훑는 것입니다. 다시 말해
전체

proc 배열을 순회하고, 상태가 sleeping이고 채널 숫자가 일치하는 프로세스를 모두 깨웁니다. 이제 다음으로, 이 함수들이 어떻게 사용되는지 보여주겠습니다. 전형적인 사용
예를 보겠습니다. 코드에서는 어떤 조건을 검사해서, 그 조건이 참일 때만 진행하고, 그렇지 않으면 그 조건이 참이 될 때까지 기다리고 싶습니다. 그래서 전형적으로 다음과 같은
패턴을 보게 됩니다.

이 while 루프에서 우리는 조건이 참인지 검사하고, 아직 참이 아니면 sleep을 호출해서 조건이 갱신될 때까지 기다립니다. 어떤 다른 프로세스가 어떤 변수들을 갱신할 수
있고, 그 변수들이 그 조건과 관련되어 있을 수 있습니다. 그 다른 프로세스는 우리가 어떤 조건을 기다리고 있는지 정확히 알지 못할 수도 있지만,

그냥 그 조건에 관련된 변수들을 기다리고 있는 어떤 프로세스든 깨우게 됩니다. 그래서 약간 너무 많은 프로세스를 깨우게 될 수도 있습니다. 공유 데이터의 변경을 기다리고 있는 여러
프로세스를 한꺼번에 깨울 수도 있습니다. 그러면 어떤 프로세스들은 조건이 참이거나 거짓이거나 할 수 있고, 그중 하나는 조건을 확인해서 참이라고 판단해 진행할 수 있지만,

다른 프로세스들은 조건이 거짓이라고 판단하고 다시 sleep으로 돌아갈 것입니다. 따라서 깨어났을 때 조건을 다시 검사해야 합니다. 하지만 이 특정 패턴에는 문제가 있습니다.
바로, 조건이 검사되는 시점과 sleep이 실행되는 시점 사이에, 조건이 참이 되어버릴 수도 있다는 점이 걱정되는 것입니다. 우리는 이 코어와 다른 코어들에서 다른 프로세스들이
실행되고 있고,

여기에서 우리 타임 슬라이스가 끝나고 다른 프로세스들이 실행될 수 있습니다. 그러는 동안 그 프로세스들이 조건을 참으로 만들 수 있고, 나아가 wakeup을 호출할 수도 있습니다.
그런데 아직 우리가 sleep을 실행하기 전이라서 그 wakeup을 놓치게 됩니다. 우리는 이 조건을 검사한 이후에는 어떤 wakeup도 놓치고 싶지 않습니다. 만약 그 뒤로 더는
wakeup이 없다면, 우리는 sleep에 들어가서

다시는 깨어나지 못할 수도 있고, 이는 정말 큰 문제가 될 수 있습니다. 이제 곧 코드를 보면서 살펴볼 예를 하나 들겠습니다. sleep 시스템 호출은 이 함수에서 처리되고, 여기
코드가 대략 무엇을 하는지 개요가 나와 있습니다. 먼저 시스템 호출의 인자를 가져오는데, 그 인자는 얼마나 오래 sleep할 것인지이고, ticks라는 단위로 측정됩니다. 타이머
인터럽트가 발생할 때마다 한 tick이

지났다고 말합니다. 그리고 모든 코어가 공유하는 어떤 전역 변수가 있는데, 그 안에 현재 tick 수가 들어 있습니다. 이것이 현재 시각을 나타내는 시간 값입니다. 이 변수는
core 0가 타이머 인터럽트를 받을 때마다 core 0에 의해 갱신됩니다. 전역 공유 데이터이고, 우리는 지금 기다리기를 시작하는 시점을 잡고 있습니다. 그리고 여기서 조건이
나오는데, 우리는

n ticks가 지났는지를 검사합니다. 아직 지나지 않았다면 sleep에 들어가고, 이 공유 변수가 다시 증가할 때까지 sleep합니다. 또한 그 사이에 프로세스가 kill되었는지
확인하는 작은 검사도 있고, 그럴 경우에는 기다리기를 중단합니다만, 그건 지금 주제와는 별개입니다. ticks는 전역 공유 변수이고, lock에 의해 보호되거나, 또는 lock에
의해 가드된다고 말할 수 있습니다. ticks를 보고 싶거나 수정하고 싶다면,

먼저 tickslock을 획득해야 합니다. 따라서 우리는 ticks를 보는 동안에는 tickslock을 쥐고 있어야 한다는 요구사항을 갖고 있습니다. 예를 들어 바로 여기에서
조건을 검사할 때가 그렇습니다. 그리고 우리는 물론 wakeup을 놓치고 싶지 않습니다. 하지만 동시에 다른 제약도 있는데, sleep하는 동안 lock을 쥔 채로 있을 수 없다는
것입니다. 기억하겠지만 spinlock은 빨리 해제되어야 하고, 특히

sleep하는 동안에는 spinlock을 쥐고 있을 수 없습니다. 그래서 여기에는 약간 문제가 있습니다. 해결책은 다음과 같습니다. 우리의 코드는 다음과 같이 보이게 됩니다. 먼저
lock을 획득합니다. 이 경우에는 tickslock이고, 어떤 조건을 검사하는 데 필요한 lock이 있다면, 그 조건을 보기 전에 반드시 그 lock을 획득해야 합니다. 그래서
lock을 획득하고 조건을 검사합니다. 다시 말해 조건을 확인한 뒤, sleep으로 가고,

그 다음 깨어나서 다시 조건을 검사합니다. 하지만 차이점은, 이제 우리가 지금 쥐고 있는 lock에 대한 포인터를 sleep 함수에 넘긴다는 점입니다. 그러면 sleep 함수는
무엇을 할까요? sleep으로 들어가기 전에 반드시 그 lock을 해제해야 합니다. 하지만 wakeup을 절대 놓치지 않는다는 보장을 줄 수 있는, 원자적인 방식으로 그 일을 해야
합니다.

그리고 이후 어느 시점에 다른 프로세스가 우리의 상태를 sleeping에서 runnable로 바꾸고, 스케줄러가 우리를 선택해서 상태를 running으로 바꿀 것입니다. 그 시점에
이 프로세스는 여전히 sleep 함수 안에서 깨어나게 되고, sleep 함수는 리턴하기 전에 이 lock을 다시 획득할 것입니다. 그래서 우리가 루프를 다시 돌 때, 우리는 그
lock을 쥔 채로 있고

락을 잡은 상태에서 조건을 다시 검사하고, 조건이 거짓인 동안 계속 루프를 돕니다. 그러다가 마침내 조건이 참이 되면 루프를 빠져나오고, 공유 데이터를 가지고 무언가를 한 다음,
마지막으로 반드시 락을 해제해야 합니다. 그래서 여기에서 acquire와 release가 쌍을 이루고 있고, 어떤 의미에서는 여기에서도 쌍을 이루고 있습니다. 자, 이제 함수
sys_sleep을 보고 실제 코드에서는 어떻게 생겼는지 살펴보겠습니다.

여기 보면, 이 함수 호출을 통해 첫 번째 인자를 얻어서 변수 n에 저장하고, 그 다음 tickslock을 acquire합니다. 우리는 공유 변수 ticks에 접근하고 있고, 시작
시간을 지역 변수로 잡습니다. 여기가 검사하는 조건입니다. 그리고 여기에서 sleep을 호출하는 것을 볼 수 있고, tickslock에 대한 포인터를 인자로 넘깁니다.

그리고 루프가 빠져나온 뒤에는 tickslock을 release하고 return합니다. 또한 killed 플래그가 설정되었는지를 검사하는 코드가 있고, 만약 그렇다면 즉시 락을
해제하고 return합니다. 우리를 호출한 코드는 killed가 설정되었다는 것을 알아채고, 그 프로세스를 종료시킬 것입니다. 그 부분은 다른 곳에서 일어납니다. 자, 그러면
채널은 어떨까요? 여기에서는 공유 변수의 주소를 채널로 사용하고 있습니다. 즉, 우리가 sleep할 때, 전역 변수 ticks의 주소를 채널로 넘깁니다. 그건 괜찮습니다. 결국
숫자이니까요. sleep이 어떻게 구현되어 있는지 보기 전에 yield를 잠깐 복습해 봅시다. 둘은 꽤 비슷하기 때문입니다.

둘 다 프로세스를 멈추고, 다시 말해 비활성 상태로 돌려놓습니다. 그리고 yield는 상태를 runnable로 바꿉니다. 그 전에 먼저 프로세스에 대한 락을 잡습니다. 여기서
sched를 호출할 때 우리는 반드시 이 프로세스의 락을 잡고 있어야 합니다. sched는 그 락을 release하고, 나중에 이 프로세스가 다시 스케줄될 때 그 락을 다시
acquire합니다. 그리고 다시 여기로 돌아온 뒤에 우리가 락을 release하고 계속 실행합니다. 이게 yield가 하는 일입니다. sleep은

정말 꽤 비슷합니다. 여기 sleep의 코드가 있습니다. 먼저 acquire가 보이는데, 이건 프로세스에 대한 락을 acquire합니다. 그 다음 상태를 바꾸는데,
runnable로 바꾸는 게 아니라 sleeping으로 바꿉니다. 그래서 스케줄러가 이 프로세스를 만났을 때 이제 이 프로세스의 상태가 runnable이 아님을 보고 무시할
것입니다. 따라서 다른 어떤 프로세스가 이 프로세스를 runnable로 다시 바꿔줄 때까지 계속 sleeping 상태로 남습니다.

우리는 상태를 sleeping으로 바꾸고, 여기서 sched를 호출합니다. 그리고 sched가 return하면 락을 release하고 return합니다. 그런데 여기서 다른 일들도
몇 가지 일어나는 것을 볼 수 있습니다. 어떤 spinlock에 대한 포인터가 인자로 넘어오고, 여기에서 그 spinlock을 release하고 있습니다. 즉, 우리는 프로세스에
대한 락을 acquire하고, 이 순서가 매우 중요합니다. 먼저 프로세스 락을 acquire한 뒤에

spinlock을 release합니다. 그래서 우리가 자는 동안에는 spinlock을 잡고 있지 않게 됩니다. 그리고 잠에서 깨어난 뒤에는 spinlock을 다시
acquire합니다. 이쪽 순서는 크게 중요하지 않습니다. 하지만 여기에서는 먼저 락을 release하고 있습니다. 헷갈릴 때에는, 보통 새로운 락을 acquire하기 전에 가능한
한 빨리 기존 락을 release하는 쪽을 선호합니다. 자, 코드를 계속 보죠. 채널도 인자로 넘어오는데,

여기서는 그냥 어떤 주소에 대한 포인터입니다. 그 채널을 proc 구조체의 필드에 저장하는 것을 볼 수 있고, 다시 깨어난 뒤에는 정리 차원에서 그 필드를 0으로 지웁니다.
다음으로 wakeup 함수를 보겠습니다. wakeup 함수는 채널을 인자로 받고, 모든 프로세스를 훑으면서 그 채널에서 자고 있는 모든 프로세스를 깨웁니다.

루프가 있어서 proc 배열을 하나씩 훑습니다. 각 프로세스에 대해, 그 프로세스의 상태가 sleeping인지, 그리고 그 프로세스의 channel 필드가 우리가 인자로 받은
channel과 같은지를 물어봅니다. 만약 그렇다면 상태를 runnable로 바꿉니다. 그래서 다음에 스케줄러가 그 프로세스를 만나면, 그 프로세스에게 time slice를 줄
것입니다.

필드 state와 channel, 그리고 몇 가지 다른 필드들은 그 프로세스 락에 의해 보호됩니다. 즉, 이 필드들에 접근하기 전에 반드시 해당 프로세스의 락을 acquire해야
합니다. 여기에서 acquire가 수행되는 것을 볼 수 있고, 그에 대응하는 release는 바로 여기 있습니다. 또한 이런 테스트도 보이는데, 우리가 지금 보고 있는 프로세스가
이 코드를 실행 중인 현재 프로세스와 같은지 묻고 있습니다.

주석에는 이 함수는 어떤 프로세스에 대해서도 락을 잡지 않은 상태에서 호출되어야 한다고 적혀 있습니다. 만약 그 말이 사실이라면, 이 테스트는 사실상 쓸데없는 것입니다. 우리는 그
프로세스의 상태를 보고 있고, 당연히 현재 실행 중인 프로세스의 상태는 running이지 sleeping이 아닐 것입니다. 그래서 이 테스트는 정말 필요 없어 보입니다. 아마도 이
코드를 작성해 온 역사 때문에 남아 있는 흔적일 수도 있고, 아니면 내가 모르는 어딘가에서 xv6 코드가 실제로 현재 프로세스에 대한 락을 잡은 상태에서 이 함수를 호출하고 있을
수도 있습니다. 어찌 되었든, 이 테스트가 여기 있는 건 사실이고, 조금의 추가 시간이 든다는 점 말고는 아무 해도 끼치지 않습니다. 이제 다시, sleep이 어떻게 사용되는지를
설명했던 의사코드로 돌아가고 싶습니다.

보통 우리는 어떤 조건을 기다리는 루프 안에서 sleep을 사용한다는 점을 기억하세요. 이 조건은 어떤 공유 변수를 살펴봄으로써 검사합니다. 공유 변수를 보기 전에 우리는 락을
acquire하고, 실제로 자는 상태에 들어가기 전에, 즉 sleep에 들어가기 전에, 그 락을 release해야 합니다. 왜냐하면 이것들은 spinlock이고, 우리가 자는 동안
이들이 잡힌 상태로 남아있도록 놔둘 수 없기 때문입니다. 잠자는 프로세스가 갖는 지연 없이, 이 spinlock들은 빠르게 release되어야 합니다. 그래서 실제로 자는 상태로
들어가기 전에 락을 release해야 하고, 이 동작은 sleep 함수 안에서 일어나야 합니다. 그리고 실제로 sleep 함수 안에서 일어납니다. 우리는 이것이 원자적이어야 한다고
말했습니다. 즉, 여기에서 우리의 채널과 일치하는 wakeup 호출이 온다면,

이 두 개의 명령어 묶음 앞에서 전부 실행되거나, 뒤에서 전부 실행됩니다. 중간에 끼어들어 실행되지는 않습니다. 즉, 우리가 아직 락을 잡고 있다면, 그 조건이 여전히 거짓이라는
것을 확실히 알 수 있고, 따라서 깨울 필요가 없습니다. 반대로 여기 이후에 wakeup이 실행되면, 우리는 올바르게 깨워지고, 다시 락을 acquire한 다음 조건을 다시
검사하게 됩니다.

이게 어떻게 가능한가 하면, sleep 함수 안에서 우리가 spinlock을 release하기 전에 프로세스에 대한 락을 acquire한다는 점을 떠올려 보세요. 그리고
마찬가지로, wakeup 함수에서도 어떤 프로세스를 깨울지 고려하기 전에 그 프로세스에 대한 락을 acquire하고 있다는 점을 주목하세요. 바로 그 acquire 때문에, 즉

이 프로세스에 대한 이 락은 한 번에 하나의 스레드만 잡을 수 있기 때문에, 두 경우 중 하나만 일어납니다. wakeup 쪽이 먼저 도착해서 프로세스 락을 acquire하면,
sleep 쪽 코드는 여전히 이 acquire 위쪽 어딘가에 있고 spinlock lk를 잡고 있는 상태입니다. 반대로 sleep 함수 안의 acquire가 먼저 도착해서 락을
acquire하면, 그때는…

um 스핀락 lk를 해제하지만, 실제로 잠에 들어가기 전에 채널과 상태 필드도 설정합니다. 그리고 나중에, 이 프로세스에 대한 이 락은 우리가 sched 함수 안으로 들어가자마자
바로 해제될 것이고, 그 시점에서 wakeup은 해당 프로세스에 대한 락을 획득할 수 있습니다. 그러면 그 프로세스가 sleep 중이라는 것과 채널이 설정되어 있다는 것을 확실히
알 수 있습니다. 그래서

그 프로세스를 깨울 수 있게 됩니다. 이렇게 해서 여기 있는 두 연산의 원자성(atomicity)을 보장하는 것입니다. 자, 이 비디오는 여기까지입니다. 다음 비디오에서 봅시다.
