# xv6 Kernel-15: Trampoline and Trapframe

| Field | Value |
| --- | --- |
| Video ID | `_p_MvMnwJbE` |
| URL | <https://www.youtube.com/watch?v=_p_MvMnwJbE> |
| Source | `docs/reference/scripts/original/15-xv6-kernel-15-trampoline-and-trapframe-_p_MvMnwJbE.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제에 대한 시리즈의 일부입니다. 이 비디오에서는 trampoline.S 어셈블리 코드에 대해 살펴보겠습니다. 여기에는 uservec이라고 불리는 코드와
userret이라고 불리는 코드가 포함되어 있습니다. 또 trap.c 파일 안의 일부 코드도 함께 살펴볼 것이고, usertrap 함수와 usertrapret 함수에 대해 이야기할
것입니다. 제가 부르는 “지도(map)”부터 시작해 보겠습니다.

즉, 유저 코드가 실행 중인데 트랩이 발생하고, 먼저 uservec 어셈블리 코드가 실행됩니다. 그 다음 usertrap 함수로 들어가고, 거기서 트랩이 어떤 종류의 트랩이든
처리합니다. 그런 뒤 usertrapret 함수를 호출하고, 이 함수가 다시 userret 어셈블리 코드 쪽으로 들어가며, 이 어셈블리 코드는 마지막에 sret(system
return) 명령을 실행하면서 끝납니다.

트랩이 발생하면, 하드웨어에 의해 인터럽트가 비활성화되고, 우리는 supervisor 모드로 전환됩니다. 그리고 현재 프로그램 카운터가 sepc 레지스터에 저장됩니다. stvec
레지스터에는 트랩 핸들러의 주소가 들어 있다고 가정하며, 하드웨어는 그 값을 프로그램 카운터에 로드합니다. 이로 인해 사실상 uservec 코드로 점프가 일어나는데, 그게 바로
stvec이 담고 있는 주소이기 때문입니다. 그리고 uservec은 유저 모드 코드의 전체 상태를 저장하는 것부터 시작해야 합니다. 즉 레지스터들과 프로그램 카운터를 저장해야
하고, 이 값들은 trapframe에 저장됩니다. 또한 sp와 tp 레지스터를 로드해서 커널 코드를 실행할 준비를 합니다. 그리고 나서 커널 주소 공간에서 실행하도록 전환합니다.
이 초기 동작들은 페이지 테이블이 유저의 주소 공간을 가리키고 있을 때 일어나지만, satp 레지스터를 로드한 이후에는 코드가 이제 커널의 주소 공간에서 실행되게 됩니다.
trampoline 페이지는 모든 주소 공간의 최상위 페이지에 매핑되어 있기 때문에, 여기서 실행 중인 코드에는 영향을 주지 않지만, 이후에 유저 주소 공간이 아닌 커널 주소
공간에 있는 코드로 점프할 수 있게 해 줍니다. 그래서 이제

uservec에서 무슨 일이 일어나는지 살펴보겠습니다. 여기 trampoline.S 파일의 코드가 있습니다. 어셈블리 코드이고, 이 파일 안에는 두 개의 코드 조각이 들어
있습니다. 하나는 uservec 함수이고, 한 페이지 반 정도를 차지합니다. 그리고 나중에 설명할 userret이라는 또 다른 코드가 있습니다. 이 두 개가 trampoline
파일 안의 전부입니다.

이 파일이 어셈블로 변환되면, “text” 섹션(혹은 세그먼트)에 배치되고, 링커에 의해 하나의 페이지에 따로 놓이며 페이지 경계에 정렬(page aligned)됩니다. 여기서
trampoline이라는 소문자 레이블을 정의하는데, 이것이 이 페이지가 물리 메모리에서 위치한 주소입니다. 그리고 uservec이라는 것도 있는데,

이 레이블은 조금 뒤에 살펴볼 이 코드의 시작 주소입니다. 이 페이지가 페이지 경계에 정렬되어 있으므로, 여기 있는 align 지시문은 아무 효과가 없습니다. 이미 4바이트 경계에
정렬돼 있기 때문에 uservec과 trampoline의 숫자 값은 같게 됩니다. 따라서 페이지의 시작부터 uservec까지의 오프셋을 계산할 때, trampoline에서
uservec을 빼면 0이 됩니다.

반면, 이 파일에서 더 아래쪽에 있는 userret의 오프셋을 계산하면, 그 오프셋은 0보다 커질 것입니다. 그래서 같은 주소에 두 개의 다른 레이블을 붙여 두는 이유가 여기에
있습니다. 이것(trampoline)은 물리 메모리에서 이 페이지의 시작이고, 이것(uservec)은 그 페이지 안에서 이 특정 루틴의 시작입니다. 그리고 global 지시문으로
이 두 레이블을 외부로 내보냅니다.

이제 처음에는 레지스터들을 저장해야 합니다. 유저 코드가 실행 중일 때 무엇이 참인지 생각해 볼 수 있는데, 그중 하나는 sscratch라고 불리는 control and
status register에 trapframe을 가리키는 포인터가 들어 있다는 점입니다. 이것이 편리한 이유는, 아직 레지스터를 저장하기 전이라서 레지스터들을 마음대로 쓸 수
없고, 거의 아무 코드도 실행할 수 없기 때문입니다. 하지만 우리는 CSRRW 같은 control and status register 읽기/쓰기 명령을 사용할 수 있는데, 이
명령은 a0와 sscratch의 값을 서로 교환해 줍니다. 이 명령이 실행되고 나면, sscratch에는 유저가 쓰고 있던 a0 값이 들어가고, a0에는 trapframe을
가리키는 포인터가 들어갑니다. 이제 이 포인터를 사용해서 나머지 레지스터들을 모두 저장할 수 있습니다.

여기서 레지스터 저장을 시작하고, a0 자신을 제외한 모든 레지스터를 저장합니다. 이 오른쪽에 보이는 숫자들이 오프셋이고, 이 명령들은 해당 레지스터를 이 포인터로부터의 해당
오프셋 위치에 더블워드로 저장합니다. 오프셋 2 자리가 a0를 위해 비워져 있는 것을 볼 수 있습니다. 그래서 a0를 제외한 나머지 전부를 저장합니다. 그리고 아래로 계속
내려가면서 모든 레지스터를 저장합니다. 이제 레지스터들을 모두 저장했으므로 조금 더 자유롭게 움직일 수 있습니다. 이미 t0를 저장했으니 t0를 사용할 수 있습니다. 이 명령은
sscratch 레지스터에서 t0로 값을 읽어오고, 다음 명령은 그 t0 값을 오프셋 112 위치에 저장합니다. 이렇게 해서 이제 a0도 저장한 셈입니다.

이제 trapframe을 살펴보겠습니다. 이 그림에서는 trapframe이 어떻게 구성되어 있는지를 보여주고 있습니다. 여기에는 유저 모드 스레드의 상태를 저장하는 여러 위치들이
있고, 방금 레지스터 저장 영역에 31개의 범용 레지스터를 모두 저장했습니다. 아직 유저의 프로그램 카운터는 저장하지 않았지만, 곧 저장할 것입니다. 또 커널 코드를 실행하기 위해
로드해야 할 여러 위치들도 있습니다. 바로 여기서 그 작업을 하고 있습니다. trapframe 안에 저장된 스택 포인터를 읽어서 sp 레지스터에 넣고, 스레드 포인터(tp)를
로드하고, satp 레지스터도 로드합니다. 이것들을 하나씩 보겠습니다.

trapframe에 저장돼 있던 sp 값을 가져다가 sp 레지스터에 넣습니다. 트랩이 여기서 발생하면 그 시점부터는 커널 모드로 실행됩니다. 그래서 이 모든 코드를 커널 모드에서
실행하게 됩니다. 타이머 인터럽트이든, 디바이스 인터럽트 핸들러이든, 시스템 콜이든 어떤 종류의 트랩이든 여기서 처리합니다. 유저 코드로 돌아가기 전까지, 여기서부터 여기까지의
작은 구간을 이 프로세스의 스레드 일부로 볼 수도 있고, 작은 미니 스레드처럼 생각할 수도 있습니다. 하지만 지금 우리는 완전히 새로운 스택에서 시작합니다.

이전의 레지스터 값들을 복구하지 않고 있기 때문에, 이것을 기존 커널 스레드의 연속으로 보는 것은 적절하지 않습니다. 대신 새로운 스레드를 시작한다고 보는 편이 맞습니다. 우리가
로드하는 것은 스택 포인터와 tp뿐이고, 다른 레지스터들은 유저 모드 코드에서 남겨진 값들을 그대로 갖게 됩니다. 우리가 로드하는 스택 포인터에 대해서는,
“복구(restore)”라기보다는 “로드(load)”라고 말하는 것이 더 정확했을 겁니다. 이전 값을 되찾아 오는 것이라기보다는 스택 포인터를 초기화하는 것이기 때문입니다. 커널
스택 페이지라고 부르는 값으로 스택 포인터를 초기화합니다. 각 프로세스, 즉 최대 64개의 프로세스 각각은 커널 스택용 페이지 하나를 가지고 있고, 여기서 sp에 로드하는 것이
바로 그 커널 스택입니다. 좀 더 정확히 말하면, 커널 스택 페이지를 가리키는 포인터를 로드하는 것입니다.

그 페이지의 맨 위입니다. 스택은 아래쪽으로 자라난다는 것을 기억하세요. 그래서 우리는 사실상 완전히 비어 있는 스택을 가리키는 포인터로 sp를 로드하고 있는 셈입니다. 이
지점에서 이런 일이 일어납니다. 우리는 계속 실행 중이고 현재 어떤 특정 코어에서 실행 중입니다. 하지만 정확히 어떤 코어에서 실행 중인지는 알지 못합니다. 다만 사용자 모드
코드로 들어가기 전에

바로 여기 있는 이 필드에 우리가 실행 중인 코어의 번호, 즉 이른바 hart id를 저장해 두었습니다. 그래서 이 시점에서 그 값을 다시 불러와서 이른바 thread
pointer 레지스터에 로드합니다. 참고로 영상을 보고 있다면, 제가 실제로는 이 둘을 뒤집어 놔서 오프셋이 정확히는 맞지 않는데, 어쨌든 개념은 같습니다. 그래서 우리는 tp에
코어 번호를 로드합니다. 이제 이걸

활용할 수 있게 되었고, 여기 아래에서 할 일은 usertrap 함수로 점프하는 것입니다. 그래서 t0 레지스터를 통한 점프를 할 겁니다. 여기서 t0를 로드하고 있는데, 이
필드에는 이전에 usertrap 함수의 주소를 저장해 두었습니다. 따라서 이 명령은 아래쪽에서 할 점프를 준비하는 것입니다. 이 시점에서 우리는 커널의 주소 공간으로 전환할 준비가
되어 있습니다.

이제 모든 설정을 마쳤기 때문에 커널의 주소 공간에서 커널 코드를 실행할 수 있습니다. 여기서는 미리 저장해 두었던 커널의 페이지 테이블에 대한 포인터를 가져와서, satp라는
제어/상태 레지스터에 저장합니다. t1 레지스터로 그 값을 가져온 다음, 이 레지스터에 써 넣습니다. 그리고 fence 명령을 실행해서,

그 이전에 실행되어야 할 모든 것이 satp가 바뀌기 전에 완전히 실행되도록 하고, satp 레지스터가 로드된 이후에 실행되어야 할 명령들은 그 이후로 미뤄지도록 보장합니다.
RISC-V 프로세서는 함수를 호출할 때, 반환 주소를 ra 레지스터(return address)에 저장한 다음 그 다음 주소로 점프하는 방식으로 호출을 합니다. 일반적으로 호출은
이렇게 이뤄집니다.

하지만 이 경우에는 단순히 점프만 하고, 반환 주소를 저장하지 않습니다. 그 이유는 usertrapret가 반환하지 않기 때문입니다. 이제 usertrap return, 아니
usertrap을 보러 가 봅시다. 그 코드가 여기 있습니다. 이 코드는 trap.c 파일에 들어 있고, 이 파일에는 제가 이번

영상에서는 다루지 않을 다른 함수들도 있습니다. 하지만 지금은 usertrap을 보겠습니다. 이것은 C 함수 형태로 표현되어 있지만, 반환하지 않는 C 함수입니다. 그럼 이 함수는
무엇을 할까요? 함수의 끝에서 또 다른 함수를 호출하고, 그 함수가 역시 반환하지 않습니다. 곧 보게 될 겁니다. 여기서는 sanity check를 합니다. status 레지스터에
무엇이 들어 있는지, 특히

이전 특권 레벨 비트를 확인합니다. 이 비트는 트랩이 발생했을 때 코어가 사용자 모드에서 실행 중이었는지, 아니면 supervisor 모드에서 실행 중이었는지를 알려 줍니다.
반드시 사용자 모드에서 실행 중이었어야 하고, 그렇지 않았다면 정말 심각한 문제가 있는 것입니다. 이제 우리는 커널 모드에서 실행 중인데, 여기서 또 다른 트랩이 발생하면 어떻게
될까요? 예를 들어 어떤

디바이스가 서비스를 필요로 해서 인터럽트를 일으킬 수 있습니다. 그래서 여기서는 stvec에 새 주소를 씁니다. 커널 인터럽트를 처리하는 함수의 주소를 stvec에 써 넣습니다.
그리고 proc 구조체에 대한 포인터를 가져옵니다. 전에 proc 구조체 그림을 보여 줬었는데, 바로 이 그림이었습니다. 여기에는 페이지 테이블에 대한 포인터, trapframe에
대한 포인터 등

여러 정보가 들어 있고, 특히 우리가 관심 있는 필드는 trapframe입니다. 이 필드는 이 프로세스의 trapframe 페이지가 메모리 상에서 어디에 있는지, 그 물리 주소에
대한 포인터입니다. 64개의 각 프로세스마다 trapframe을 위한 자기 자신의 물리 메모리 페이지 하나씩을 가집니다. 이 페이지들은 사용자 주소 공간의 끝에서 두 번째 페이지에
매핑되지만, 우리는 더 이상

사용자 주소 공간에서 실행 중이 아니므로 물리 주소가 필요합니다. 그리고 이 필드에는 물리 메모리에 있는 trapframe에 대한 포인터가 들어 있습니다. 이제 이걸 사용할 수
있습니다. 이전까지는 아직 사용자 주소 공간의 프로그램 카운터, 그러니까 program counter를 저장하지 않았습니다. 여기서 그 값을 저장합니다. 이것이 사용자 모드
프로세스의 마지막 상태 정보입니다.

이 부분은 제어/상태 레지스터를 읽어서 그 값을 저장하는 과정입니다. 제 로드맵 그림에서 큰 if 문을 보여줬는데, 지금이 바로 그 시점입니다. 이제 우리는 usertrap 안에
있고, 그 if 문을 실행하게 됩니다. 바로 다음에 일어나는 일이 그것입니다. 우선 scause 레지스터의 값을 보고 무슨 일이 일어났는지를 판단합니다. 그 값이 8이면, 트랩이

시스템 콜의 결과로 발생했다는 뜻입니다. 그러면 여기 있는 코드를 실행하고, 결국 system call이라는 함수를 호출해서 처리하게 됩니다. 하지만 그전에 이 trap, 아니
killed 필드를 검사합니다. 이건 proc 안에 있는 필드이고, 불리언 값입니다. 누군가 이 프로세스를 죽이고 싶다면 이 불리언 필드를 1로 설정할 수 있습니다. 그래서 지금
당장 이 프로세스가 자살해야 하는지

확인합니다. 만약 그래야 한다면 exit 함수를 호출합니다. exit는 반환하지 않습니다. 그게 그 분기입니다. 하지만 지금은 자살할 필요가 없다고 가정하고 계속 진행해 봅시다.
RISC-V에서 시스템 콜을 수행하는 명령은 environment call 명령, 즉 ecall 명령입니다. 우리 RISC-V 코어는 이 명령의 포인터를 epc 레지스터에 저장해
둘 것입니다. 하지만 우리가 돌아갈 때,

이 명령을 다시 실행하고 싶지는 않습니다. 대신 ecall 바로 다음 명령을 가리키도록 pc를 증가시켜야 합니다. RISC-V 프로세서의 명령은 4바이트 길이이므로, pc를 4만큼
증가시켜서 조정합니다. 이제 인터럽트를 다시 켤 준비가 되었습니다. 이 시점부터는 커널 코드도 또 다른 트랩에 의해 인터럽트될 수 있습니다. 예를 들어 어떤 디바이스가

처리를 필요로 하면, 우리가 시스템 콜을 처리하는 도중에도 인터럽트가 일어날 수 있습니다. 사용자 스레드의 상태는 여기 trapframe에 저장해 두었고, 만약 우리가 인터럽트되면
커널 스레드의 상태는 다른 어딘가에 저장해야 합니다. 하지만 지금은 그 문제를 깊이 걱정하지는 않겠습니다. 이제 이 함수를 호출하고, 시스템 콜의 종류에 따라 적절히 처리합니다.
만약

exit 시스템 콜이라면 더 이상 돌아오지 않을 수도 있습니다. 어쨌든 그 외의 경우에는 여기로 되돌아와서 계속 진행합니다. 만약 시스템 콜이 아니라면 deviceintr라는
함수를 호출합니다. 이 함수는 다시 cause 레지스터를 확인해서 어떤 디바이스가 처리를 필요로 하는지 알아내고, 코드 값을 반환합니다. 일반적인 디바이스, 예를 들어 직렬 콘솔
디바이스나 디스크 디바이스인 경우에는 1을 반환합니다. 타이머 인터럽트인 경우,

좀 더 정확히 말하면, 머신 모드 코드가 타이머 인터럽트를 소프트웨어 인터럽트로 변환한 경우에는 2를 반환합니다. 그 외의 경우에는 에러로 간주하고 0을 반환합니다. 여기서는
에러인지 아닌지를 검사합니다. 에러가 아니고 디바이스 인터럽트라면 여기에서 처리합니다. 타이머 인터럽트라면, 우리는 어떤 카운터를 업데이트해서 시간 측정을 계속하기 위해…

현재 시간을 추적해야 하지만, 기본적으로는 그냥 2를 리턴하고, 그 값을 이 switch device 변수에 저장합니다. 그리고 바로 아래에서 그 값을 검사합니다. 만약 이 값이
0이면 에러 메시지를 출력하고, 앞으로 아래쪽에서 자기 자신을 죽여야 한다는 것을 가리키기 위해 killed라는 불리언을 설정합니다. 에러 메시지는 단순히 프로세스 id를
출력하고, 에러가 발생한 사용자 주소 공간에서의 위치를 출력하고, 그리고 stval의 값을 출력할 텐데, 여기에 추가 정보가 들어 있을 수도 있습니다. 포맷팅 코드 %p에 익숙하지
않다면, 이것은 기본적으로 … 제가 잠깐 멈춘 이유는, 여기서 우리가 control and status 레지스터를 읽고 있다는 것을 봤고, 잠시 동안 우리가 인터럽트를 켰다는
사실을 잊었기 때문입니다.

하지만 우리는 시스템 콜 안에 있을 때만 인터럽트를 켰습니다. 디바이스 인터럽트에 있을 때는 인터럽트가 켜지지 않습니다. 따라서 디바이스 핸들러는 인터럽트가 비활성화된 상태에서
실행되고, 마찬가지로 이 에러 메시지도 인터럽트가 비활성화된 상태에서 실행됩니다. 그래서 이 레지스터들의 값은 여전히 직접 읽어도 괜찮습니다. 자, 시스템 콜 중이거나 여기서든
어느 시점에 killed를 true로 설정했다면, 자기 자신을 종료합니다.

마지막으로, 타이머 인터럽트가 있었다면 yield 함수를 호출합니다. yield 함수는 일종의 no-op처럼 생각할 수 있습니다. 이 함수를 호출한 뒤 리턴하지만, 바로 리턴하지
않을 수도 있습니다. yield는 scheduler를 호출하고, scheduler는 다른 쓰레드를 실행하도록 스케줄할 수 있고, 이 yield 함수가 최종적으로 리턴하기까지 꽤
시간이 걸릴 수도 있습니다. 하지만 언젠가 스케줄러가 이 특정 쓰레드에게 다시 프로세서를 돌려줄 것이고, 그 과정에서 모든 레지스터를 저장했다가 복원하기 때문에, yield는
단순히 리턴합니다. 다만 반드시 같은 코어에서 리턴하는 것은 아닙니다. 다른 코어로 스위치되었을 수도 있습니다. 자, 이 함수로 계속 진행해 보겠습니다. 이렇게 맞춰서 놓고 보면,

이 테스트 이후에 usertrapret을 호출합니다. usertrapret은 바로 여기 있죠. 그러니까 사실상 일종의 goto라고 볼 수도 있고, 그냥 아래로 떨어져서 실행이
이어진다고 생각할 수도 있습니다. 우리는 fork 처리 과정에서도 usertrapret을 호출하는데, 지금은 그 얘기는 하지 않겠습니다. 어쨌든 그 때문에 이 부분을 단순히
fall-through가 아니라 별도의 함수로 빼놓은 것입니다.

그래서 이제, 어, 지금은 여기 아래에 와 있고, yield를 할지 말지 고려를 끝냈고, 이제 사용자 모드 쓰레드로 돌아갈 준비가 된 상태입니다. 우리는 지금 usertrapret
안에 있고, 인터럽트를 비활성화해야 하고, 몇 가지 설정을 한 다음 trampoline 페이지 안에 있는 어셈블리 코드로 들어가야 합니다. 그 시점에 무슨 일이 일어나는지 차례대로
살펴보겠습니다. 시스템 콜이 있었던 경우처럼, 인터럽트가 켜져 있을 수도 있기 때문에, 인터럽트를 끌 필요가 있습니다. 이제 control and status 레지스터를 다룰
것이고, 이 레지스터들을 만지는 동안 코어가 인터럽트되는 것을 허용할 수 없습니다. 여기 위에서는 proc 구조체에 대한 포인터도 얻고 있습니다.

이전에 우리는 stvec 레지스터를 kernelvec을 가리키도록 설정했는데, kernel 모드에서 실행 중일 때 발생하는 인터럽트와 트랩은 kernelvec이 처리하도록 하기
위한 것이었습니다. 이제 sdvec에 uservec 코드의 주소를 써 넣어서 그 값을 복원하려고 합니다. 그러면 이후의 트랩은 모두 uservec으로 점프하게 됩니다. 그 주소가
무엇이냐 하면, 내가 아까 0이라고 말했던 trampoline으로부터의 오프셋에, 메모리에서 가장 높은 페이지인 trampoline 페이지의 주소를 더한 값입니다.

다음으로 trapframe을 봅니다. 다시 말해, proc 구조체에 대한 포인터가 있으니, 그것을 따라가서 이 프로세스의 trapframe 페이지, 즉 우리의 trapframe의
물리 주소에 도달합니다. 그리고 다음 트랩이 발생할 때 필요하게 될 이 필드들, 여기 있는 네 개의 필드를 설정할 겁니다. 지금 이 시점에서 우리가 하고 있는 일이 바로
그것입니다. 현재 우리는 커널의 주소 공간에서 실행 중입니다. 그래서 커널 페이지 테이블에 대한 포인터를 얻어서 이 satp 필드에 저장합니다. 이건 엄밀히 말하면 포인터는
아니지만, 포인터처럼 생각할 수 있습니다. 몇 비트가 설정되어 있지만, 우리는 여기 저장된 그대로 다시 복원할 것입니다. 그다음으로 sp 필드, 즉 스택 포인터를 설정합니다. 모든
프로세스는 스택을 가지고 있습니다. 여기에서 그 스택이 보이고요.

아래쪽에 그려 놓은 것처럼 페이지로 가는 작은 포인터를 따로 그리진 않았지만, 개념은 같습니다. 이 커널의 스택 페이지에 대한 포인터가 있고, 여기서는 그 페이지 바로 끝 주소를
가리키도록 레지스터를 초기화합니다. 즉, 사실상 빈 스택이 됩니다. 그리고 trap 필드를 usertrap 함수, 그러니까 이전에 봤던 C 코드의 주소를 가리키도록 초기화합니다.

마지막으로, 현재 코어 번호를 얻기 위해 현재 thread pointer 레지스터를 읽습니다. 아까 yield를 했기 때문에, 더 이상 같은 코어에서 실행 중이 아닐 수도
있습니다. 따라서 지금 실행 중인 코어의 코어 번호를 다시 얻어서 hartid라는 필드에 저장합니다. 이제 sret 명령을 써서 사용자 코드로 돌아갈 겁니다.

그래서 sret 명령을 위해 준비를 해야 합니다. sret 명령은 status 레지스터를 사용할 것입니다. 여기서 우리가 하는 일은 이전 권한 모드가 사용자 모드였다고 알려 주는
것입니다. 그래서 이 비트를 클리어합니다. 그러면 sret 명령은 이전 권한 모드로 돌아가게 되고, 즉 우리를 사용자 모드로 바꿀 것입니다. 또 이전 인터럽트 인에이블 비트가
설정되어 있었다고 알려 주고 있습니다. 그래서 sret 명령이 실행될 때 인터럽트를 활성화하게 됩니다. 그런 다음 이 값을 status 레지스터에 써 넣습니다. 여기서 status
레지스터를 읽고, 업데이트하고, 다시 써 넣는 것입니다. sret 명령은 또 scause가 아니라 sepc 레지스터를 사용해서 사용자 모드 프로그램의 프로그램 카운터를 복원합니다.
그래서 여기에서 우리가 저장해 두었던 값을 읽어 와서 epc 레지스터로 옮깁니다.

그리고 satp라는 변수가 있는데, 이것은 레지스터가 아니라 변수입니다. 여기에는 이 사용자 프로세스의 현재 페이지 테이블에 대한 포인터, 즉 이 사용자 프로세스의 페이지 테이블에
대한 포인터를 넣습니다. proc 구조체에는 page table을 가리키는 page table 필드가 있고, 우리는 사실상 그 페이지 테이블의 주소를 이 변수에 저장하는 셈입니다.
하지만 satp 레지스터에는 몇 개의 필드가 섞여 있습니다. make satp는 전처리기 매크로로, 페이지 테이블의 주소를 인자로 받아서, 오프셋 비트들을 쉬프트해서 없애고, 위쪽
필드의 몇 비트를 설정해서 우리가 RISC-V sv39 모드를 사용하고 있다는 것을 나타내 줍니다. 하드웨어와 관련된 몇 가지 세부사항이지만, 본질적으로는 사용자 페이지 테이블에
대한 포인터를 만들어 넣는 작업이라고 생각하면 됩니다.

마지막으로, 여기 아래에 이런 것이 있는데, 이걸 조금 풀어서 설명해 보겠습니다. 두 번째 줄에서 우리가 하고 있는 일은 함수를 호출하는 것입니다. 그리고 그 함수에 두 개의
인자를 넘기고 있습니다. 그럼 우리가 호출하는 함수는 무엇이냐? 바로 여기에서 그것을 정하고 있습니다.

그리고 이것은 userret 코드, 더 정확히 말하면 userret에서 trampoline을 뺀 값이 trampoline 페이지 안에서 userret 코드가 있는 오프셋입니다.
그리고 여기에 메모리에서 가장 높은 페이지, 즉 trampoline 페이지의 주소를 더해 줍니다. 그래서 이 계산 결과가 우리가 사용할 주소가 됩니다. 그리고 trampoline
페이지는 모든 주소 공간에 매핑되어 있다는 점을 기억하세요.

지금 우리는 커널의 주소 공간에서 실행 중이므로 괜찮습니다. trampoline 페이지는 사용자 주소 공간에도 매핑되어 있기 때문에, SATP 레지스터에 어떤 값이 들어 있든지
상관없이, 어쨌든 userret 코드에 대한 포인터를 얻을 수 있고, 그다음 이 코드를 호출합니다. 여기 있는 캐스팅은 컴파일러에게 fn이 “void를 반환하고, 인자가 두 개
있는 함수에 대한 포인터다”라고 알려 주는 방법입니다.

인자 두 개는 trapframe, 그러니까 메모리에서 두 번째로 높은 페이지의 주소에 대한 포인터와, SATP 레지스터에 저장해야 할 값입니다. 이제 trampoline 페이지
안의 userret을 보겠습니다. 자, 여기 있습니다. 주석을 보면 우리가 인자를 두 개 넘긴다고 되어 있죠.

RISC-V에서는 호출을 구현할 때, 또는 C 컴파일러가 함수를 호출할 때, 두 인자를 a0와 a1 레지스터에 넣습니다. 반환 주소는 ra 레지스터, 즉 return address
레지스터에 저장합니다. 하지만 우리는 이 함수에서 다시 돌아오지 않을 것이기 때문에 반환 주소는 신경 쓰지 않습니다. 이 시점에서 a1에는 사용자 주소 공간의 페이지 테이블에 대한
포인터가 들어 있습니다.

우리는 이 값을 SATP라고 불리는 제어 및 상태 레지스터(CSR)에 기록합니다. 이제 사용자 주소 공간으로 전환된 상태입니다. fence를 실행하고, a0는 trapframe
페이지를 가리키고 있습니다. 그래서 a0를 활용할 수 있습니다. 우리가 가장 먼저 하는 일은, 오프셋 112에 저장되어 있던 사용자 a0 레지스터 값을 가져오는 것입니다. 그것을
t0 레지스터로 로드합니다.

그리고 그 값을 scratch 레지스터에 씁니다. 이제 scratch 레지스터에는 사용자 a0가 들어 있고, a0에는 trapframe에 대한 포인터가 들어 있습니다. 그다음
a0를 제외한, 이전에 저장해 두었던 나머지 모든 레지스터를 로드합니다. a0는 오프셋 112에 있는 값을 여기서는 로드하지 않습니다.

모든 레지스터를 로드한 다음, 여기에서 scratch 레지스터와 a0의 내용을 서로 바꿉니다. 이제 a0에는 우리가 필요로 하는 사용자 값이 들어 있고, scratch 레지스터에는
trapframe에 대한 포인터가 들어 있습니다. 이 포인터는 다음 trap이 발생했을 때 우리가 가지고 있기를 기대하는 값입니다. 그리고 드디어 sret 명령을 실행할 준비가 된
것입니다.

이제 정리하면서, 제가 ‘road map’이라고 부른 그림으로 돌아가 보겠습니다. 우리는 지금 이 아래쪽에 있습니다. 여기에 대해 한 가지 언급하고 싶은 것은, 설명을 제가
논리적으로 이해하기 쉬운 순서대로 했다는 점입니다. 실제로 sstatus 레지스터는 usertrapret 쪽에서 미리 설정되었습니다. 하지만 어쨌든 우리가 무슨 일을 했는지 볼 수
있습니다. 인터럽트를 비활성화했고, st를 다시 사용자 공간 코드, 즉 여기 위쪽에 있는 이 코드로 되돌려 놓았습니다.

sp와 tp를 설정해서, 다음 trap이 발생했을 때 사용할 수 있도록 만들어 두었습니다. scpc 레지스터를 로드했고, SATP 레지스터를 사용자 페이지 테이블을 가리키도록
설정했습니다. 그래서 이제 우리는 사용자 가상 주소 공간에서 실행 중입니다. 사용자 레지스터들을 모두 복원했고,

status 레지스터를 로드했거나, 정확히 말하면 위에서 보여준 대로 그 비트들을 바꾸었습니다. 그리고 마지막으로 sret을 실행할 준비가 되었습니다. 그러면 프로그램 카운터를
원래 값으로 되돌리고, 실행을 계속하며, 모드를 사용자 모드로 바꾸고 인터럽트를 활성화합니다. 이제 우리는 trap이 여기 위에서 발생하기 직전 상태로 완전히 되돌아온 것입니다.

그리고 다음 trap을 받아들일 준비가 된 상태입니다. trampoline 페이지에 대한 설명은 여기까지입니다. 다음 비디오에서 봅시다.
