# xv6 Kernel-5: kalloc Mem Management

| Field | Value |
| --- | --- |
| Video ID | `wPXE08HBNnE` |
| URL | <https://www.youtube.com/watch?v=wPXE08HBNnE> |
| Source | `docs/reference/scripts/original/05-xv6-kernel-5-kalloc-mem-management-wPXE08HBNnE.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 시리즈의 일부입니다. 이 비디오에서는 메모리 관리, 그리고 xv6 커널이 여유 메모리를 어떻게 관리하는지를 살펴보겠습니다. 이 부분은
상당히 직선적이고 단순합니다. 모든 메모리 관리는 4KB 블록, 또는 페이지라고도 부를 수 있는 단위로 이루어집니다. 그래서 4킬로바이트 페이지들이고, 이 페이지들은 free
list 위에서 관리됩니다. 여기에는 중요한 함수 두 개, kalloc과

kfree가 있고, kalloc의 경우에는 free list에서 항목을 하나 꺼내 오고, kfree의 경우에는 free block을 free list에 다시 추가합니다. 실제로는
이게 전부라고 해도 될 정도입니다. 하지만 이걸 조금 더 자세히 들여다보겠습니다. 우선, 이 모든 코드는 kalloc.c 파일에 들어 있습니다. 여기 파일이 있고, 몇 개의
include가 있습니다. 여기에 free list에 대한 포인터가 있고,

run이라는 작은 구조체가 있는데, 이 안에는 다음 블록을 가리키는 포인터 하나만 있습니다. 그러니까 free list는 4킬로바이트 페이지들로 이루어진 연결 리스트를 가리키는
포인터입니다. 이 구조체는 free list와 lock이라는 이름의 spinlock을 묶어서 하나로 모은 것입니다. 이 lock은 free list와 연관시키는 데 사용됩니다.
free list를 조작할 때마다 lock을 잡아야 합니다. 다시 말해 lock을 acquire, 혹은 set해야 하고,

free list를 다 보고 나면 그 lock을 release해야 합니다. 또 하나의 변수가 여기 있는데, 이 변수는 프로그램이 빌드될 때 링커에 의해 설정되며, 커널 바로 뒤의
첫 주소를 가리키도록 설정됩니다. 커널의 실행 코드, 즉 text 섹션과 data 섹션 뒤의 첫 주소이고, 이것이 첫 번째로 사용할 수 있는 메모리입니다. 초기화 과정에서 이 값을
사용할 겁니다.

이제 초기화를 잠깐 보면, 먼저 lock을 초기화하고, 그다음 freerange를 호출해서 사용 가능한 첫 바이트부터 물리 메모리의 최상단까지를 free로 만듭니다. 이 최상단은
상수로 정해져 있고, 우리 경우에는 128MB입니다. 이제 allocation 루틴을 봅시다. 이 루틴은 여기서 spinlock을 acquire하고, free list를 조작한
다음 lock을 release합니다. 마지막으로 포인터를 반환합니다.

좀 더 자세히 보면, 먼저 free list에서 첫 번째 항목을 하나 꺼냅니다. 뭔가를 얻었다면, 즉 메모리가 아직 다 소진되지 않았다면 계속 진행할 수 있습니다. 이때 free
list를 수정해서 다음 항목을 가리키게 합니다. 예를 들어 free list가 이 항목을 가리키고 있다면, 그 항목을 가져오고 free list를 그다음, 즉

리스트에서 두 번째였던 항목을 가리키도록 바꿉니다. 마지막으로 spinlock을 release한 뒤, 그 포인터를 반환합니다. 반환하기 전에, 만약 무언가를 얻었다면, 페이지 안의
모든 바이트를 값 5로 설정해서, 그 페이지를 쓰레기 값으로 채웁니다. 다음은 kfree 루틴입니다. 이 루틴은 페이지에 대한 포인터를 인자로 받습니다. 이 루틴이 하는 일은,
먼저 약간의 오류 검사를 하는 것입니다. 이 물리 주소 pa가

페이지 경계에 정렬되어 있는지 확인하고, 너무 작지도, 너무 크지도 않은지 확인합니다. 즉 시작 주소보다 작지 않고, 끝 주소보다 크지 않은지 검사합니다. 이 검사를 통과하면,
페이지를 쓰레기 값으로 채우는 것부터 시작합니다. 이 경우 쓰레기 값은 1입니다. 그 목적은 커널 나머지 코드에 있는 버그를 끌어내거나

트리거하기 위한 것입니다. free list에 페이지를 반환한 뒤에는 그 페이지를 다시 사용하면 안 됩니다. 그래서 페이지 안의 모든 바이트를 어떤 값으로 설정해 두면, 그
페이지의 데이터를 사용하려는 프로그램 코드가 있다면 그 코드를 혼란스럽게 만들어, 버그를 드러나게 하려는 의도입니다. 그런 다음 r을 우리의 포인터로 사용할 것이고,
spinlock을 acquire하고, free list를 조작해서 그 페이지를 free list에 추가한 뒤 spinlock을 release합니다.

free list에 추가하는 방식은, r이 이 free block이라면, 우선 그 block의 next 포인터를 free list의 첫 번째 요소를 가리키도록 설정하고, 그다음
free list가 이제 이 block을 가리키도록 free list를 재지정하는 것입니다. 이제 다시 초기화를 보겠습니다. 우리는 이 시작 지점부터 물리적 최상단, 즉 물리
메모리의 맨 위까지 모든 메모리를 초기화하고 있습니다.

그리고 이를 수행하는 함수가 여기 있습니다. 이 함수는 시작 주소를 인자로 받고, 그 주소를 가장 가까운 페이지 경계까지 올림 처리합니다. 왜냐하면 이 주소가 반드시 페이지
정렬되어 있지는 않기 때문입니다. 그런 다음 루프를 돕니다. 여기서 하는 일은, 각 페이지를 kfree를 호출해서 free list에 추가하는 것입니다. 단순한 루프이고, p를
4,096바이트씩 증가시키면서

p가 끝 주소를 넘어설 때까지 계속합니다.
