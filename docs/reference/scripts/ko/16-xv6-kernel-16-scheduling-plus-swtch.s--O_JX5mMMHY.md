# xv6 Kernel-16: Scheduling + swtch.S

| Field | Value |
| --- | --- |
| Video ID | `-O_JX5mMMHY` |
| URL | <https://www.youtube.com/watch?v=-O_JX5mMMHY> |
| Source | `docs/reference/scripts/original/16-xv6-kernel-16-scheduling-plus-swtch.s--O_JX5mMMHY.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의 중 하나이고, 스케줄링은 제 생각에 커널에서 아마 가장 흥미로운 부분입니다. 이 비디오에서는 그 부분을 살펴보려고 합니다. 세
개의 함수를 볼 텐데, 이 함수 이름은 ‘스케드’라고 발음하겠습니다. 그래서 yield, sched, scheduler 세 함수를 보고, 몇 가지 헬퍼 함수들도 살펴보겠습니다.
cpuid, mycpu, myproc을 보고,

또 switch.S 파일에 들어 있는 어셈블리 코드를 훑어볼 겁니다. 먼저 비교적 직관적이고 빨리 정리할 수 있는 헬퍼 함수들부터 시작하겠습니다. cpuid 함수는 단순히 tp
레지스터 안의 값을 반환합니다. 이 레지스터에는 우리가 현재 어느 코어에서 실행 중인지 그 코어 번호가 항상 들어 있습니다. 인터럽트가 있을 수 있고,

언제든 상황이 바뀔 수 있기 때문에, 우리가 값을 반환하는 바로 그 순간 값이 이미 낡아 있지 않도록 하려면, cpuid는 반드시 인터럽트가 비활성화된 상태에서 호출되어야 합니다.
mycpu 함수는 현재 코어 번호 식별자를 얻은 다음, 그 번호를 사용해 배열 안에서 CPU 구조체를 찾아봅니다. 우리가 예전에 봤던 그림을 기억해 보면, 여덟 개 코어 각각에
대해 CPU struct가 하나씩 있었고,

여기서 우리가 하는 일은 이 특정 코어의 데이터를 표현하는 그 구조체에 대한 포인터를 반환하는 것입니다. myproc 함수는 그 구조체로 가서, 그러니까 mycpu를 호출해 그
구조체를 얻은 뒤, 그 안의 proc 필드로 가서 그걸 따라가 반환합니다. CPU 구조체 안에는 현재 실행 중인 프로세스에 대한 포인터가 들어 있고,

우리는 그 proc 구조체에 대한 포인터를 반환합니다. 만약 지금 어떤 프로세스도 실행 중이 아니라면 그 값은 널일 수 있습니다. 그래도 어쨌든 그 값을 돌려줍니다. mycpu는
반드시 인터럽트가 비활성화된 상태에서 호출돼야 하고, myproc은 여기저기서 호출됩니다. 그래서 한 가지 하는 일이, 여기서 push_off를 호출해 인터럽트를 비활성화하는
것입니다. 이 호출은 카운터도 증가시킵니다. 그리고 나서

pop_off를 호출해 인터럽트를 복구하는데, 이때 카운터를 감소시키고, 카운터가 0으로 돌아가면 인터럽트 상태를 이전 상태로 되돌립니다. 자, 이제 우리가 앞에서 봤던 로드맵을
다시 보겠습니다. 이 그림은 trap과 sret 명령 사이에 일어나는 모든 일을 보여줍니다. 유저 코드가 실행 중이고 trap이 발생하면, 무슨 일이 일어났는지 확인합니다. 그리고

타이머 인터럽트인 경우 yield 함수를 호출합니다. 그래서 이제 yield 함수에 집중하겠습니다. 전에 이 부분을 약간 암시만 했는데, 지금은 이 영역을 좀 더 자세히 보여줄
겁니다. yield가 반환한 뒤에는 다시 유저 모드 함수로 돌아가 계속 실행합니다. 한 가지 짚고 넘어갈 점은, trap이 발생하면 인터럽트가 비활성화된다는 점이고, 그 뒤 우리가

yield를 호출하는 이 경로를 따라 내려가는 동안 내내 인터럽트는 비활성화된 상태로 유지됩니다. 따라서 yield 함수가 실행되는 동안 인터럽트는 비활성화되어 있습니다. 이제 그
영역을 좀 더 자세히 들여다보면, trap이 발생하고 여러 작업을 한 뒤, 이것이 시스템 콜도 아니고, 별도의 인터럽트 처리가 필요한 디바이스 인터럽트도 아니라고 판단하면, 결국
yield 함수를 호출하게 됩니다. 그래서 여기서 일어나는 일이

이렇고, 결국에는 yield에서 돌아와 약간 다른 일들을 처리한 다음 sret을 실행해 유저 모드 코드로 되돌아갑니다. yield는 sched를 호출합니다. 여기 그림에서
yield가 sched를 호출한다는 걸 표시해 두었습니다. yield와 sched 안에서는 사실 그렇게 많은 일이 일어나지 않고, 몇 줄 정도의 문장만 실행됩니다.

그리고 나서 switch라는 함수를 호출합니다. 이쪽은 프로세서 스레드입니다. 우리는 프로세스 p에 대해 유저 모드에서 실행 중이고,

지금은 같은 프로세스 p에 대해 커널 모드에서 이 부분을 실행하는 중입니다. 그리고 이 시점에서 switch가 일어나면, 프로세스 p에서 스케줄러 스레드로 전환하고, 그 다음에는
스케줄러 스레드를 실행합니다. 스케줄러 스레드는 다른 일들을 좀 더 할 수도 있고, 다른 프로세스들에 대해 타임 슬라이스를 줄 수도 있지만, 결국에는 다시 돌아와서

여기서의 switch 호출이 반환되고, sched 함수 실행이 계속되고, 그 다음 yield 함수 실행이 이어집니다. 그리고 나서 유저 상태를 복구하고 반환합니다. 이제
switch를 호출하기 전에 yield와 sched 함수에서 무슨 일이 일어나는지 보겠습니다. 우리는 프로세스 p의 상태를 검사하고 수정할 겁니다. 우리가 실행 중일 때 이
프로세스의 상태는

running입니다. 즉 지금은 실행 중입니다. 그런데 이걸 runnable로 바꿀 겁니다. runnable은 더 이상 실행 중이 아니고, 다음 타임 슬라이스를 기다리는 상태를
뜻합니다. 그래서 프로세스 p 자체에 대한 락을 획득하고, 그 상태를 runnable로 바꿉니다. 그리고 CPU 구조체 안에 있는 interrupts_enabled 필드도 저장해
둡니다. 여기서 CPU 구조체를 잠깐 복습해 봅시다. 우리가 실행 중인 proc에 대한 포인터뿐만 아니라,

push_off와 pop_off에서 사용하는 두 개의 필드가 있습니다. noff는 카운터이고, interrupts_enabled는 이전 인터럽트 상태입니다. 그리고 레지스터를
저장할 context 영역도 있습니다. 우리는 이 interrupts_enabled를 저장해 두었다가 나중에 복원하고, p에 대한 락을 해제하고, 유저 모드 코드로 돌아갑니다.
그러니까 switch를 호출한 뒤에는 다른 일들이 일어나고,

결국 switch가 반환합니다. 제가 여기서 그걸 표시해 두었습니다. 그 다음 sked 함수의 실행을 마저 진행하고, yield 함수 실행도 마치고, 계속 이어지는 거죠. 이제
스케줄러 스레드 안에서는 무슨 일이 일어날까요? 스케줄러 스레드 실행을 재개하면, 먼저 CPU의 proc 필드를 null로 바꿉니다. 더 이상 이 특정 프로세스를 실행 중이 아니기
때문입니다. 그리고 그 락을 해제합니다.

그 다음 다른 일들을 좀 하다가, 언젠가는 다시 p를 실행하기로 결정할 겁니다. 그 사이에 다른 프로세스들에게도 타임 슬라이스를 줄 수 있지만, 결국에는 이 코어, 혹은 아마 다른
코어가 p에게 다시 타임 슬라이스를 주기로 결정할 겁니다. 그 시점에서 우리는 다시 p에 대한 락을 획득하고, 그 상태를 running으로 바꿉니다. 그리고

현재 CPU의 proc 필드를 설정합니다. 다시 말해, 우리가 실행 중일 수 있는 코어는 다른 코어일 수도 있다는 점을 기억해야 합니다. 현재 CPU struct의 proc
필드를, 이제 시작하려는 프로세스 p를 가리키도록 설정합니다. 그리고 switch를 사용해 다시 sched 함수와 그 다음 yield 함수 안으로 돌아갑니다. 이제 switch에서
흥미로운 일들이 벌어지므로, 다음으로 그 부분을 이야기하겠습니다.

CPU 구조체 안에는 레지스터 저장 영역이 있어서, ra, sp, 그리고 s 레지스터들을 저장할 수 있습니다. 그리고 proc 구조체 안에도 같은 레지스터들을 저장할 수 있는
레지스터 저장 영역이 있습니다. 프로세스 p에서 전환할 때는, 프로세스 p의 현재 레지스터들을 proc 구조체 안의 그 레지스터 저장 영역에 저장하고, CPU struct 안의
레지스터 저장 영역에 있는 레지스터들을

로드합니다. 그리고 스케줄러 스레드에서 다시 프로세스 p의 스레드로 돌아갈 때는, 해당 코어의 CPU struct 안에 스케줄러의 레지스터들을 저장하고, 프로세스 p의 레지스터
저장 영역에서 레지스터들을 로드합니다. 자, 이제 switch를 살펴봅시다. switch는 어셈블리 언어로 작성되어 있고, 여기 보면 인자를 두 개 넘겨받는데…

포인터, 두 포인터 모두 컨텍스트에 대한 포인터입니다. 그래서 본질적으로 여기 이 컨텍스트에 대한 포인터 하나, 저기 저 컨텍스트에 대한 포인터 하나를 전달하는 셈입니다.
RISC-V 시스템의 proc 구조체 안에서 함수 호출이 일어날 때마다, 함수 호출을 수행하는 명령어는 현재 프로그램 카운터를 반환 주소 레지스터인 ra 레지스터에 저장하고,

호출하고자 하는 함수의 첫 번째 명령어로 점프합니다. return 명령어(여기 하나 보이죠)는 ra 레지스터에 들어 있는 값을 pc로 옮겨서 호출한 쪽으로 되돌아가게 만듭니다.
만약 어떤 함수가 다른 어떤 함수도 호출하지 않는다면, 반환 주소를 그 레지스터 안에 그대로 두어도 되고 스택에 저장할 필요가 없습니다.

그리고 이 특정 함수에서는 스택에 아무것도 push 하거나 pop 하지 않는 것을 볼 수 있습니다. 그래서 여기서는 그런 일이 일어나고 있는 겁니다. RISC-V 호출 규약에서는,
어떤 함수든 a 레지스터와 t 레지스터를 사용하고 갱신하고 수정하고 덮어쓸 수 있다고 가정합니다. 따라서 switch를 호출하는 쪽은,

죄송합니다, s 레지스터가 아니라 a 레지스터와 t 레지스터의 값에 대해, switch가 돌아온 뒤에는 아무 가정도 해서는 안 됩니다. 그렇기 때문에 그 레지스터들은 저장하지
않습니다. 대신 무엇을 하느냐 하면, 다른 모든 레지스터는 저장합니다. ra와 스택 포인터를 저장하고, 모든 s 레지스터를 저장합니다. 어디에 저장하느냐 하면, 첫 번째 인자,
이전에 실행 중이던 스레드의 상태를 저장해 둘 옛 컨텍스트에 대한 포인터가 a0에 들어 있습니다.

다음에 실행할 스레드의 컨텍스트에 대한 포인터는 a1에 들어 있는 new 인자가 가리키는 컨텍스트입니다. 그래서 a0가 우리가 모든 것을 저장하는 위치입니다. 여기서 store
double word를 보고 있는데, ra, sp, s 레지스터들을 저장합니다. gp 레지스터는 xv6 kernel에서 사용하지 않으므로 신경 쓰지 않습니다. tp 레지스터는

현재 실행 중인 코어 번호를 담고 있고, 그 값은 변하지 않습니다. 이 저장 동작을 하는 동안 우리는 특정 코어에서 실행 중이고, 그 이후에도 여전히 그 코어에서 실행 중이기
때문에 tp 레지스터는 바꾸지 않습니다. 그러니까 이전에 실행 중이던 스레드의 상태를 저장한 다음에는, 새 컨텍스트에 대한 포인터를 사용해서 다음 스레드의 상태를 로드하고, 정확히
같은 레지스터들을 로드합니다.

여기에서는 우리를 호출한 함수의 반환 주소를 저장하고, 여기에서는 예전에 저장해 두었던 어떤 반환 주소를 다시 로드합니다. 그래서 이 return은 직접 원래 호출자에게 돌아가는
게 아니라, 다른 어떤 곳으로 돌아가게 됩니다. 그래서 약간 이상하게 느껴지죠. 아까 제가 보여준 그림에서 보면, 여기서 switch를 호출하고, 여기서 switch에서 리턴을
합니다.

하지만 여기서 실행 중인 이 switch 호출은 즉시 같은 스레드로 되돌아오지 않고, 다른 스레드로 돌아갑니다. 마찬가지로 이쪽의 호출은 이 스레드 쪽으로 돌아오는 식입니다. 자,
이게 switch이고, 이제 yield 함수로 가서 그 코드를 어떻게 동작하는지 따라가 보겠습니다. yield, 한 번의 스케줄링 라운드 동안 CPU를 양보합니다. 주된 동작은

sched를 호출하는 것입니다. 그런데 그 전에, 먼저 현재 우리가 실행 중인 프로세스에 대한 포인터를 얻습니다. 프로세스 p가 그 proc에 대한 포인터를 얻고, 그다음에
lock을 획득합니다. 기억해 두어야 할 것은 proc 구조체에 state 같은 것을 보호하는 lock 필드가 있다는 점입니다. 우리는 state를 RUNNING에서
RUNNABLE로 바꾸려고 하므로, 바로 그 일을 합니다. lock을 획득하고,

그다음 state를 RUNNABLE로 바꿉니다. 그 다음 sched를 호출하고, 우리는 사라집니다. 그리고 아마 꽤 오랫동안, 다른 프로세스들이 타임 슬라이스를 가져가는 동안
기다립니다. 궁극적으로 scheduler 함수가 다시 switch를 호출하고, 우리가 이 스레드로 돌아오게 됩니다. 그래서 어느 시점에서든 sched가 반환할 것이고, 그 시점에
여기서 획득했던 lock을 해제합니다. yield는 정확히 두 곳에서만 호출됩니다.

user trap과 kernel trap에서만 호출됩니다. 그래서 우리가 사용자 모드에서 실행 중일 때 트랩이 발생하면 user trap을 실행하고, 타이머 인터럽트가 있었다면
yield를 호출합니다. 마찬가지로 supervisor 모드에서 실행 중일 때는 kernel trap이라는 함수를 가지고 있고, 타이머가 울렸다면 yield를 호출합니다. 어쨌든,

항상 trap 핸들러 안에서 호출됩니다. 그 시점에는 인터럽트는 비활성화되어 있고, 어떤 lock도 보유되고 있지 않습니다. 여기에서 acquire를 하고 그 다음 release를
하는데, 기억하다시피 acquire는 spinlock이고, 인터럽트를 비활성화한 뒤에 이전에 인터럽트가 활성화되어 있었는지를 기억합니다. 지금은 활성화되어 있지 않으므로,

그 상태를 interrupts enabled previously 같은 필드에 저장합니다. 이 값은 항상 false일 겁니다. 그리고 어떤 lock도 잡고 있지 않았으므로, lock
카운터인 nlock 또는 noff 카운터를 증가시킵니다. 0에서 1로 증가시킬 겁니다. 그래서 이 시점에서 우리가 sched를 호출할 때, noff 필드는 1이고 인터럽트는
비활성화된 상태입니다. 자, 이제 sched 안에서 무슨 일이 일어나는지 보겠습니다. myproc 필드에 대한 포인터를 가져오고,

몇 가지 오류 검사를 합니다. 우리가 lock을 잡고 있는지 확인합니다. 맞죠, 여기에서 lock을 획득했습니다. sched는 다른 몇몇 곳에서도 호출될 수 있지만, 지금은
yield에서의 호출에만 집중합시다. lock을 잡고 있는지 확인하고, noff가 1인지 확인하고, state가 RUNNING이 아닌지 확인합니다.

그리고 인터럽트가 비활성화되어 있는지도 확인합니다. 만약 인터럽트가 활성화되어 있다면, 이 조건이 true가 되고 에러 메시지를 출력하게 됩니다. 이제 우리는 intena,
인터럽트 활성화 상태의 값을 저장하려고 합니다. yield에서 호출되는 이 특정 경우에는 항상 false일 겁니다. 그래서 그 상태를 저장합니다. 하지만 우리가 sched를 다른
곳에서 호출한다면,

그 값이 true일 수도 있습니다. 어쨌든 이전 intena(인터럽트 활성화 여부)의 값을 저장해서 지역 변수에 넣고, 나중에 switch 호출 이후에 그 값을 다시 복원합니다.
이 지역 변수가 레지스터에 저장되는지 스택에 저장되는지는, 컴파일러가 정확히 어떻게 할지 알 수 없지만 중요하지 않습니다. 중요한 점은,

이 스레드의 상태 전체가 완전히 저장된다는 것입니다. 이 지역 변수가 스택에 있건 레지스터에 있건 상관없이, 어쨌든 이 스레드가 가지고 있는 어떤 데이터에 저장되며, 나중에
복원됩니다. 그런 다음 그 값을 복원하고, return을 하고, lock을 해제합니다. 이제 scheduler 함수를 살펴보겠습니다.

어떻게 생겼는지 보면, 꽤 짧습니다. 여기서부터 여기까지고, 무한 루프를 담고 있습니다. 이 함수 전체에서 c는 이 특정 코어에 대한 CPU 구조체 포인터로 설정됩니다. 각 코어는
scheduler 함수용 단일 스레드를 실행합니다. 그리고 이 함수는 한 코어에서 실행되는 거죠. 물론 그것은…

동시에 다른 코어들 위에서도 실행될 수 있겠지만, 지금은 이 특정 코어 하나에만 집중해 봅시다. 그래서 이 코어를 설명하는 CPU struct에 대한 포인터를 하나 얻고, 그
다음에 여기에는 무한 루프가 있습니다. 그리고 그 안쪽에 또 다른 루프가 있죠. 우선 이 안쪽 루프부터 보겠습니다. 이게 뭘 하느냐 하면, proc 배열을 하나씩 차례대로 훑고
있습니다. 자, 여기 우리의 배열이 있고

프로세스가 64개 있는데, 이들을 하나씩 차례대로 보면서 각 프로세스의 proc 구조체를 살펴봅니다. 여기에서 보듯이, 각 proc에 대해 그것이 runnable 상태인지 아닌지를
물어보고, runnable이면 switch를 호출합니다. 그리고 그 프로세스를 위한 lock을 획득합니다. 왜냐하면 state 변수를 읽고 바꿀 것이기 때문에, 그 작업을 하는
동안에는 lock을 쥐고 있어야 하죠.

그래서 이 특정 프로세스에 대한 lock을 획득하고, 만약 runnable이면 이 코드를 실행합니다. runnable이 아니면 lock을 해제하고 다음 프로세스를 보러 넘어갑니다.
그리고 전부를 다 훑고 나면, 다시 바깥쪽 루프로 돌아가서 처음부터 다시 시작합니다. 만약 어떤 프로세스가 runnable이면, 이제 그 상태를 running으로 바꿀 겁니다.

즉, 그 프로세스를 선택한 것이고, 또 CPU struct 안에 이 프로세스를 가리키는 proc 포인터를 저장해서 지금 어떤 프로세스를 실행 중인지 알 수 있도록 합니다. 그리고
나서 switch를 호출하는데, 여기 switch 호출이 보입니다. 현재 코어의 context에 대한 포인터 하나와, 우리가 새로 실행할 프로세스의 context에 대한 포인터
하나를 넘깁니다. 그러니까

switch의 old 인자로는 스케줄러 스레드의 레지스터들을 저장할 메모리 영역에 대한 포인터를 넘기고, new 인자로는 해당 proc 구조체가 가진 context 영역에 대한
포인터를 넘깁니다. 그리고 그곳으로, 이제 막 실행을 시작하려는 프로세스의 레지스터들을 로드합니다. 이렇게 해서 switch를 호출하고, switch에서 돌아오고 나면 우리는 더
이상 그 프로세스를 실행하고 있지 않습니다. 그래서 마지막에

proc 필드를 null로 설정합니다. 이제 이게 어떻게 동작하는지 그림으로 다시 보여드리겠습니다. 이번에는 이렇게 보죠. 여기서는 사용자 입장에서 본 모습입니다. trap이
발생하고, switch로 들어가고, 스케줄러가 실행되면서 다른 프로세스들에게 시간을 조금씩(time slot, time slice) 나눠줍니다. 그리고 결국에는 다시 돌아와서
sret을 실행하고 사용자 코드로 돌아옵니다. 그런데 이제 같은 상황을

스케줄러의 관점에서 보겠습니다. 우리는 어떤 프로세스 p를 실행하기로 고르고, p에 대한 lock을 획득하고, 그 state를 running으로 바꿉니다. 동시에 proc 필드도
업데이트하고요. 그 다음에 그 프로세스로 스위칭합니다. 그러면 어느 시점에서든 그 프로세스는 CPU를 내놓게 됩니다. 최소한 타이머 인터럽트라도 언젠가는 일어나서 trap을 일으킬
테니까요. 혹은

다른 이유일 수도 있지만, 어쨌든 타이머 인터럽트가 trap을 유발하고, 우리가 yield로 되돌아오게 됩니다. 그리고 yield 안에서 switch를 다시 호출합니다. 다만
yield에서 switch를 호출할 때는 context 인자들을 반대 순서로 제공합니다. 자, 안으로 조금 더 들어가 보겠습니다. yield를 호출하고, sked를 호출하고,
sked에서 switch를 호출합니다. 이때 old는 프로세스의 context입니다. 그래서 여기에

프로세스 레지스터들을 저장합니다. 프로세스의 상태를 저장하고, 스케줄러 스레드의 레지스터들을 로드합니다. 즉, mycpu struct로 가서 그 안의 context를 사용합니다.
그래서 switch가 돌아오면, 우리가 p를 proc에 설정하고 switch를 호출했었고, 그 다음에 뭔가 일이 진행되다가 결국 타이머 인터럽트가 발생해서 yield가 실행되고
다시

switch를 호출해서 스케줄러 스레드로 돌아옵니다. 그러면 스케줄러 스레드 안에서 루프를 마저 수행하면서 proc 필드를 null로 설정하고, p에 대한 lock을 해제합니다.
여기서 정확히 그 동작을 볼 수 있습니다. switch를 호출하고, 돌아와서 proc을 null로 설정하고, 그 다음에 p의 lock을 해제합니다. 그리고 다시
반복(iterate)합니다. 한 가지 짚고 넘어가고 싶은 점은, 제가 아까 “p를 선택한다, 그리고 p에 대한

lock을 획득한다”고 말했는데, p를 선택하려면 runnable한 프로세스를 찾을 수 있어야 하고, state를 보려면 먼저 lock을 잡아야 합니다. 그래서 실제로는 조금 다른
순서로 합니다. 먼저 lock을 획득한 다음, state를 보고 runnable인지 확인해요. 이건 사소한 세부사항일 뿐이고, switch 함수가 호출되는 패턴은 정확히 두
군데뿐입니다. sked 함수에서 스케줄러 함수로 들어갈 때 한 번,

그리고 스케줄러 함수 안에서 어떤 프로세스로 돌아갈 때 한 번입니다. 즉, sked에서 switch를 호출할 때는 항상 “프로세스 → 스케줄러 스레드”로 전환하는 것이고, 스케줄러
스레드 안에서 switch가 호출될 때는 항상 “스케줄러 스레드 → 프로세스”로 전환하는 것입니다. sked 코드를 보면, 여기 switch가 호출되는 부분이 있고,

old context에 대한 포인터 하나와 new context에 대한 포인터 하나를 넘깁니다. 그러면 switch는, 지금 떠나가고 있는 프로세스의 컨텍스트에 현재 레지스터 값을
저장하고, CPU(스케줄러 스레드)에 해당하는 context 영역에서 레지스터들을 로드합니다. 이 방향이 바로 “프로세스에서 스케줄러 스레드로 가는” 방향입니다. 반대 방향,

즉 스케줄러 함수에서 호출될 때는 조금 전 도표에 나온 이 위치입니다. 여기 scheduler 함수가 있고, 여기서 switch를 호출합니다. 이때는 스케줄러 함수(스케줄러
스레드)에서 이제 실행하게 될 어떤 프로세스로 전환합니다. 따라서 현재 CPU, 현재 코어에 연관된 context에 스케줄러의 레지스터들을 저장하고,

다음에 실행할 프로세스의 context에서 레지스터들을 로드합니다. 또 하나 언급하고 싶은 점은, 항상 lock의 acquire와 release가 짝을 이룬다는 겁니다. 예를 들어
yield 함수에서 보면 acquire와 release 한 쌍이 있습니다. 여기 yield 함수가 있고, 여기에서 lock을 acquire하고, 여기에서 release합니다.
그리고 scheduler 함수에서도

비슷한 일이 일어납니다. 여기 scheduler 함수가 있고, 여기서 lock을 acquire하고 여기서 release합니다. 얼핏 보면 이 acquire와 이 release가
서로 짝처럼 보이지만, 사실은 완전히 그렇진 않습니다. 여기서는 yield 함수에서 lock을 acquire합니다. 이 lock은 spinlock이기 때문에 오랫동안 쥐고 있으면
안 됩니다. 실제로 조금 지나서 scheduler 함수에서

곧바로 release되는 것을 볼 수 있습니다. 그리고 여기서는 scheduler 함수에서 lock을 acquire하고, 몇 개 명령만 실행한 뒤에 yield 함수에서
release합니다. 그러니까, 분명히 짝이 맞긴 한데, 조금 특이한 방식으로 쌍을 이루고 있습니다. 예를 들어 어떤 코어에서 p에 대한 lock을 acquire하고, 곧바로
release할 수도 있지만, 나중에

어떤 다른 코어가 p를 선택해서 실행하기로 마음먹고 lock을 acquire한 뒤 그 프로세스를 실행하기 시작하고, 나중에 yield 함수가 그 lock을 release하게 될
수도 있습니다. 따라서 lock이 한 코어에서 acquire되고, 다른 코어에서 release될 수도 있는 것입니다. 또 하나 강조하고 싶은 점은, 우리가 switch 함수를
호출할 때마다, 그리고 프로세스에서

스케줄러 스레드에서는 항상 인터럽트가 꺼져 있고, 우리는 그 사실을 확실히 알고 있습니다. 왜냐하면 여기 sched 함수가 있는데, 여기서 switch를 호출하고, 그 바로 직전에
인터럽트가 꺼져 있는지 확인하기 때문입니다. 그래서 스케줄러 함수로 돌아올 때는 항상 인터럽트가 꺼져 있습니다. 스케줄러 함수를 보면, 우리가 지금 하는 일을 보면요,

여기서 switch를 호출하고, 어느 시점에 다시 돌아옵니다. 즉, 어떤 프로세스의 타임 슬라이스를 실행한 뒤 switch에서 리턴해 돌아오는 거죠. 그 시점, 바로 여기에서
인터럽트는 꺼져 있을 것이고, 우리는 lock을 해제합니다. 하지만 인터럽트는 그대로 꺼진 상태일 수 있고, 우리는 다시 루프를 돕니다. 이제 다른 프로세스로 들어간다고 해
봅시다. 다른 runnable 프로세스를 하나 선택해서

그걸로 switch해서 들어가면, switch를 실행하고 나서 다시 여기로 돌아오게 됩니다. 이 yield는 user trap 함수의 일부로서 실행되고 있었고, trap이 발생하는
시점에 인터럽트는 꺼져 있으며, 그 상태는 yield 함수가 실행되는 동안 계속 유지됩니다. 그래서 여기 acquire는 이전 인터럽트 상태를 기억해 두고, release는 이
시점에서 인터럽트를 다시 켜지 않습니다.

하지만 우리는 사용자 레지스터 상태 등을 복원하고, 그다음에 sret 명령을 실행합니다. 그 순간 인터럽트가 켜지게 되고, 그래서 다음 프로세스로 들어갈 때 인터럽트가 켜지게
됩니다. 그런데 다음 프로세스가 없을 수도 있습니다. 즉, 프로세스 배열에 있는 모든 프로세스가 runnable이 아닐 수도 있다는 뜻입니다. 그래서 runnable한 프로세스를

하나도 찾지 못할 수도 있습니다. 그러면 어떻게 할까요? 배열 전체를 다 훑었는데 runnable한 것을 하나도 못 찾으면, 이 for 루프를 빠져나와서, 다시 이 while
루프를 반복하러 돌아옵니다. 그리고 여기서 우리가 인터럽트를 켠다는 것을 볼 수 있습니다. 그래서 runnable 프로세스가 전혀 없는 상황에 들어갈 수도 있습니다. 그럴 수도
있는 상황은,

모든 프로세스가 sleep 상태이거나, 뭔가를 기다리고 있는 상태일 때입니다. 예를 들면 사용자 입력을 기다리고 있을 수 있고, 그래서 runnable 프로세스가 하나도 없을 수
있습니다. 그러면 이런 상황이 생길 수 있습니다. 만약 switch에서 빠져나올 때 인터럽트가 꺼져 있고, 여기에서 다시 켜주지 않는다면,

우리는 runnable 프로세스를 전혀 찾지 못하고 있는 동안 인터럽트가 계속 꺼진 채로 남게 됩니다. 지금 여기서 그런 상황을 다루고 있는 겁니다. 이 주석을 보면 “디바이스가
인터럽트를 걸 수 있도록 해서 데드락을 피하라”라고 되어 있죠. 그래서 배열 전체를 한 번 다 훑고 나면 인터럽트를 켜 줍니다. 매번 배열 전체를 한 번씩 훑을 때마다, 최소한
아주 짧은 시간이라도 인터럽트를 켭니다. 하지만 그 짧은 시간만으로도 인터럽트가 발생하고 trap이

처리되기에 충분하고, 그러면 다른 프로세스들이 깨어날 수도 있습니다. 바로 그런 일이 여기에서 일어나고 있는 겁니다. 자, 이 프로세스 스위칭에 관한 부분은, 제가 보기엔 커널에서
가장 흥미로운 부분입니다. 그리고 여러분도 제가 이 비디오를 만들면서 느낀 즐거움만큼 즐기셨기를 바랍니다. 저는 앞으로도 계속 비디오를 만들 계획이지만, 약간의 지연이 있을 수도
있습니다.

다음 비디오에서 다시 뵙겠습니다.
