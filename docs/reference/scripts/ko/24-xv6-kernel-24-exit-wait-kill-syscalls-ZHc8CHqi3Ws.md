# xv6 Kernel-24: Exit Wait Kill SysCalls

| Field | Value |
| --- | --- |
| Video ID | `ZHc8CHqi3Ws` |
| URL | <https://www.youtube.com/watch?v=ZHc8CHqi3Ws> |
| Source | `docs/reference/scripts/original/24-xv6-kernel-24-exit-wait-kill-syscalls-ZHc8CHqi3Ws.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의 중 하나입니다. 이 비디오에서는 exit, wait, kill 시스템 콜에 대해 이야기하겠습니다. 또한 reparent 함수에
대해 설명하고, 이 시스템 콜들과 관련된 sleep과 wakeup을 다시 살펴보겠습니다. 우선 exit 시스템 콜을 복습하면서 시작해 봅시다. 기억하시겠지만, exit는 리턴하지
않습니다. exit는 프로세스를 종료하는 데 사용됩니다.

그리고 우리는 exit에 종료 상태(exit status)를 값으로 넘겨주고, 그 값은 다른 어떤 프로세스에게 반환됩니다. Unix와 Linux에서 종료 상태는 8비트 양의
숫자이고, xv6에서는 32비트 정수입니다만, 그건 크게 중요하지는 않습니다. Unix와 Linux에서는 상위 비트들이 다른 프로세스에 추가 정보를 전달하는 데 사용됩니다. 예를
들어, 어떤 시그널 때문에 프로세스가 종료되었는지 등의 정보를 전달하는 데 쓰입니다.

하지만 xv6에는 시그널이 없기 때문에 그냥 32비트를 전부 우리가 쓸 수 있습니다. 그러나 Unix, Linux, xv6 모두에서 0은 프로세스가 성공적으로 종료되었다는 것을
나타내는 데 사용됩니다. 여기서 “성공”이 무엇을 의미하는지는 그 프로세스마다 다를 수 있겠죠. 그 외의 양의 값은 프로세스에 오류가 있었다는 것을 나타내는 데 사용됩니다.
그렇다면 exit를 호출하면 무슨 일이 벌어질까요? 만약 어떤 부모 프로세스가 자식이 종료되기를 기다리고 있다면,

그 부모가 먼저 깨워집니다. 그리고 나서 종료 상태가 여기(자식)에서 부모 쪽 코드로 어떤 방식으로든 복사됩니다. 그러면 부모가 그 이후에 해당 프로세스를 정리(clean
up)하는 일을 책임지게 됩니다. 프로세스가 종료된 뒤에는 더 이상 명령어를 실행하지 않습니다. 우리는 그 프로세스의 여러 데이터 구조들을 해제해야 하고, 특히 그 프로세스의
proc 구조체를 다시 미사용 상태로 돌려놓아야 합니다. 그런데 이 작업을

자식 프로세스 자신이 할 수는 없습니다. 이미 더 이상 명령어를 실행하지 않기 때문입니다. 따라서 그 정리 작업을 수행하는 책임은 부모에게 있습니다. 하지만 만약 그 시점에
기다리고 있는 부모가 없다면, 종료 중인 프로세스는 그대로 멈춰서 기다려야 합니다. 이 시점에서 이 프로세스는 우리가 좀비(zombie)라고 부르는 상태가 됩니다. 다시 말해,
살아 있는 건 아니고, 더 이상 명령어를 실행하지도 않지만, 아직 완전히 죽은 것도 아닙니다.

이 프로세스의 데이터 구조들은 여전히 남아 있습니다. 왜냐하면 종료 상태를 저장해 둘 공간이 필요하기 때문입니다. 언젠가 미래의 어느 시점에 부모가 마침내 wait 시스템 콜을
호출하게 되고, 그때 비로소 종료 작업이 완전히 끝날 수 있습니다. 그런데 어떤 문제가 있을 수 있냐 하면, 어떤 프로세스가 자신의 자식들을 하나도 기다리지 않고(exit만 하고)
종료해 버리는 경우입니다. 그때 무슨 일이 벌어질까요? 프로세스 A가 종료할 때,

A의 자식 프로세스들은 모두 다른 곳으로 옮겨집니다. 우리는 이 과정을 다시 부모를 정한다, 즉 re-parented 된다고 말합니다. 이 자식 프로세스들은 init 프로세스로
옮겨집니다. 즉, 더 이상 종료 중인 그 프로세스의 자식이 아니고, 대신 init 프로세스의 자식이 됩니다. 그럼 init은 무엇을 할까요? 약간의 초기화 작업을 한 다음,
자식들이 종료되기를 기다리면서 무한 루프에 들어갑니다. 그래서 여기 이… 음, 입양된 자식들이라고 부를 수 있겠죠,

이 입양된 자식들 중 하나가 exit 시스템 콜을 호출할 때마다, init은 그 종료 상태를 수거하게 됩니다. 그러면 그 좀비들이 완전히 죽을 수 있게 되고, 어떤 프로세스가 죽은
프로세스를 정리하는 일을 수행할 수 있게 됩니다. 이제 wait 시스템 콜을 살펴보겠습니다. wait는 xv6뿐 아니라 모든 Unix와 Linux 시스템에서도 사용할 수 있는
시스템 콜입니다. wait는 어떤 메모리 위치의 주소를 인자로 받고,

종료된 어떤 자식 프로세스의 프로세스 ID를 반환합니다. 만약 부모에게 자식이 하나도 없다면 -1을 반환합니다. 하지만 자식이 하나라도 있다고 가정하면, 필요하다면 그 중 하나가
종료될 때까지 기다리게 됩니다. 그리고 그 자식으로부터 종료 상태를 가져와서 저장합니다. 따라서 이 인자는 종료 상태를 저장할 위치를 가리키는 포인터입니다.

Unix와 Linux 시스템에는 xv6에는 없는 추가 시스템 콜인 waitpid가 있습니다. 이것은 더 많은 제어 기능을 제공합니다. 예를 들어, 특정 프로세스가 종료되기를 기다릴
수 있고, 또 잠들지 않고(busy wait하지 않고) 넘어가는 것도 가능합니다. 어떤 파라미터를 통해, 좀비 자식이 없다면 즉시 반환하도록 할 수 있습니다. 자, 그럼 wait는
어떻게 동작할까요? 무엇을 할까요?

먼저 이미 종료된 자식, 즉 좀비 자식을 찾습니다. 좀비 자식을 발견하면, 그 자식으로부터 종료 상태를 가져오고, 그 자식을 정리(clean up)합니다. 구체적으로는 해당 자식의
proc 구조체를 제거하고, freeproc을 호출해서 그 proc을 다시 미사용 proc으로 바꿔서 재사용할 수 있게 만듭니다. 그리고 물론 종료된 그 자식의 프로세스 ID를
반환합니다.

하지만 좀비 자식이 하나도 없다면, wait 시스템 콜은 잠들어야(sleep) 합니다. 그래서 sleep 함수를 호출합니다. 그리고 어떤 자식이 마침내 종료하면, 그 자식의
exit 시스템 콜 내부에서 wakeup 함수가 호출됩니다. 이 wakeup이 부모를 깨우고, 부모는 다시 루프의 처음으로 돌아가서 좀비 자식을 찾게 됩니다.

만약 부모가 다른 이유로 깨어난 것이라면, 좀비 자식이 없을 것이고, 부모는 다시 잠들게 될 것입니다. sleep과 wakeup 함수는 채널(channel)을 통해 서로 통신한다는
점을 기억하십시오. 이 채널은 해석되지 않는 값으로, sleep과 wakeup 자신은 그 값을 해석하지 않습니다. 하지만 어떤 프로세스들을 깨워야 하는지를 조정하는 데 사용됩니다.
그러면 wakeup 호출이 어떤 프로세스들을 깨워야 하는지는

wakeup 호출에 전달되는 채널이 무엇인지에 의해 결정됩니다. 이 용도에서는 부모의 proc 구조체의 주소를 코드(또는 번호)처럼 사용해서 sleep에 전달합니다. 따라서 이
wakeup은 그 부모 프로세스만 깨우게 되고, 다른 어떤 프로세스도 깨우지 않습니다. 이제 이것이 코드에서 어떻게 보이는지 살펴봅시다. exit 시스템 콜이 호출될 때마다,

커널은 sysexit 함수로 진입하는데, 이 함수가 실제 작업을 하는 exit를 호출합니다. 이 exit 함수는 절대 리턴하지 않으므로, 우리는 이 지점으로 다시 돌아오지
않습니다. 인자는 하나뿐이므로, 우리는 argint를 호출해서 사용자 레지스터 a0에 있는 값을 가져와 로컬 변수 n에 저장합니다. 그 값이 exit 함수에 전달됩니다. 여기
exit 함수가 있습니다.

exit, kill, wait, reparent 함수는 모두 proc.c 파일 안에 있습니다. exit는 status 값을 인자로 받습니다. 무엇을 할까요? 전역 변수
initproc은 초기 프로세스의 proc 구조체를 가리킵니다. 여기서는 그 초기 프로세스가 exit를 시도하지 않는지 확인하고 있습니다. 아직 파일 시스템에 대해서는 이야기하지
않았지만, 여기에서 우리는 이 프로세스가 열고 있는 파일들을 닫고 있다는 것을 볼 수 있습니다. 열린 파일 배열을 순회하면서,

열려 있는 각 파일에 대해 그것을 닫고, 엔트리를 비우고 있습니다. 또 여기 코드로 현재 작업 디렉터리도 정리하고 있습니다. 이제 이 함수의 핵심 부분을 보겠습니다. 나머지
코드에서는 if 문이나 루프가 없다는 점을 알 수 있습니다. 곧바로 순차적으로 실행되며, 그중에서 우리가 볼 수 있는 것 중 하나는 프로세스의 상태를 좀비 상태로 바꾼다는
것입니다. 따라서 종료하는 모든 프로세스는 좀비가 되고,

부모가 결국 그 프로세스를 정리하고, proc 구조체를 미사용 상태로 되돌려 재활용할 수 있게 됩니다. 또한 이 status, 즉 여기 인자로 받은 값을 proc 구조체의
xstate라는 필드에 저장합니다.

그리고 우리가 이 필드들에 접근할 때마다 항상 그 proc 구조체에 대한 락을 쥐고 있어야 합니다. 그래서 그 지점 전에 락을 잡아 둔 거죠. 그 전에 보면 reparent를
호출하고 있습니다. 이 프로세스에 자식이 있다면, 그 자식들은 옮겨져야 합니다. 말하자면 고아가 되어야 하고, 초기 프로세스가 입양해야 하죠. 그 작업이 reparent 함수에서
일어납니다. 그리고 부모 프로세스가 우리를

기다리고 있을 수도 있습니다. 어쩌면 부모가 sleep 중일 수도 있고, 아닐 수도 있지만, 만약 sleep 중인 부모가 있다면 깨워야 합니다. 그래서 여기에서 wakeup을
호출하고, 채널로는 부모의 proc 구조체에 대한 포인터를 넘깁니다. parent 필드에 접근하기 전에 weightlock이라는 락을 쥐고 있어야 하므로 여기서
weightlock을 잡고 있습니다. reparent 함수도

parent 필드에 접근할 것이고, reparent 함수는 호출 시 이미 weightlock을 잡고 있어야 한다는 전제를 가집니다. 그래서 여기에서 weightlock을 잡은 다음
reparent를 호출하고 부모 프로세스를 깨웁니다. 이 시점에서 부모 프로세스가 깨어날 수도 있는데, 부모가 제일 먼저 하는 일은 곧 보겠지만 weightlock을 획득하려고
시도하는 것입니다. 그래서

우리가 weightlock을 풀기 전까지 부모는 실제로 아무 진전도 못 합니다. 우리는 좀비가 된 이후에야 weightlock을 풀기 때문에, 부모 프로세스가 깨어났을 때는 우리를
좀비로 발견하게 됩니다. 이 프로세스의 상태가 ZOMBIE이고, status도 이미 설정된 상태인 거죠. 그리고 마지막으로 sched로 점프합니다. sched는 스케줄러이고, 그
전에 우리의

상태를 ZOMBIE로 먼저 설정합니다. 스케줄러는 RUNNABLE 상태가 아닌 것은 절대 스케줄하지 않으므로, 이 프로세스는 다시는 스케줄되지 않습니다. 따라서 sched 안의 이
마지막 명령은 우리가 다시는 실행하지 않습니다. sched 안에서는 이 프로세스에 대한 락을 잡고 있고 다른 어떤 락도 잡고 있지 않아야 한다는 요구사항이 있습니다. 그
acquire가 바로 여기에서 이루어지고, sched의 요구사항을 만족시킵니다. 자, 다음으로

reparent 함수를 보겠습니다. 호출자는 weightlock을 잡고 있어야 하고, 이 함수가 하는 일은 종료 중인 프로세스에 대한 포인터를 하나 전달받는 것입니다. 그걸 부모
프로세스라고 부르겠습니다. 그리고 proc 배열을 훑으면서 자식을 찾습니다. 기본적으로 가능한 각 자식 후보에 대해 그 parent 포인터가 p를 가리키는지 확인하고, 그렇다면
자식을 찾은 것입니다.

그러면 그 자식의 parent 포인터를 초기 프로세스의 proc 구조체를 가리키도록 수정합니다. 여기서 재부모화가 일어나는 거죠. 그리고 새 자식을 initproc, 즉 초기
프로세스에 추가했으니, 그 프로세스가 sleep 중일 수도 있으므로 깨워야 합니다. init 프로세스는 자식들이 종료되길 기다렸다가 없애는 tight loop 안에 있을 것이기
때문입니다. 그래서 혹시 sleep 중일지 모르니

init 프로세스에 대해 wakeup 호출을 합니다. reparent가 하는 일은 이것이 전부입니다. 다음으로 wait 시스템 콜을 보겠습니다. 커널은 sys_wait을 호출하고,
그것이 wait를 호출해서 실제 일을 합니다. wait 시스템 콜은 인자를 하나만 가지고 있으므로, 여기서 argaddr를 호출해 사용자 레지스터 a0의 값을 가져와 로컬 변수에
저장합니다.

바로 위의 sys_exit에서는 여기에서 argint를 호출했었죠. 차이점은 argint는 32비트를 가져오고, argaddr는 64비트 값을 가져온다는 점입니다. 그리고 그
64비트 값은 우리가 종료 status를 저장할 가상 주소입니다. wait는 종료한 자식의 프로세스 id를 반환하고, 그 값을 다시 사용자 모드 코드에 돌려줍니다. 여기 wait
코드가 있고,

전체 구조부터 보겠습니다. 종료 status를 저장할 가상 주소가 인자로 전달됩니다. 여기 무한 루프가 있습니다. 무한 루프이고, 먼저 로컬 변수 havekids를 0으로
초기화합니다. 그리고 모든 자식들을 훑어보면서 좀비인 것을 찾습니다. 좀비를 찾으면 이런 일들을 합니다.

여기에서 모든 자식을 훑고, 좀비 자식을 하나도 찾지 못했다면 자식이 전혀 없는지 확인합니다. 자식을 하나라도 찾으면 여기에서 havekids를 1로 설정하지만, 아래로 내려왔을
때 자식이 하나도 없다고 판정되면 즉시 -1을 반환합니다. 이 함수는 시작할 때 weightlock을 획득합니다. parent 포인터들을 살펴볼 것이기 때문입니다.

그리고 -1을 반환하기 전에 weightlock을 해제합니다. 또 어떤 다른 악의적인 프로세스가 우리의 killed 플래그를 설정했다면, 이 프로세스는 자신도 종료해야 합니다.
그래서 killed가 설정된 경우 역시 락을 풀고 -1을 반환합니다. 이 커널 코드를 처음 호출한 것은 usertrap에서이고,

sys_wait을 거쳐 syscall을 거쳐 usertrap으로 되돌아가 사용자 모드 코드로 복귀하기 전에 killed 플래그를 다시 확인합니다. 만약 설정되어 있다면 그 시점에
exit를 호출하므로, wait 시스템 콜을 호출한 프로세스에게 실제로 -1이 반환되는 일은 없습니다. wait 시스템 콜은 아예 반환하지 않게 됩니다. 자, 그래서 좀비를 찾지
못했지만

자식은 있는 경우에는 sleep에 들어갑니다. 여기서 sleep을 하고 있고, 채널로는 바로 이 프로세스의 proc 구조체 주소를 사용합니다. 그 주소는 함수 맨 처음에 우리
자신의 proc 구조체 주소를 가져와 저장해 둔 것입니다. 그리고 그 주소를 채널로 사용해 sleep에 들어갑니다. 어떤 자식이든 exit를 호출하면, 부모의 주소, 즉 같은
주소로 wakeup을 호출해

우리를 깨웁니다. 그러면 다시 바깥쪽 무한 루프, 즉 for 루프를 반복하면서 좀비 자식을 다시 찾습니다. 두 번째 인자로 weightlock을 넘기고 있는데, sleep과
wakeup은 넘겨진 락이 잡혀 있어야 하며,

그 락을 풀고 프로세스를 sleep 상태로 만드는 것이 하나의 원자적 연산으로 이루어지도록 동작한다는 점을 기억하십시오. 그래서 어떤 시그널도 놓치지 않습니다. 우리가 완전히
sleep 상태가 될 때까지 weightlock은 유지됩니다. 이제 어떤 자식이 종료해서 이 프로세스가 sleep에서 깨어났다고 가정해 봅시다. 바깥 루프가 다시 반복됩니다. 그때
무슨 일이 일어날까요? 다시 havekids를 0으로 초기화하고,

proc 배열을 훑으면서 자식을 찾습니다. proc 배열을 훑으면서 각 엔트리를 np라고 부르고, 그게 우리의 자식인지, 즉 parent 포인터가 이 프로세스를 가리키는지
확인합니다. 그렇다면 자식을 찾은 것이고, havekids를 1로 설정한 뒤 그 자식이 좀비인지 확인합니다. state 필드에 접근할 때는 항상 락을 잡아야 하므로,

np에 대한 락을 잡은 다음 state 필드를 확인합니다. 좀비를 찾았다면, 이제 이 좀비를 처리할 것입니다. 먼저 그 프로세스 id를 가져오고, 이 값이 아래에서 반환될
값입니다. 그리고 여기에서 그 프로세스의 종료 status를 읽어옵니다. addr가 null이라면 copy를 건너뛸 수 있으므로, 먼저 그것을 확인하고, 만약 addr가…

실제로는 종료 상태(exit state)를 저장하기 위한 가상 주소가 이미 인자로 제공되었기 때문에, 그 경우에는 copyout을 수행합니다. 여기서는 copyout 함수(커널
메모리에서 어떤 프로세스의 가상 주소로 데이터를 복사하는 함수)를 사용하고 있고, 그 “어떤 프로세스”는 부모 프로세스입니다. 우리가 복사해 넣는 대상은 이 변수 addr에 들어
있는 가상 주소입니다. 그리고 그 과정에서 어떤 문제가 생기면,

자식에 대한 락과 wait 락, 이렇게 두 개의 락을 해제하고 -1을 리턴합니다. 하지만 copyout이 제대로 동작했다고 가정하면, 이제 자식 프로세스를 정리(clean
up)합니다. 좀비 상태였던 자식 프로세스는 이제 말 그대로 “안정하게 묻힐” 준비가 된 셈입니다. 마지막으로 해야 할 일, 즉 자식과 관련된 데이터 구조를 해제하는 일을
처리합니다. 그것을 위해 freeproc을 호출하고,

그 다음 자식에 잡고 있던 락을 해제합니다. 이 시점에서 자식의 proc 구조체는 더 이상 사용되지 않고, 재활용될 수 있는 상태가 됩니다. 그리고 아직 잡고 있던 wait 락도
해제하고, 리턴합니다. 이제 kill 시스템 콜을 살펴보겠습니다. 먼저 syscall 처리 코드에서 sys_kill 함수로 들어가고, 실제 일을 하는 kill 함수를 호출합니다.
kill은 인자를 하나 받는데,

종료시키고자 하는 대상 프로세스의 프로세스 ID입니다. arg를 호출해서 사용자 레지스터 a0의 값을 가져와 로컬 변수에 저장하고, 그것을 kill에 넘깁니다. kill은 모든
것이 정상적으로 처리되면 0을, 존재하지 않는 프로세스 ID가 넘어왔으면 -1을 리턴합니다. 자, 이게 kill의 코드입니다. 꽤 단순한데,

단 하나의 루프가 있고, 이 루프가 원하는 프로세스 ID를 찾기 위해 전체를 탐색합니다. 이것을 조금 더 자세히 보겠습니다. proc 배열을 하나씩 훑어가면서, 각 proc p,
각 프로세스 p에 대해 그 pid 필드를 확인해서 우리가 찾는 pid인지 검사합니다. 만약 찾는 pid라면, 그 프로세스의 killed 필드를 true로 설정합니다. 그리고
state 필드도 살펴봅니다.

이 pid, killed, state 이 세 필드는 모두 락으로 보호되는 proc 구조체의 필드들입니다. 따라서 이 필드들을 읽거나 수정하려면 먼저 락을 획득해야 합니다. 그래서
여기서 이 proc 구조체에 대한 락을 획득하고, if 조건이 성립해서 우리가 찾던 프로세스라면, 그 안에서 필요한 일을 하고 락을 해제한 다음, 모든 것이 정상이라면 0을
리턴합니다.

만약 현재 보고 있는 항목이 우리가 찾는 프로세스가 아니라면, 락을 해제하고 루프를 계속 반복합니다. 전체 루프를 끝까지 돌았는데도 매칭되는 프로세스를 찾지 못하면 -1을
리턴합니다. 여기에서 killed 플래그를 true로 설정한 다음, 이 프로세스의 state를 검사합니다. 만약 이 프로세스가 sleeping 상태라면, 즉 running도
runnable도 아니라면,

어떤 것을 기다리면서 잠들어 있는 상태입니다. 무엇을 기다리는지 정확히는 모를 수도 있고, 사실 중요하지도 않습니다. 우리가 해야 할 일은 그 프로세스를 깨우는 것뿐입니다.
state를 runnable로 바꾸는 방식으로 깨울 수 있습니다. 프로세스가 sleeping 상태라면, 그건 해당 프로세스의 커널 스레드가 커널 안에서 잠들어 있었다는 의미입니다.
그래서 어떤 시스템 콜 도중이었는지, 또는 무엇 때문에 잠들어 있었는지와 상관없이,

그 프로세스는 다시 깨어나 해당 시스템 콜을 마무리하게 됩니다. 아마도 갑자기 깨웠기 때문에 에러를 리턴하는 등의 결과가 나올 수도 있지만, 어쨌든 어떤 프로세스든 어떤 시스템
콜이든, 사용자 모드로 돌아가기 전에 항상 killed 플래그를 검사합니다. 따라서 이 프로세스가 무엇을 하고 있었든, 어떤 시스템 콜 도중이었든,

그 시스템 콜은 마치긴 하겠지만, 그리고 그 결과가 꼭 “옳은” 것은 아닐지도 모르지만, 어쨌든 사용자 모드로 돌아가기 전에 killed 플래그가 설정되어 있다는 것을 보게 되고,
그러면 exit를 호출합니다. 따라서 이 프로세스는 다시는 사용자 모드로 돌아가지 않습니다. 여기에서는 sleeping 상태에서 그 프로세스를 깨우고 있습니다. 그렇지 않으면, 그
프로세스는 조건이 다시는 만족되지 않는다면 영원히 잠든 채로 남을 수도 있습니다. 우리는 그것을 즉시 종료시키고 싶기 때문에,

여기서 그런 처리를 하는 것입니다. 자, 이 시스템 콜들에 대한 내용은 여기까지입니다. 다음 비디오에서 다시 만나겠습니다.
