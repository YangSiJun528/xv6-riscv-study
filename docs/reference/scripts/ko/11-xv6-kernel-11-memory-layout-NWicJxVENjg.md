# xv6 Kernel-11: Memory Layout

| Field | Value |
| --- | --- |
| Video ID | `NWicJxVENjg` |
| URL | <https://www.youtube.com/watch?v=NWicJxVENjg> |
| Source | `docs/reference/scripts/original/11-xv6-kernel-11-memory-layout-NWicJxVENjg.md` |
| Status | Korean translation generated from local `original/`; review recommended |

---

## 한국어 번역

이 비디오는 xv6 운영체제 커널에 대한 연속 강의 중 하나입니다. 이 비디오에서는 메모리가 어떻게 배치되어 있는지를 다루고, 파일 memlayout.h를 함께 살펴볼 것입니다.
물리 메모리의 전체적인 구성, 커널의 가상 주소 공간과 커널의 페이지 테이블에 대해 이야기하고, 마지막으로 trampoline과 trapframe이 왜 필요한지 설명하고 동기를
부여하겠습니다.

먼저 사용자 스레드 그림부터 시작해 봅시다. 사용자 스레드가 어떤 시점에 명령어를 실행하고 있다가, 어느 순간 trap이 발생해서 커널에서 명령어를 실행하기 시작합니다. 그리고
다른 어느 시점에서 커널 코드는 sret, 즉 system return 명령어를 실행해서 다시 사용자 코드로 돌아갑니다. 사용자 코드는 사용자 모드에서 실행되고, 커널 코드는

supervisor 모드에서 실행됩니다. RISC-V에서는 이것을 supervisor 모드라고 부릅니다. 제가 때때로 kernel 모드라고 부르기도 하는데 같은 뜻입니다. 이제
trap이 발생하는 순간과 sret 명령어가 실행되는 순간에 무슨 일이 일어나는지 좀 더 자세히 보겠습니다. 여기에서 우리는 사용자 모드에서 실행 중이고 trap이 발생합니다.
그리고 컨텍스트 스위치가 일어나는데, 이때 커널 코드를 실행하게 되고, RISC-V가

supervisor 모드 혹은 kernel 모드라고 부르는 모드에서 실행하게 됩니다. 그리고 어떤 나중 시점에 sysret 명령어를 실행해서 사용자 모드의 사용자 코드, 즉
사용자의 가상 주소 공간으로 돌아가게 됩니다. 여기 있는 것이 trap 명령이고, 이것은 하드웨어에서 일어나는 일들을 나타냅니다. 그리고 아래쪽에는 sret 명령이 있습니다.
trap은 하드웨어에 의해 처리되고, 무엇을 하느냐 하면, 코어는 모드를

커널 모드, 또는 말하자면 supervisor 모드로 전환합니다. 인터럽트를 비활성화하고, 프로그램 카운터를 sepc라고 불리는 하나의 제어/상태 레지스터에 저장합니다. 그리고 또
다른 CSR인 stvec에 들어 있는 값으로 프로그램 카운터를 로드합니다. 이 레지스터에는 uservec 루틴의 첫 번째 명령어의 주소가 들어 있고, 결과적으로 이것은
uservec 루틴으로의 점프가 됩니다.

uservec 루틴은 어셈블리 언어로 작성되어 있고, 시작하자마자 사용자 레지스터들을 저장합니다. 범용 레지스터들과 프로그램 카운터는 사용자 프로그램에게 매우 중요하고, 나중에
복원할 수 있도록 즉시 저장되어야 합니다. 범용 레지스터를 사용하지 않고는 아무것도 할 수 없기 때문에, 이것은 거의 첫 번째로

해야 할 일입니다. 사용자 레지스터를 저장한 다음, 몇 개의 중요한 커널 레지스터를 로드해야 합니다. 커널 스택 포인터 레지스터를 로드해야 하고, 코어 번호가 들어 있는 tp
레지스터도 로드해야 합니다. 마지막으로 커널의 주소 공간으로 전환할 필요가 있습니다. satp라고 불리는 또 다른 제어/상태 레지스터가 있는데, 여기에

커널 페이지 테이블의 주소를 로드해서, 모든 실행을 다른 가상 주소 공간으로 전환합니다. 그리고 마지막으로 C로 작성된 usertrap 함수로 점프합니다. 나중에 사용자 코드로
돌아갈 준비를 할 때에는 usertrapret이라고 하는 함수를 호출합니다. 아, 정확히는 루틴이죠. 이 usertrapret 함수는 userret이라는 루틴을 호출하는데,
userret은 어셈블리 언어로 작성되어 있습니다.

userret은 satp 레지스터를 복원해서 다시 사용자의 가상 주소 공간으로 전환합니다. 그리고 sret 명령을 실행하기 직전에 사용자 레지스터들을 복원합니다. 마지막으로
sret 명령어는 모드를 사용자 모드로 전환하고, sepc 레지스터에 들어 있는 값으로부터 프로그램 카운터를 복원하며, 인터럽트를 다시 활성화합니다. 그리고

그 이후로는 사용자 모드에서 실행을 계속합니다. 방금 제가 이것들을 함수라고 부르긴 했지만, 엄밀히 말하면 그렇지 않습니다. 함수는 호출되었다가 다시 반환되지만, 여기의 것들은
사실상 반환이 없습니다. uservec은 마지막에 usertrap으로 점프하면서 끝납니다. usertrap은 C 함수로 작성되어 있지만, 결코 자기 자신으로 되돌아오지 않습니다.
이것저것을 호출하다가 결국 usertrapret 함수를

호출합니다. 그리고 usertrap은 userret을 호출하고, userret은 결코 usertrap으로 반환하지 않습니다. 그런 의미에서 이것들은 진짜 함수라기보다는 그냥 코드
블록에 가깝습니다. 마찬가지로 userret도 일반적인 의미의 return을 하지 않습니다. 대신 sret 명령을 실행함으로써 종료됩니다. 자, 이제 어떤 문제가 있는지를
보겠습니다. 문제는 사용자 레지스터를 저장하고 커널 레지스터를 로드하는 코드가

사용자의 주소 공간에서 실행된다는 점입니다. 따라서 이 명령어들과, 이들이 참조하는 메모리 위치들은 모두 사용자의 가상 주소 공간 안에 있어야 합니다. 마찬가지로 아래쪽에서
satp를 복원한 이후에는 다시 사용자의 주소 공간에서 실행하기 때문에, 그 명령어들 역시 그 주소 공간에 있어야 하고, 이전 레지스터 내용을 가져오는 메모리 위치들 역시

사용자의 주소 공간 안에 있어야 합니다. 이제 사용자 주소 공간을 살펴봅시다. 이 그림은 전에 보여준 적이 있는데, 사용자 가상 주소 공간에는 프로그램의 코드와 데이터가 들어
있고, 스택을 위한 페이지 하나, 그리고 필요하다면 힙을 위한 몇 개의 페이지가 들어 있습니다. 그리고 맨 위쪽에는 trampoline 페이지라고 부르는 것이 있고, 그 아래에
trapframe 페이지가 있습니다.

이 페이지들은 모든 가상 주소 공간에 존재하며, 정확히 동일한 위치, 즉 맨 위 두 페이지에 놓여 있습니다. 그 아래에 무엇이 어떻게 배치되는지는 사용자 프로그램에 따라
달라집니다. 스택 페이지가 정확히 어디에 위치하는지는 다양할 수 있지만, 이 두 페이지는 항상 같은 위치에 있습니다. 사용자 페이지 테이블은 이 두 페이지를 사용자 모드에서는 접근
불가로 표시합니다.

그래서 사용자 코드가 이들을 접근하려 하면 오류가 발생합니다. 즉, 사용자 코드 입장에서는 사실상 보이지 않는 페이지입니다. trampoline 페이지에는 코드가 들어 있으므로
읽기 및 실행 가능으로 표시되고, trapframe 페이지에는 데이터가 들어 있으므로 읽기 및 쓰기 가능으로 표시됩니다. 이제 여기 또 다른 그림이 있는데, 무슨 일이 일어나고
있는지를 보여줍니다. 잠시 후에 다시 돌아오겠지만, 여기에는

모든 사용자 프로세스의 가상 주소 공간들이 있습니다. 최대 64개의 사용자 프로세스가 있을 수 있고, 각 프로세스는 하나의 trapframe 페이지와 하나의 trampoline
페이지를 가집니다. 파란색이 trampoline, 빨간색이 trapframe입니다. 커널은 가상 주소 공간이 정확히 하나만 있고, 모든 커널 코드는 이 하나의 주소 공간을
사용합니다. 모든 코어는

이 하나의 가상 주소 공간을 공유합니다. 그리고 이 공간에는 여러 가지가 들어 있지만, 특히 강조하고 싶은 것은, 이 커널 주소 공간의 맨 위에도 trampoline 페이지가
포함되어 있다는 점입니다. 그 얘기를 더 하기 전에, 잠깐 trampoline 페이지와 trapframe 페이지를 다시 보겠습니다. 말했듯이 trampoline 페이지는
하나뿐이고, 이 안에는 코드가 들어 있습니다. 구체적으로는 uservec과 userret

함수가 들어 있습니다. 즉, uservec과 userret, 이 두 개의 어셈블리 언어 루틴이 들어 있고, 그 외에는 아무것도 없습니다. 이 페이지는 모든 가상 주소 공간에서
정확히 같은 주소, 즉 맨 위 페이지에 매핑되어 있습니다. 여기서 말하는 가상 주소 공간은 최대 64개의 사용자 프로세스를 위한 64개 각각의 사용자 가상 주소 공간과, 커널 주소
공간을 포함합니다. 그리고 말했듯이 읽기 및 실행 가능으로 표시되어 있습니다.

하지만 이 페이지는 유저 모드로 실행 중일 때는 접근할 수 없습니다. 트랩프레임 페이지가 있고, 각 프로세스는 자기 자신의 페이지를 하나씩 가집니다. 그래서 각 페이지는 서로
다릅니다. 최대 64개의 프로세스가 있고, 그중 일부는 휴면 상태로 실제로 실행 중이 아닐 수도 있지만, 동시에 활성 상태일 수 있는 프로세스는 최대 64개입니다. 그리고 각 가상
주소 공간은 자기 자신의 trapframe을 가지고, 그 안에 여러 데이터가 들어 있습니다. 특히 그 안에 우리가 사용자 레지스터들을 저장할 영역이 들어 있습니다.

그래서 이 영역은 읽기/쓰기 가능하지만 유저 모드에서는 접근 불가로 표시됩니다. 여기 아래 그림에서 제가 보여주는 것은 모든 64개 사용자 프로세스의 가상 주소 공간의 맨 위
부분이고, 맨 위 페이지가 trampoline 페이지, 그 바로 아래 페이지가 trapframe 페이지입니다. 아이디어는 페이지 테이블이 이 trampoline 페이지들을 전부
동일한 물리 페이지에 매핑한다는 것입니다.

정정하자면, 이 trampoline 페이지들은 전부 같은 물리 주소에 매핑되지만, 여기 파란색으로 표시된 trapframe 페이지들은 각각 서로 다른 물리 페이지에 매핑됩니다. 즉
공유되지 않습니다. 변경되지 않는 코드라면 문제 없이 공유할 수 있기 때문에 공유해도 괜찮지만, 각 사용자 프로세스는 레지스터를 저장하기 위한 자기만의 영역이 필요합니다.

이제 이 그림을 다시 보겠습니다. 오른쪽은 물리 메모리, 왼쪽은 커널의 가상 주소 공간입니다. 먼저 물리 메모리부터 시작해 보면, 0에서 시작해서 256 엑사바이트라는 엄청나게 큰
수까지 올라갑니다. 이 영역의 거의 대부분은 사용되지 않고, 할당도 되어 있지 않습니다. 위쪽에는 아무것도 없습니다.

xv6가 실행되도록 의도된 컴퓨터에는 물리 주기억장치 RAM이 128MB 장착되어 있고, 이 부분이 여기 영역에 있습니다. 이 RAM은 특정 주소에 위치하는데, 공교롭게도 2GB
경계에 놓입니다. 그 아래쪽에는 메모리 매핑된 장치들을 위한 공간이 있습니다. 직렬 통신 장치는 한 페이지를 차지하고, 디스크 장치도 한 페이지를 차지합니다.

또한 platform-level interrupt controller는 4MB를 차지하고, 그 아래에는 core local interrupt controller가 있습니다. 그
밑에 부트 ROM이 있고, 커널이 실행을 시작하고 부팅 과정이 끝난 뒤에는 커널이 이 부트 ROM을 전혀 접근하지 않습니다. 이제 왼쪽을 보면 커널의 가상 주소 공간이 보입니다.
첫 번째로 주목해야 할 점은,

모든 물리 메모리가 가상 주소 공간 안에 직접 매핑(direct-mapped)되어 있다는 점입니다. 이는 커널이 어떤 주소를 사용할 때, 그것이 가상 주소인지 물리 메모리 주소인지
굳이 구분할 필요가 없다는 뜻입니다. 같은 숫자를 물리 위치를 가리키는 데 사용할 수 있고, 비록 가상 주소 변환이 켜져 있어도 같은 위치로 가게 됩니다. 마찬가지로 아래쪽의 모든
것들도

직접 매핑되어 있습니다. 따라서 가상 디스크, 직렬 통신 장치, platform-level interrupt controller에 접근하기 위해 커널은 물리 주소를 그대로 사용할
수 있고, direct mapping이기 때문에 아무 문제가 없습니다. core local interrupt controller는 오직 머신 모드에서만 접근합니다. 머신 모드에
있을 때는 가상 주소 변환이 없고, 페이지 테이블도 활성화되어 있지 않으며, 오직 물리 주소만 사용한다는 것을 기억하십시오.

core local interrupt controller는 오직 머신 모드에서만 접근되므로, 이것을 가상 주소 공간에 매핑할 필요가 실제로는 없습니다. 이제 우리가 이야기하고 싶은
다른 부분은 가상 주소 공간의 맨 위에서 무슨 일이 일어나느냐 하는 점입니다. 256GB에서 한 페이지를 뺀 위치에 trampoline 페이지가 있고,

그 아래에 커널 스택 페이지라 부르는 여러 개의 페이지가 있습니다. 커널 스택 페이지는 사용자 프로세스마다 하나씩 있어서 총 64개가 있습니다. 이 모든 페이지들, 즉
trampoline과 스택 페이지들 사이에는 guard 페이지가 하나씩 끼어 있습니다. 이 guard 페이지는 매핑되지 않았고, 읽기/쓰기/실행이 전혀 불가능합니다. 따라서 이
페이지에 접근하려는 어떤 시도도 오류를 일으키고, 이로써 커널 스택 영역에서 발생하는

스택 오버플로를 잡아낼 수 있습니다. 커널 내부에서, 물리 주기억장치 안에는 여기 커널 코드와 read-only 데이터가 있고, 그 뒤를 커널의 read-write 데이터가
따릅니다. 이 부분이 커널에서 사용하는 변수들입니다. 그 위쪽 나머지 물리 메모리는 페이지 할당기(page allocator)에서 사용됩니다. 여기에는 kalloc과

kfree라는 두 함수가 있고, 이 메모리는 처음에 페이지 단위로 나뉘어 있습니다. 그 페이지들은 모두 free list에 들어 있습니다. 우리가 kalloc 함수를 호출할 때마다
free list에서 자유 메모리 페이지 하나를 할당합니다. 그러면 페이지 하나가 어떤 용도로 사용되도록 할당됩니다. 그리고 그 페이지 사용을 끝냈을 때 kfree 루틴을
호출하면,

이 영역으로 다시 반환되고 free list에 되돌아가서, 다음에 페이지가 필요할 때 재사용됩니다. 그래서 대부분의 물리 메모리는 kalloc과 kfree가 사용하는 이 페이지
영역으로 차지됩니다. 이제 이 그림은 같은 내용을 조금 다르게 보여 주려는 시도입니다. 다시 여기 어딘가에 물리 메모리가 있고, 이쪽에는

가상 주소 공간들이 있습니다. 사용자 프로세스가 64개 있으므로 여기에는 세 개만 그려 놨지만, 실제로는 각 사용자 프로세스마다 가상 주소 공간이 하나씩 있습니다. 커널에는 정확히
하나의 가상 주소 공간만 있고, 모든 코어는 이 하나의 커널 가상 주소 공간과 하나의 커널 페이지 테이블을 공유합니다. 여기 파란색으로 표시된 trampoline 페이지들이 모든
가상 주소 공간 맨 위에 있고,

각각의 유저 모드 가상 주소 공간 맨 위에는 빨간색으로 표시된 trapframe이 있습니다. 이 페이지들은 물리 메모리 어딘가에 매핑됩니다. trampoline 페이지는 코드에
매핑될 텐데, 이 코드는 실제로 커널 text 영역의 일부인 페이지입니다. 커널 코드는 물리 메모리의 이 영역에 들어 있고,

그래서 trampoline 페이지는 이 text 영역 어딘가에 매핑됩니다. trapframe 페이지들은 커널이 시작할 때 할당되며, free 영역 어딘가에 할당됩니다. 즉
kalloc과 kfree가 사용하는 페이지들이 있는 이 영역 어딘가에 할당됩니다. 그래서 제가 여기에서 그 trapframe들이 그 영역 어딘가에 매핑된 모양을 보여주고 있습니다.
물론 모든 물리 메모리는 direct map이 되어 있으므로,

커널은 필요하다면 이들을 직접 접근할 수 있습니다. 하지만 각 사용자 프로세스의 페이지 테이블들은 이 trapframe 페이지들을 kalloc 호출을 통해 얻은 페이지들 중 하나에
매핑합니다. 또한 여기 위쪽에는 이 커널 스택 페이지들도 있습니다. 64개 프로세스 각각에 대해 커널 스택이 하나씩 필요합니다. 그 이유는 우리가 처음 커널 모드로 들어갈 때,
트랩이 발생하면 실행을 시작하면서

사용자 레지스터를 저장하고 커널 레지스터를 적재하기 때문입니다. 각 프로세스는 당연히 별도의 스택이 필요합니다. 두 개의 서로 다른 스레드는 같은 스택을 공유할 수 없고, 각각
독립된 스택을 가져야 합니다. 그래서 64개 사용자 프로세스에 대응하는 64개 스레드 각각은 자기만의 스택을 가집니다. 따라서 유저 모드 코드가 커널 모드 실행으로 전환될 때

커널 스택에 접근할 필요가 있을 것이고, 그래서 여기에는 64개의 유저 프로세스 각각에 대해 하나의 스택 페이지가 있습니다. 시작할 때 64개의 페이지가 있고, 이 페이지들은
`kalloc`을 호출해서 free pool에서 할당됩니다. 그리고 `kalloc`이 호출될 때마다 그 페이지가 위쪽의 이 영역들 중 하나에 매핑됩니다. 그리고 여기에서 그
페이지들을 서로 구분해 주는 guard 페이지들을 보여주고 있습니다. 자, 이제 실제로

`memlayout.h` 파일의 코드를 볼 준비가 된 것 같습니다. 여기 페이지가 두 개밖에 없는데, 이쪽 것부터 시작해 봅시다. 몇 개의 주석이 있고, 직렬(serial) 장치의
주소로 시작합니다. 그게 여기 있는 이 주소입니다. 그래서 그 주소를 볼 수 있고, 여기 `#define` 상수로 정의되어 있습니다. 그리고 디스크 장치를 위한 페이지가 있고,
이건 바로 여기 표시되어 있습니다.

이게 우리가 정의하고 있는 이 주소입니다. 이것은 core local interrupter이고, 특정한 주소에 매핑되어 있고, 그게 여기 표시되어 있습니다. 이건 우리가 커널
모드로 실행할 때는 사용하지 않지만, 머신 모드에서는 사용됩니다. 그래서 여기 타이머와 타이머 인터럽트에 사용될 주소를 줍니다. 타이머 인터럽트는

머신 모드에서 실행되는 코드가 처리하고, 그 인터럽트를 만들어 내는 장치는 여기에서 접근합니다. 이런 메모리 맵 장치들은 레지스터라고 불리는 것들을 가지고 있는데, 때로는 하드웨어
레지스터라고 부르기도 합니다. CPU의 범용 레지스터와 혼동하면 안 됩니다. 여기서 일어나는 일이 바로 그것입니다. `mtime`이라고 불리는 레지스터가 하나 있고, 이게

어디에 위치하냐 하면, 이 시작 위치에 어떤 상수를 더한 주소에 있습니다. 그리고 그 하드웨어 레지스터에는 부팅이 일어난 이후의 사이클 수가 들어 있습니다. 그래서 커널 코드는 그
주소에서 읽을 수 있고, 사실상 이 장치에 접근해서 현재 값을 얻습니다. 이 장치는 이른바 메모리 위치에 저장된 값을 계속해서 갱신하고 있고,

우리는 현재 시간이 필요할 때 그냥 이걸 읽기만 하면 됩니다. 여기 있는 `mtimecmp`는 함수입니다. C 전처리기에서 여기 공백이 있는지 없는지가 중요하다는 걸 떠올려
보세요. 공백이 있으면 이건 그냥 그대로 치환되는 값이고, 공백이 없으면 이건 매개변수, 혹은 인자가 되고, 이건 함수를 정의하는 것이 됩니다. 그래서 특정 코어 번호가 주어지면,

여기 있는 식이 그 레지스터들 중 하나의 주소를 계산합니다. 코어가 8개 있고, 따라서 하드웨어 레지스터도 8개가 있습니다. 그 레지스터들은 어디에 위치하냐 하면, 시작 주소에
어떤 값을 더한 곳에 있고, 각 레지스터는 8바이트입니다. 그래서 이 식이 이 하드웨어 레지스터들 중 하나의 주소를 계산합니다. 이 레지스터는

커널이 값을 적어 넣을 것이고, 다음 인터럽트를 언제 줄지를 알려 줍니다. 커널이 이 위치에 값을 쓰고, 그리고 인터럽트가, 아니, 그 시간이 도달하면 이 장치가 자동으로
인터럽트를 생성합니다. 그리고 platform-level interrupt controller에 대해서도 비슷한 일이 일어납니다. 시작 주소가 있고, 여러 가지

서로 다른 함수들이 있고, 코어 번호의 함수로 정의된 서로 다른 하드웨어 레지스터들을 볼 수 있습니다. `hartid`는 코어 번호 0에서 7까지이고, 이 식들이 그 장치 내부의
여러 주소들을 계산합니다. 자, `memlayout`의 두 번째 페이지로 넘어가면, 커널이 어디에 로드될지의 정의가 있습니다. 말했듯이, 여기 있는 수는

2GB이고, 이게 바로 커널 베이스, 즉 커널이 로드되는 이 지점입니다. 또한 물리적 메인 메모리의 꼭대기도 있습니다. 시작 위치에서부터 128MB의 메인 메모리가 있고, 그 끝이
물리적 메인 메모리의 꼭대기입니다. `PHYSTOP`은 그래서 바로 여기 이 주소를 나타냅니다. 이 상수 `TRAMPOLINE`은 trampoline 페이지의 주소를 나타내는데,
이건 최대 가상 주소에서

한 페이지를 뺀 값, 즉 256GB에서 한 페이지를 뺀 값입니다. 그러니까 바로 이 지점입니다. 그리고 마지막으로 스택 페이지들의 주소가 있습니다. 이건 함수이고, 프로세스 번호
0에서 63이 주어지면 여기 어떤 주소를 계산합니다. 이 대수식을 직접 풀어볼 수 있겠지만, 기본적으로는 guard 페이지 때문에 두 페이지 단위로 계산합니다. 그래서 두
페이지마다 하나의

스택 페이지 주소가 있습니다. 마지막으로 `TRAPFRAME`이라는 상수가 있는데, 이건 trapframe 페이지의 주소입니다. 즉 trampoline 페이지의 주소에서 4096을
뺀 값이고, 바로 이 값입니다. 이 정의에서 일어나는 일이 바로 이것입니다. 자, 메모리 레이아웃은 여기까지입니다. 다음 비디오에서 봅시다.
