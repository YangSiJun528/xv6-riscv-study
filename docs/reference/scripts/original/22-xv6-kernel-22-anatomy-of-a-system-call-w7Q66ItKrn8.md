# xv6 Kernel-22: Anatomy of a System Call

| Field | Value |
| --- | --- |
| Video ID | `w7Q66ItKrn8` |
| URL | <https://www.youtube.com/watch?v=w7Q66ItKrn8> |
| Source | `docs/reference/scripts/original-raw/22-xv6_Kernel-22_-_Anatomy_of_a_System_Call-w7Q66ItKrn8.en-orig.md` |
| Status | machine-cleaned from YouTube `en-orig` automatic captions; review recommended |

---

## Corrected Transcript

What exactly happens during a system call this video is titled anatomy of a system call and in it
I'll walk through all the actions that occur whenever system call is invoked I'll look at the s
break system call as an example s-break is found in unix and linux systems and we're talking about
the xv6 operating system kernel and that is a simplified version of unix

and linux here are the functions and files that we'll be discussing in the in this video every user
mode program that wants to make a system call will need to include this user.h file and we have a
number of assembly code functions in usys.s that are run in user mode the definitions in syscall.h
are used both by the user mode code and by the kernel code so this file is

shared between both user mode and kernel code once the trap occurs we will be thrown into the code
in the trampoline page this is assembly language code and the uservec code will then invoke the user
trap function user trap function will determine that we've got a system call and invoke the function
syscall syscall will determine which system call we want and it will in our case invoke

s-break to do the system call that we're going to be looking at s-break will then invoke grow proc
which will actually increase the size of the address space and then when everything returns syscall
will then end uh by going back to usertrap rec and usertrapret will then jump to userret the
assembly language code which will execute the srep instruction and bring us back into executing user
mode

code so let's begin with user.h any user mode program that wants to invoke a system call needs to
include this file and it contains a number of function prototypes so it contains one prototype for
each of the 21 system calls and in particular we see the prototype for s break here s break is
passed a single argument and that argument is the amount to grow the heap by it can be positive or
it

could be negative if you want to shrink the heap it can also be zero it returns the previous size of
the heap so if you want to know how big the heap is you can just invoke it with zero in the risk by
risk five system arguments are passed in registers a0 a1 and a2 and so on so this function has three
arguments ours only has one so only a zero would be used if if there

is a return value and all of the system calls have a return value that would be placed in a zero
this function also includes sorry this file user.h also includes a number of other definitions for
helper functions like printf and stringlength and so on but we won't talk about those here so next
let's uh ask which function gets invoked when a user mode program calls

something like s-break or fork or read or write or any other system calls well the answer is a short
assembly language program that runs in user mode is invoked those uh functions are also often called
stub routines and they're all collected and all found in this file usys.s this is an assembly
language file that contains a short stub routine for each of the 21 system calls this file is

actually automatically generated by a perl script usasis.pl but I've shown here what goes into that
file it begins with an include which is syscall.h and let me just stop here because this is called
syscall.h is pretty straightforward and I can dispense with it quickly syscall.h associates a number
with each of the system calls this number is how the user mode code

communicates with the kernel as to which system call is desired in our case s break is 12. okay and
so we include those definitions here and then for each one of the 21 system calls we have these five
lines in the file I've only shown the code the stub routine for s-break but it's repeated once for
each of the other 20 system calls and what does this dub function look

like well it's got a label and a return so it's like any function it's also got a global which
exports that label so it's visible in the linking phase but primarily it has these three
instructions load immediate e call in red load immediate will move into register a7 some value in
this case we remember that the s break constant was 12. so this just moves 12 into a7

and then we execute the environment call or e-call instruction which performs the system call at the
point which any of these global these uh stub functions is called we can expect the arguments to be
in registers a0 through a5 we can accommodate up to six arguments to any system call and those
registers will be unchanged by this load immediate and so they'll still

be valid and the kernel will fetch the values that it saved for those registers and that's how it
accesses the arguments the kernel code will also place or overwrite the a0 register with whatever
the return value should be so after the kernel comes back that is after we resume executing in user
mode at this point a0 will contain our result and we don't touch it we just do the

return instruction so in risk five whenever a function is called the return address is placed in
register r a return address and then the return instruction just simply copies the value in r a to
the program counter the kernel will preserve all registers except for a0 and so we can be sure that
at this point the value that was in our a here will still be there and so this

can return properly okay so we're executing in user mode and at some point an e-call instruction is
executed and that causes a trap after that we execute in kernel mode and then finally we execute the
sred instruction in kernel mode and that returns us to user mode and we resume execution directly
after the ecall instruction which is the return instruction in this dub routine

so let's look at that in a little bit more detail here is the process from the trap all the way down
to the s red instruction a trap will change the mode into kernel mode disable interrupts save the
program counter and jump to the uservec function user func uservec is located in the trampoline page
and it will save the user registers load the kernel registers

and change to the kernel's virtual address space and finally it will jump to user trap we'll look at
user trap in more detail but it will figure out that it's a system call and so on but eventually
user trap will call user trap ret and that will then invoke user rep which is assembly code and that
will finish the process and execute the srad instruction in particular we'll go back

to the user's address space restore the user's registers and then the s-red instruction itself will
change the mode to user mode and restore the program counter to whatever was saved and re-enable
interrupts here's a diagram I used in an earlier video showing the same thing in a little bit more
detail from the trap to the esret instruction again you see the

assembly code in the trampoline page at uservec being invoked to save the state of the process it
jumps to the user trap function which then does some stuff and eventually calls user trap ret and
that restores the state of the user process along with user rep and ultimately executes the esret
instruction so what happens in user trap well we have a big if statement that

determines the cause of the trap is it an exception or is a device requiring attention or is it a
timer interrupt or is it a system call if it's a system call we enable interrupts and then invoke
the syscall function and after that returns it invokes the user trap ret to begin the process of
going back to the user mode code we also check the killed flag there's a boolean value that is set

if somebody wants this process to die and if it was set then we invoke exit I've also hinted at here
that there could be some process switching going on the moment we re-enable interrupts it's possible
that a timer interrupt could occur or a device could require attention so the scheduler could get
involved and I tried to indicate that over here to say that any time between

the interrupts being enabled here and the interrupts being disabled at the beginning of user trap
ret it's possible that we could switch to another process but eventually we'll come back and finish
executing whatever code is involved with the system call so now let's take a look at the user trap
function in more detail so I'm not going to go over everything

here but we basically have an if statement that looks at the status and control register called
s-cause to determine whether it's a system call or something else and so we have some other stuff
down here in the if statement but with risk five the s-cos will have a value of eight if it's a
system call so that's what happens here we basically execute the system call function

first we check the killed flag to see whether someone has set it and if so we invoke the exit
function exit will never return so if that happens this process will stop right here but assuming it
continues going we then look at the epc field in the trapframe when the trap occurred the code and
userback will save all the registers and the program counter will have gotten

saved in the epc field the program counter will point to the e-call instruction itself and we need
to advance it past the e-call instruction which is four bytes to the next instruction that's
presumably the return instruction in the stub routine the e-call instruction is four bytes so we
advance it to point to the next instruction so when we return we'll execute that

return instruction here we turn interrupts on and then we invoke this call after syscall returns
down here we check the killed flag again and perhaps exit right there the uh which device would be
set if there's a device interrupt but we initialize it up here to zero and it's not changed so we
will not execute this yield function here and then continuing uh with this

function uh after the yield we call user trap rep and then that which is shown right here basically
restores the state and ends up going back to the user rep function down here and returning to the
user mode code after the function user trap figures out that the cause of the trap was a system call
it will invoke the syscall function which is located in the syscall

syscall.c file and here's what happens remember at the time of the trap the code will save all of
the registers of the user mode process and the stub function has preloaded into register a7 a number
in the case of s break it would be 12 a number which indicates which system call is required so the
first thing we do is we get a pointer to the proc structure and then

we chase it to the trapframe and look at the a7 the register a7 field and retrieve that okay then
what we're going to do is we're going to use that as an index into an array so let me take a look at
what occurs earlier in this file this is a collection of function prototypes and here we see s break
all lowercase this is the function we're going to be calling to get the work done

and then we have an array this array is called syscalls and it's a bunch of pointers to functions
and so for number 12 where is it here it's uh right here uh we have the value of a pointer to the
function cis s break okay and uh even though these aren't in quite the same order all 21 system
calls are represented with functions to execute the system call and

uh the array has all 12 entries for each one of those so that's what's going on here in syscall we
look up the and get the pointer to the function and then we invoke that function okay it's past
nothing and it's going to return a value okay and that value will be stored in the trapframe in a0
overwriting whatever was in a zero okay so that's where we actually call

the s break function before we do the call we just check to make sure that the user mode code is
providing a legal number so we check to make sure it's greater than zero and that it's an entry in
the syscalls array and that that pointer is uh has been initialized to something and is not null so
if it's all okay we uh invoke it but if the user has provided a bad system

call number then we print a message normally the kernel wouldn't just directly print a message for
this sort of thing but it would just return an error code so we do see the error code being returned
we're writing a minus one into a0 but we're also printing uh some information here so we can debug
both the kernel and the user mode program the process id the processes name

and the number that was actually provided okay so in this file we have a couple of other functions
so I want to look at this first one these functions are helper functions that will be used in
functions like sysbreak that implement the actual system call so we need a way to fetch the
arguments so our raw will be passed a number that's the number of the argument you want

zero through five zero so we're switching on zero through five here and all it does is it goes into
the trapframe and returns the value of the users register a0 a1 a2 3 4 or 5.

and then it returns that value uh that that is used by this next function argent and argent is uh
just going to call our raw but it's also going to store that value it's passed a pointer to an
integer and so it stores it in that integer variable and we have a couple of others there's uh our
address it's exactly the same code so the only difference is that uh the

variable that is being stored into it has a type uh uen64 as opposed to an integer here there's also
uh one for arg string okay so fetch the nth word size system call argument okay so that's the number
between zero and five as a null terminated string copy it into buffer and at most max characters so
uh in addition to the number of the argument we want we passed a pointer to

some buffer and its size and it returns the string length of bouquet and -1 if there's a problem so
here we are fetching the address here and putting in a local variable address and then we have this
function fetch string to do the work and fetch string is right here fetch the null terminated string
at address from the current process and it returns the length of the string

and -1 for an error so here we see that it's calling copy in string we discussed that in a previous
video when I discussed the virtual memory functions helper functions so basically it's going to use
the current procedures page table and here's the address in kernel memory and here's the virtual
address and the maximum number of characters to copy and then it's going to copy that return the

string links there's also another helper function called fetch address and the idea is the argument
to the system call will be an address and we want to go into the virtual address space and fetch a
64-bit value from the virtual address space so this is past the virtual address and the value that's
fetched will be stored and it will be stored at the location

pointed to by this pointer here so to do this we see that the copy in function is used we passed a
page table to indicate which virtual address space it is and here is ip that's the place where we're
supposed to store the uh eight bytes right this thing will be 64 bits or or eight bytes and here's
the virtual address and here is the number of bytes that we need to copy

and well ip is a pointer to 64-bit value so the size of that is eight bytes so this is eight here we
see it expressed slightly differently here so this is also 8 bytes first I want to say that if
there's a problem we just return -1 otherwise we return 0.

here we're checking to make sure that the address that the user mode program has provided is correct
and we see that we're checking to make sure that the address well it better be somewhere between
zero and the size of the address space right the top of the heap the break uh point so we're
checking to make sure that it is less than the break address okay and then uh we are adding well

eight bytes to that and again we are uh looking to see whether uh it is not too large okay and if
there's a problem with it we return minus 1.

now you might say why in the world would we perform this check because clearly if this is true and
then you add 8 to address this is going to definitely be true but here is the thing what about wrap
around we're using unsigned integers what if address is way up at the very top of the 64-bit values
and when you add 8 to it it wraps around so this one could be

okay while this one is not but if this one is below the size of the virtual address space which is
limited to substantially limited then we won't have a wrap around here so this is the kind of thing
where you have security flaws in kernels so you need to be really really careful with all possible
values that the user code could provide and so this is an example why

this second check is actually necessary now we're ready to look at the s-break function that
actually does the work it's located in the file sysproc.c along with a bunch of other functions for
other system calls some are pretty simple like here's the one for the get pid and all it does is
simply go to the proc structure and fetch the process id and return that

each of these functions is passed no arguments and returns a return value that will go into a0 other
functions or are more complicated here we see the fork system call and it's just calling this
function fork which is located elsewhere to do all the work let's go to let's go down here to where
we see uh this is so let's go down to where we see uh sysbreak and we see that it will call

the function grow proc to do the work but first it has a single argument which is number zero in
register a0 so local variable n is here the call to argent is just going to fetch the value from
register a0 and place it in the variable in and we are then going to capture the previous size of
this process so we are just getting the size field and saving it here and that's what

we will be returning and then we talk call grow proc to do the work finally let's look at the grow
proc function which comes from the file proc dot c so this will be passed a number of bytes and it
will grow or shrink the user memory returning minus one if there's a problem we check the number of
bytes to see if it's positive in which case we call uvm alec or if

it's negative we call uvm dialic or if it's zero we do nothing and uvm alec is past the old size and
the new size so here we're capturing the old size from the proc structure p and adding a positive
number to it and if there is any problem for example if the pages cannot be allocated we return -1
otherwise we will be returning 0.

if we are shrinking the address space then n is a negative number and here the old size is larger
than the new size and uvm dialect will always succeed so we don't need to check but in any case both
of these return the size of the address space afterwards so we save that in the size field of the
proc structure okay that's it for the anatomy of a system call I will see you in the next

video
