# 시작하면서

확실히 pintos 하고 나서 배경 지식이 있는데다가 코드가 더 간단함.

그리고 강의가 되게 좋다. 지금 쭉 영상 보면 과정 전체 복습하기 좋을 듯?

일단 인상깊은건?

영상
- 자료를 프린트로 뽑아서 보여주는데 생각보다 직관적임
- 설명이 되게 좋다. 핀토스 구현하기 전에 이걸 봤으면 어땠을까 싶음.
  - (아마 배경지식 문제로 이해 못했을거 같다는 생각이 들지만,)

코드
- 배열 위주로 간단하게 구현함.
  - 프로세스 리스트도 최대 크기가 고정된 배열을 사용함
- 스핀락만 있거나, 스케쥴러 계획도 하나인 등 되게 단순함.
  - 실제 구현에 중점을 두지 않는 모습
- 멀티 코어 프로세스임. 단, 캐싱이나 프로세스 간 통신과 같이 따다로운 부분은 고려하지 않음.

공부는 유튜브 영상 보고, AI 사용해서 내가 복기하기 좋은 형태로 정리하고,
인상깊거나 정리하고 싶은 내용 쓰는 식으로 갈 듯?

# 1. Intro / 2. General Features

AI 정리: [docs/ai/001_xv6_script_01-02_intro_general_features_20260524.md](/docs/ai/001_xv6_script_01-02_intro_general_features_20260524.md)

확실히 pintos에 비하면 단순화되어있다.

- 멀티 코어 프로세스임.
  - pintos는 싱글코어였음. 근데 코드 상의 큰 차이는 없는듯? (이따 스핀락 챕터에서 추가 설명)
  - 캐싱이나 프로세스 간 통신과 같이 까다로운 부분은 고려하지 않음.
- 메모리 레이아웃
  - 특히 메모리의 스택 영역은 고정이고, 위치가 다르다.
  - 커널 내부에선 malloc 기능이 없고, page 단위로만 할당 가능.
  - pintos랑 다른 부분은 heap이 있다는거?
- 배열 위주 구현
  - 간단하게 구현하기 위해서 linked list를 잘 안쓰는듯
  - 프로세스, CPU, File 등에 최대 크기가 고정된 배열을 사용함 ([param.h](/kernel/param.h) 참고)

# 3. Startup Organization

AI 정리: [docs/ai/002_xv6_script_03_startup_organization_20260524.md](/docs/ai/002_xv6_script_03_startup_organization_20260524.md)

Pintos에선 명확하게 다루지 않던 부분이 다뤄지는 것은 좋음.

실제로 로드가 어떻게 되는가?

(실제 OS는 부트스트랩 과정이 있어서 다르지만, 하드웨어 신호부터 시작까지의 과정을 보여준다는 점에서)

내용은 AI 정리로도 충분한 듯?

# 4. Spin Locks

AI 정리: [docs/ai/003_xv6_script_04_spinlocks_20260524.md](/docs/ai/003_xv6_script_04_spinlocks_20260524.md)

- 스케줄러와 락
  - xv6 스케줄러는 단순한 라운드로빈이고, 우선순위 스케줄링은 없다.
  - 그래서 우선순위와 우선순위 기부와 관련된 기능이 없는데, 우선순위 스케줄링 자체가 없으니 이상한 일은 아님.
    - 그래서 해당 내용 관련해서는 Pintos나 CSAPP([쉬운코드 영상](https://youtu.be/gTkvX2Awj6g)) 참고하기
  - 락도 단순한거만 있는데, spin lock과 sleep lock 정도만 있다.
    - sleep lock은 mutex에 가까운 sleep 기반 lock인데, 커널 내부용이고 우선순위 기부 같은 기능이 없어서 mutex는 아니라고 생각함.
  - mutex, semaphore나 condition variable가 없음.
- xv6 구현 특징
  - 핵심은 Pintos나 CSAPP 책의 내용과 크게 다르지 않음.
  - 멀티프로세서 관점에서 비교할만한건,
    - `acquire()`에서 인터럽트를 끄고, `release()`에서 복구함. cnt 기반으로 중첩을 고려해서 복구함.
    - 주요 목적은 데드락을 막는 것.
      - 다른 스레드(혹은 core)의 접근을 막는건 락 자체가 해결하므로 고려대상 아님.
      - 같은 CPU에서 interrupt handler가 같은 lock을 다시 잡으면 데드락 발생.
    - 그래서 함수 내부에서만 끄면 부족하고, lock을 잡은 뒤 `release()` 전까지 critical section 전체에서 꺼져 있어야 함.
    - 스핀락은 짧은 부분에서만 동작하는걸 의도하고 구현의 편의를 위해서 그렇게 함.

# 5. Kalloc Memory Management

AI 정리: [docs/ai/004_xv6_script_05_kalloc_memory_management_20260524.md](/docs/ai/004_xv6_script_05_kalloc_memory_management_20260524.md)

