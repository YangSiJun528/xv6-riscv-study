# AGENTS

- Write requested files or documents in `docs/ai/` by default.
- Name AI-generated documents as `NNN_TOPIC_YYYYMMDD.md`, continuing the next 3-digit number from existing files.
- Keep `docs/ai/` documents as concise as possible while preserving the same meaning.
- Keep this agent document as concise as possible while preserving the same meaning.
- Treat `docs/daliy/` as user-owned study notes; read it only as reference and never write AI-generated content there.
